# M5.5 验收报告（1:1 复刻 CC 真实 UI + 鲸鱼蓝辨识度）

日期：2026-06-13。M5.5 是 M5 验收中途的方向调整：**不要「CC 风格」，要 1:1 复刻 CC 真实 UI**，仅两处保留 DeepSeek 辨识度——鲸鱼蓝 accent + deepcode 欢迎页（🐳）。计划见 `docs/plans/2026-06-13-deepcode-cc-ui-1to1.md`（Task 1-6）。

## 落地范围（T1-T6 + 后续打磨）

| 元素 | 组件 | 状态 |
|---|---|---|
| 工作 spinner（✻ 动画 + 中文动词 + 秒 + token + esc 中断） | `Spinner.tsx` + useChat 计时/计数 | ✅ |
| 双列欢迎框（🐳/欢迎/鲸鱼/账号/cwd ∣ 上手/小贴士/更新） | `Banner.tsx` | ✅ |
| CC 状态页脚（[模型∣模式]∣cwd git / Context 条 / N DEEPCODE.md / ✓工具×n / 命令提示） | `StatusFooter.tsx`（替代已删的 StatusLine 指标行） | ✅ |
| 工具行 `⏺ Name(主参)` + `⎿ preview … +N 行` | `ToolLine.tsx` + `toolArg.ts` | ✅ |
| Transcript：user `>`、assistant `⏺`、reasoning dim 斜体折叠 | `Transcript.tsx` | ✅ |
| 输入框 `❯` + placeholder + 圆角蓝上下线 | `InputBox.tsx` | ✅ |
| 权限弹窗编号菜单 + 问句 + 数字键 | `PermissionDialog.tsx` | ✅ |
| IME 组字光标定位（TTY 硬光标 park 到输入 caret） | `App.tsx` | ✅（pyte 验证） |

## 本次会话收口批（用户对照真 CC 截图的四项决策 + 一个预存修复）

用户用 AskUserQuestion 拍板四项，逐项落地：

| 决策 | 改动 | 文件 |
|---|---|---|
| **强调色提亮** `#4D6BFE → #6E8BFF` | accent 是全 UI 唯一强调色来源，边框/❯/选中态/banner/鲸鱼一起变；深色终端更跳 | `theme.ts` + `tui.theme.test.ts` |
| **/init 文件名 → `DEEPCODE.md`** | 去掉「DeepSeek 工具却生成 CLAUDE.md」的品牌错乱，与全局 `~/.deepcode/DEEPCODE.md` 统一。INIT_PROMPT 写 DEEPCODE.md；读取优先级改 `DEEPCODE.md › CLAUDE.md › AGENTS.md`（向后兼容旧仓库）；/help、suggest hint、Banner、footer 文案全同步 | `commands.ts` `prompt.ts` `suggest.ts` `useChat.ts` `repl.ts` `StatusFooter.tsx` + `prompt.test.ts`/`tui.statusfooter.test.tsx` |
| **吉祥物 → 像素块鲸鱼** | 线条 ASCII → 实心半角块（尾鳍 ◀ / 喷水 ╲╿╱ / 圆身 / 负空间眼），单 accent 色；保留老版轮廓语义 | `Banner.tsx` |
| **细节打磨**（多选） | ① cwd 缩写 home 为 `~`（tildify）② 左列内容居中（鲸鱼整块保形居中、文字各行按可见宽居中）③ 右列加「更新」段（What's new） | `Banner.tsx` |
| **预存修复**：`npm test` 不确定性 | 仓库无 vitest 配置 → 默认 glob 误扫 `bench/fixtures/*.test.mjs`（给 agent 修的「故意写错」基准夹具，node:test 写的），导致 `npm test` 时好时坏。新增 `vitest.config.ts` 锁定 `include: ['test/**/*.test.{ts,tsx}']` | `vitest.config.ts` |

## 验证

- **typecheck**：`tsc --noEmit` 干净。
- **单测**：`npm test` → **32 文件 / 210 通过**（确定性，含新增 DEEPCODE.md 优先级用例）。
- **pty 冒烟**（`/tmp/ds-smoke/smoke55.py`，真 pty，假 key 不触发 API）：**13/13 通过**。
  - 欢迎框 🐳 / placeholder / 页脚三行（[模型∣模式]、Context、/ 看命令）/ 旧状态行已删（无「缓存命中」「tok/s」）/ 提亮蓝 accent 颜色码 `38;2;110;139;255` / `/` 命令菜单 / `@sample` 文件菜单 / `!` bang 直跑 / `/exit` 全名单次回车退出（P4）。
  - 备注：`/exit` 须按现实打字节奏（打全名→ink 重渲染→精确匹配菜单收起→回车）。把 `/exit\r` 一次性原子写入会触发竞态（所有按键同批处理、回车到达时 suggestionsActive 仍是旧值）——这是 pty 原子写入的非真实路径，真实终端按键有间隔不受影响。
- **用户真终端核对**：颜色（提亮蓝）与鲸鱼像素块形状已由用户在真终端确认通过。

## 已知权衡 / 带入 M6

- 鲸鱼眼睛用「负空间」（块中留洞），单色下略含蓄；如需高对比黑眼，需把眼睛行拆成双色 Text 段（待用户进一步反馈）。
- 「更新」段内容当前为静态两行（手写 M5.5 亮点），无 CHANGELOG 数据源；M6 做版本来源时一并接真实 release 数据。
- 版本号仍停 0.1.0（M6 校正版本来源）。
- ansitoimg 截图对 ink 增量重绘会叠帧 + 偶尔降 truecolor，仅作粗略存档；视觉以真终端为准。
