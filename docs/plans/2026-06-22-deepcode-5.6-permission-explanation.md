# 5.6 权限弹窗 deny / 来源层级展示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 权限决策携带 `PermissionDecisionReason`，弹窗与硬拒绝文本展示命中的 deny 规则及其来源层级（builtin/user/project/local/flag）。

**Architecture:** settingsLayers 产出 per-rule provenance（allow+deny），deny.ts 合并 builtin 源，permissions.ts 新增命中规则查找 + 在 checkPermission 返回/透传 decisionReason，useChat/headless 注入来源映射，PermissionDialog 渲染来源行。镜像 CC 结构，剪到 deepcode 实有变体。

**Tech Stack:** TypeScript/ESM、vitest、ink5（React TUI）、picomatch、shell-quote。

## Global Constraints

- 测试框架 vitest（`import { describe, it, expect, vi } from 'vitest'`）；命令 `npx vitest run <file>`。
- 全量门禁：`npm test` + `npx tsc --noEmit` + `npm run build` 全绿方可合并。
- 显示文案中文（与现有弹窗/拒绝文本一致）。
- 来源集合固定：`'builtin' | 'user' | 'project' | 'local' | 'flag'`（不引 CC 的 policy/cliArg/command/session）。
- 改写权限/规则的测试务必同步 mock，避免真写 `~/.deepcode`（教训沿用 3.9）。
- 类型-only import 跨 deny.ts↔permissions.ts 用 `import type`，避免运行时循环依赖。
- 不引 CC 的 `subcommandResults`：复合 Bash 命中收敛为单 `rule`。

---

### Task 1: 决策原因类型 + 来源显示名

**Files:**
- Modify: `src/permissions.ts`（在文件顶部 `Decision` 类型附近，约 70-71 行后新增）
- Test: `test/permissions.test.ts`

**Interfaces:**
- Produces:
  - `type PermissionRuleSource = 'builtin' | 'user' | 'project' | 'local' | 'flag'`
  - `interface PermissionRule { source: PermissionRuleSource; behavior: 'allow' | 'deny'; value: string }`
  - `type PermissionDecisionReason = { type: 'rule'; rule: PermissionRule } | { type: 'hook'; hookName: string; reason?: string } | { type: 'other'; reason: string }`
  - `function permissionSourceName(s: PermissionRuleSource): string`

- [ ] **Step 1: Write the failing test**

在 `test/permissions.test.ts` import 行加入 `permissionSourceName`，文件末尾追加：

```ts
import { permissionSourceName } from '../src/permissions.js'

describe('permissionSourceName', () => {
  it('五个来源映射到中文显示名', () => {
    expect(permissionSourceName('builtin')).toBe('内置规则')
    expect(permissionSourceName('user')).toBe('用户设置')
    expect(permissionSourceName('project')).toBe('共享项目设置')
    expect(permissionSourceName('local')).toBe('项目本地设置')
    expect(permissionSourceName('flag')).toBe('命令行参数')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/permissions.test.ts -t permissionSourceName`
Expected: FAIL（`permissionSourceName` 未导出）

- [ ] **Step 3: Write minimal implementation**

在 `src/permissions.ts` 的 `export type Decision = 'yes' | 'no' | 'always'`（第 71 行）下方插入：

```ts
export type PermissionRuleSource = 'builtin' | 'user' | 'project' | 'local' | 'flag'

export interface PermissionRule {
  source: PermissionRuleSource
  behavior: 'allow' | 'deny'
  value: string
}

export type PermissionDecisionReason =
  | { type: 'rule'; rule: PermissionRule }
  | { type: 'hook'; hookName: string; reason?: string }
  | { type: 'other'; reason: string }

const SOURCE_NAMES: Record<PermissionRuleSource, string> = {
  builtin: '内置规则',
  user: '用户设置',
  project: '共享项目设置',
  local: '项目本地设置',
  flag: '命令行参数',
}

/** 来源层级 → 中文显示名。 */
export function permissionSourceName(s: PermissionRuleSource): string {
  return SOURCE_NAMES[s] ?? String(s)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/permissions.test.ts -t permissionSourceName`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/permissions.ts test/permissions.test.ts
git commit -m "feat(5.6): PermissionDecisionReason 类型 + permissionSourceName"
```

---

### Task 2: settingsLayers per-rule provenance

**Files:**
- Modify: `src/settingsLayers.ts:70-104`（mergeScopePartials）、`109-113`（LayeredResult）、`170-171`（loadLayeredSettings 返回）
- Test: `test/settingsLayers.test.ts`

**Interfaces:**
- Consumes: `SettingScope`（已存在）
- Produces: `mergeScopePartials` 返回新增 `permissionSources: { allow: Record<string, SettingScope>; deny: Record<string, SettingScope> }`；`LayeredResult.permissionSources` 同型。

- [ ] **Step 1: Write the failing test**

在 `test/settingsLayers.test.ts` 末尾追加：

```ts
import { mergeScopePartials } from '../src/settingsLayers.js'

describe('mergeScopePartials permissionSources', () => {
  it('per-rule 归属到贡献 scope，重复取最高优先级', () => {
    const { permissionSources } = mergeScopePartials([
      { scope: 'user', partial: { permissions: { allow: ['Bash(ls)'], deny: ['~/.ssh/**'] } } },
      { scope: 'local', partial: { permissions: { allow: ['Bash(ls)', 'Read(/tmp/x)'], deny: ['~/.aws/**'] } } },
    ])
    expect(permissionSources.allow['Read(/tmp/x)']).toBe('local')
    expect(permissionSources.allow['Bash(ls)']).toBe('local') // 重复 → 最高优先级 local
    expect(permissionSources.deny['~/.ssh/**']).toBe('user')
    expect(permissionSources.deny['~/.aws/**']).toBe('local')
  })

  it('无 permissions 时为空映射', () => {
    const { permissionSources } = mergeScopePartials([{ scope: 'user', partial: { model: 'x' } }])
    expect(permissionSources).toEqual({ allow: {}, deny: {} })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/settingsLayers.test.ts -t permissionSources`
Expected: FAIL（返回值无 `permissionSources`）

- [ ] **Step 3: Write minimal implementation**

`src/settingsLayers.ts`：

1. 改 `mergeScopePartials` 返回类型签名（第 70-73 行）：

```ts
export function mergeScopePartials(layers: ScopePartial[]): {
  settings: any
  provenance: Record<string, SettingScope | 'merged'>
  permissionSources: { allow: Record<string, SettingScope>; deny: Record<string, SettingScope> }
} {
```

2. 在 `return { settings, provenance }`（第 103 行）前插入累积逻辑并改 return：

```ts
  const permissionSources: { allow: Record<string, SettingScope>; deny: Record<string, SettingScope> } = { allow: {}, deny: {} }
  for (const { scope, partial } of layers) {
    const perm = partial.permissions as { allow?: string[]; deny?: string[] } | undefined
    if (perm?.allow) for (const r of perm.allow) permissionSources.allow[r] = scope
    if (perm?.deny) for (const r of perm.deny) permissionSources.deny[r] = scope
  }
  return { settings, provenance, permissionSources }
```

（layers 已按 user→project→local→flag 优先级排列，后写覆盖天然给出最高优先级 scope。）

3. `LayeredResult`（第 109-113 行）增字段：

```ts
export interface LayeredResult {
  settings: Settings
  provenance: Record<string, SettingScope | 'merged'>
  permissionSources: { allow: Record<string, SettingScope>; deny: Record<string, SettingScope> }
  scopes: LoadedScope[]
}
```

4. `loadLayeredSettings`（第 170-171 行）：

```ts
  const { settings, provenance, permissionSources } = mergeScopePartials(layers)
  return { settings: settings as Settings, provenance, permissionSources, scopes }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/settingsLayers.test.ts`
Expected: PASS（全部）

- [ ] **Step 5: Commit**

```bash
git add src/settingsLayers.ts test/settingsLayers.test.ts
git commit -m "feat(5.6): settingsLayers per-rule provenance（allow+deny）"
```

---

### Task 3: deny.ts buildDenySourceMap

**Files:**
- Modify: `src/deny.ts`
- Test: `test/deny.test.ts`

**Interfaces:**
- Consumes: `PermissionRuleSource`（Task 1，type-only import）；`BUILTIN_DENY`（已存在）
- Produces: `function buildDenySourceMap(configDenySources?: Record<string, PermissionRuleSource>): Record<string, PermissionRuleSource>`

- [ ] **Step 1: Write the failing test**

在 `test/deny.test.ts` 末尾追加：

```ts
import { buildDenySourceMap, BUILTIN_DENY } from '../src/deny.js'

describe('buildDenySourceMap', () => {
  it('builtin 标 builtin，config 并入且同名覆盖', () => {
    const m = buildDenySourceMap({ '~/secret/**': 'user', '~/.ssh/**': 'project' })
    expect(m['**/id_rsa']).toBe('builtin')
    expect(m['~/secret/**']).toBe('user')
    expect(m['~/.ssh/**']).toBe('project') // config 覆盖 builtin 同名
  })

  it('无 config 时全为 builtin', () => {
    const m = buildDenySourceMap()
    for (const p of BUILTIN_DENY) expect(m[p]).toBe('builtin')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/deny.test.ts -t buildDenySourceMap`
Expected: FAIL（未导出）

- [ ] **Step 3: Write minimal implementation**

`src/deny.ts` 顶部 import 后加 type import，文件末尾加函数：

```ts
import type { PermissionRuleSource } from './permissions.js'
```

```ts
/** deny pattern → 来源映射：内置规则标 'builtin'，并入 config deny 的 scope（同名 config 覆盖 builtin）。 */
export function buildDenySourceMap(
  configDenySources?: Record<string, PermissionRuleSource>,
): Record<string, PermissionRuleSource> {
  const out: Record<string, PermissionRuleSource> = {}
  for (const pat of BUILTIN_DENY) out[pat] = 'builtin'
  if (configDenySources) for (const [pat, src] of Object.entries(configDenySources)) out[pat] = src
  return out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/deny.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/deny.ts test/deny.test.ts
git commit -m "feat(5.6): buildDenySourceMap（builtin 源标注 + config 合并）"
```

---

### Task 4: 命中规则查找 findMatchingRule / findBashMatchingRule

**Files:**
- Modify: `src/permissions.ts:113-148`（matchRule / bashCommandAllowed 区域，新增 find* 并让 bashCommandAllowed 委托）
- Test: `test/permissions.test.ts`

**Interfaces:**
- Consumes: `matchRule`、`matchExactRule`（本文件内）、`splitBashCommand`
- Produces:
  - `function findMatchingRule(rules: string[], toolName: string, desc: string): string | null`
  - `function findBashMatchingRule(command: string, rules: string[]): string | null`
  - `bashCommandAllowed` 行为不变（委托 findBashMatchingRule）

- [ ] **Step 1: Write the failing test**

在 `test/permissions.test.ts` import 行加入 `findMatchingRule, findBashMatchingRule`，追加：

```ts
import { findMatchingRule, findBashMatchingRule } from '../src/permissions.js'

describe('findMatchingRule / findBashMatchingRule', () => {
  it('返回命中规则字符串', () => {
    expect(findMatchingRule(['Read(/a)', 'Read(/b)'], 'Read', '/b')).toBe('Read(/b)')
    expect(findMatchingRule(['Read(/a)'], 'Read', '/z')).toBeNull()
  })
  it('Bash 单命令前缀命中', () => {
    expect(findBashMatchingRule('npm test -- x', ['Bash(npm test:*)'])).toBe('Bash(npm test:*)')
  })
  it('Bash 复合：精确全量规则命中', () => {
    expect(findBashMatchingRule('ls && pwd', ['Bash(ls && pwd)'])).toBe('Bash(ls && pwd)')
  })
  it('Bash 复合：每段覆盖返回首段命中规则', () => {
    expect(findBashMatchingRule('ls && pwd', ['Bash(ls)', 'Bash(pwd)'])).toBe('Bash(ls)')
  })
  it('Bash 复合：未全覆盖返回 null', () => {
    expect(findBashMatchingRule('ls && pwd', ['Bash(ls)'])).toBeNull()
  })
  it('bashCommandAllowed 与 findBashMatchingRule 等价', () => {
    expect(bashCommandAllowed('ls && pwd', ['Bash(ls)', 'Bash(pwd)'])).toBe(true)
    expect(bashCommandAllowed('ls && rm', ['Bash(ls)'])).toBe(false)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/permissions.test.ts -t findMatchingRule`
Expected: FAIL（未导出）

- [ ] **Step 3: Write minimal implementation**

`src/permissions.ts`：在 `bashCommandAllowed`（第 139-148 行）之后新增，并把 `bashCommandAllowed` 改为委托：

```ts
/** 返回第一条命中规则字符串，无则 null。 */
export function findMatchingRule(rules: string[], toolName: string, desc: string): string | null {
  for (const r of rules) if (matchRule(r, toolName, desc)) return r
  return null
}

/** Bash 命中规则查找：too-complex→null；单命令→现匹配；复合→精确全量规则 OR 每段覆盖（返回首段命中规则作代表）。 */
export function findBashMatchingRule(command: string, rules: string[]): string | null {
  const { tooComplex, commands } = splitBashCommand(command)
  if (tooComplex) return null
  if (commands.length <= 1) {
    const d = commands[0] ?? command
    return findMatchingRule(rules, 'Bash', d)
  }
  for (const r of rules) if (matchExactRule(r, 'Bash', command)) return r
  if (commands.every(s => rules.some(r => matchRule(r, 'Bash', s)))) {
    return findMatchingRule(rules, 'Bash', commands[0])
  }
  return null
}
```

把现有 `bashCommandAllowed`（第 139-148 行）整体替换为委托（行为不变）：

```ts
/** Bash 命令是否被规则集允许（委托 findBashMatchingRule，保持原行为）。 */
export function bashCommandAllowed(command: string, rules: string[]): boolean {
  return findBashMatchingRule(command, rules) !== null
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/permissions.test.ts`
Expected: PASS（含既有 bashCommandAllowed 测试，验证等价）

- [ ] **Step 5: Commit**

```bash
git add src/permissions.ts test/permissions.test.ts
git commit -m "feat(5.6): findMatchingRule/findBashMatchingRule + bashCommandAllowed 委托"
```

---

### Task 5: checkPermission 携带/透传 decisionReason

**Files:**
- Modify: `src/permissions.ts:73-87`（PermissionContext + ask 签名）、`150-205`（checkPermission）
- Test: `test/permissions.test.ts`（含更新第 51 行 toEqual 断言）

**Interfaces:**
- Consumes: `PermissionDecisionReason`、`permissionSourceName`、`findMatchingRule`、`findBashMatchingRule`、`isDeniedPath`
- Produces:
  - `PermissionContext` 增 `ruleSources?: Record<string, PermissionRuleSource>`、`denySources?: Record<string, PermissionRuleSource>`
  - `ask` 签名：`(toolName: string, desc: string, reason?: PermissionDecisionReason) => Promise<Decision>`
  - `checkPermission` 返回 `Promise<{ ok: true; decisionReason?: PermissionDecisionReason } | { ok: false; reason: string; decisionReason?: PermissionDecisionReason }>`

- [ ] **Step 1: Write the failing test**

更新 `test/permissions.test.ts` 第 51 行（用户拒绝断言）从精确 `toEqual` 改为：

```ts
    expect(r).toMatchObject({ ok: false, reason: '用户拒绝了此操作' })
```

追加新 describe：

```ts
describe('checkPermission decisionReason', () => {
  const denyTool = (name: string): any => ({
    name, isReadOnly: false, needsPermission: () => 'x',
    deniablePaths: () => ['/home/u/.ssh/id_rsa'],
  })
  it('非 Bash deny：reason 文本含来源 + decisionReason rule', async () => {
    const r = await checkPermission(denyTool('Read'), {}, pc({
      deny: ['~/.ssh/**'], denySources: { '~/.ssh/**': 'user' },
    }))
    expect(r.ok).toBe(false)
    if (!r.ok) {
      expect(r.reason).toContain('来自 用户设置')
      expect(r.decisionReason).toEqual({ type: 'rule', rule: { source: 'user', behavior: 'deny', value: '~/.ssh/**' } })
    }
  })
  it('Bash deny：降级 ask 并把 deny reason 透传给 ask', async () => {
    let got: any = null
    await checkPermission(denyTool('Bash'), {}, pc({
      deny: ['~/.ssh/**'], denySources: { '~/.ssh/**': 'builtin' },
      ask: async (_t, _d, reason) => { got = reason; return 'no' },
    }))
    expect(got).toEqual({ type: 'rule', rule: { source: 'builtin', behavior: 'deny', value: '~/.ssh/**' } })
  })
  it('allow by rule：ok 带 decisionReason allow', async () => {
    const tool = fakeTool('Read', false, '/tmp/x')
    const r = await checkPermission(tool, {}, pc({
      rules: ['Read(/tmp/x)'], ruleSources: { 'Read(/tmp/x)': 'local' },
    }))
    expect(r).toMatchObject({ ok: true, decisionReason: { type: 'rule', rule: { source: 'local', behavior: 'allow', value: 'Read(/tmp/x)' } } })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/permissions.test.ts -t "checkPermission decisionReason"`
Expected: FAIL（ask 三参未支持 / 返回无 decisionReason）

- [ ] **Step 3: Write minimal implementation**

`src/permissions.ts`：

1. `PermissionContext`（第 73-80 行）改：

```ts
export interface PermissionContext {
  mode: PermissionMode
  rules: string[]
  saveRule: (rule: string) => void
  ask: (toolName: string, desc: string, reason?: PermissionDecisionReason) => Promise<Decision>
  deny?: string[]
  cwd?: string
  ruleSources?: Record<string, PermissionRuleSource>
  denySources?: Record<string, PermissionRuleSource>
}
```

2. `checkPermission`（第 150-205 行）整体替换为：

```ts
export async function checkPermission(
  tool: Tool<any>,
  input: unknown,
  pc: PermissionContext,
  hooks?: PermissionHooks,
): Promise<
  | { ok: true; decisionReason?: PermissionDecisionReason }
  | { ok: false; reason: string; decisionReason?: PermissionDecisionReason }
> {
  // deny 最高优先级：早于 isReadOnly/yolo/acceptEdits/rules
  let forceAsk = false
  let denyHit: string | null = null
  if (pc.deny?.length && tool.deniablePaths) {
    for (const p of tool.deniablePaths(input as any, pc.cwd ?? process.cwd())) {
      const hit = isDeniedPath(p, pc.deny)
      if (!hit) continue
      denyHit = hit
      if (tool.name === 'Bash') { forceAsk = true; break } // Bash：降级 ask 防误操作
      const src = pc.denySources?.[hit] ?? 'builtin'
      const reason = `路径被 deny 规则拒绝（${hit}，来自 ${permissionSourceName(src)}）`
      await hooks?.onDenied?.(tool.name, tool.needsPermission(input) || tool.name, reason)
      return { ok: false, reason, decisionReason: { type: 'rule', rule: { source: src, behavior: 'deny', value: hit } } }
    }
  }
  if (tool.isReadOnly && !forceAsk) return { ok: true }
  const desc = tool.needsPermission(input)
  if (desc === false && !forceAsk) return { ok: true }
  if (desc === false) return { ok: true } // forceAsk 仅对 Bash（desc 恒为 string）
  if (pc.mode === 'yolo' && !forceAsk) return { ok: true }
  if (pc.mode === 'acceptEdits' && !forceAsk && (tool.name === 'Edit' || tool.name === 'Write')) return { ok: true }
  const matched = tool.name === 'Bash'
    ? findBashMatchingRule(desc, pc.rules)
    : findMatchingRule(pc.rules, tool.name, desc)
  if (matched && !forceAsk) {
    const src = pc.ruleSources?.[matched] ?? 'user'
    return { ok: true, decisionReason: { type: 'rule', rule: { source: src, behavior: 'allow', value: matched } } }
  }
  // PermissionRequest hook：交互 ask 前。allow→放行；deny/block→拒绝。
  if (hooks?.onRequest) {
    const out = await hooks.onRequest(tool.name, desc)
    if (out.permission === 'allow') return { ok: true }
    if (out.permission === 'deny' || out.block) {
      const reason = out.permissionReason ?? out.blockReason ?? '权限被 hook 拒绝'
      await hooks.onDenied?.(tool.name, desc, reason)
      return { ok: false, reason, decisionReason: { type: 'hook', hookName: 'PermissionRequest', reason } }
    }
    // fall through 到 pc.ask（fail-safe 问用户）。
  }
  const askReason: PermissionDecisionReason | undefined = denyHit
    ? { type: 'rule', rule: { source: pc.denySources?.[denyHit] ?? 'builtin', behavior: 'deny', value: denyHit } }
    : undefined
  const decision = await pc.ask(tool.name, desc, askReason)
  if (decision === 'always') {
    const firstLine = desc.split('\n')[0]
    const compound = tool.name === 'Bash' && splitBashCommand(desc).commands.length > 1
    const pat = tool.name === 'Bash'
      ? (isDangerous(desc) || compound)
        ? desc.replace(/\n/g, ' ')
        : firstLine.split(' ').slice(0, 2).join(' ') + ':*'
      : desc.replace(/\n/g, ' ')
    pc.saveRule(`${tool.name}(${pat})`)
    return { ok: true }
  }
  if (decision === 'yes') return { ok: true }
  await hooks?.onDenied?.(tool.name, desc, '用户拒绝了此操作')
  return { ok: false, reason: '用户拒绝了此操作', decisionReason: { type: 'other', reason: '用户拒绝了此操作' } }
}
```

- [ ] **Step 4: Run test to verify it passes + 全量回归**

Run: `npx vitest run test/permissions.test.ts && npx tsc --noEmit`
Expected: PASS；tsc 干净（loop.ts execCall 读 `.ok`/`.reason` 不受新增可选字段影响）

- [ ] **Step 5: Commit**

```bash
git add src/permissions.ts test/permissions.test.ts
git commit -m "feat(5.6): checkPermission 携带/透传 PermissionDecisionReason + deny 文本带来源"
```

---

### Task 6: useChat 接线（来源映射 + PendingAsk.reason）

**Files:**
- Modify: `src/tui/useChat.ts:24`（import）、`158`（PendingAsk）、`216`（settings 加载）、`529-538`（ask）、`614-625`（PermissionContext）
- Test: `test/useChat.permission.test.ts`（新建）或现有 useChat 测试中验证

**Interfaces:**
- Consumes: `loadLayeredSettings`、`buildDenySourceMap`、`PermissionDecisionReason`
- Produces: `PendingAsk` 增 `reason?: PermissionDecisionReason`；PermissionContext 注入 `ruleSources`/`denySources`

- [ ] **Step 1: Write the failing test**

新建 `test/useChat.permission.test.ts`，最小验证 ask 透传 reason 进 pendingAsk：

```ts
import { describe, it, expect } from 'vitest'
import type { PendingAsk } from '../src/tui/useChat.js'
import type { PermissionDecisionReason } from '../src/permissions.js'

// 烟囱测试：PendingAsk 类型须含可选 reason 字段（编译期 + 运行期形状）
describe('PendingAsk.reason', () => {
  it('可携带 decisionReason', () => {
    const reason: PermissionDecisionReason = { type: 'rule', rule: { source: 'user', behavior: 'deny', value: '~/.ssh/**' } }
    const ask: PendingAsk = { toolName: 'Bash', desc: 'cat ~/.ssh/id_rsa', dangerous: false, reason, resolve: () => {} }
    expect(ask.reason).toEqual(reason)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/useChat.permission.test.ts`
Expected: FAIL（`reason` 不是 PendingAsk 合法字段 → tsc/vitest 报错）

- [ ] **Step 3: Write minimal implementation**

`src/tui/useChat.ts`：

1. import（第 24 行附近）补：

```ts
import { loadLayeredSettings } from '../settingsLayers.js'
import { buildDenySourceMap } from '../deny.js'
import type { PermissionDecisionReason } from '../permissions.js'
```

（注：第 945 行已有的动态 `import('../settingsLayers.js')` 可保留不动。）

2. PendingAsk（第 158 行）：

```ts
export interface PendingAsk { toolName: string; desc: string; dangerous: boolean; reason?: PermissionDecisionReason; resolve: (d: Decision) => void }
```

3. settings 加载（第 216 行）改为取 layered，并派生来源映射：

```ts
  const layered = loadLayeredSettings(opts.cwd, opts.flagSettingsPath)
  const settings = layered.settings
  const ruleSources = layered.permissionSources.allow
  const denySources = buildDenySourceMap(layered.permissionSources.deny)
```

4. ask（第 529-538 行）签名 + 存 reason：

```ts
  const ask = (toolName: string, desc: string, reason?: PermissionDecisionReason): Promise<Decision> =>
    new Promise<Decision>(res => {
      pendingAsk = { toolName, desc, dangerous: isDangerous(desc), reason, resolve: res }
      setState()
    })
```

（保留原 Promise 体其余逻辑——仅签名加 `reason` 形参、`pendingAsk` 加 `reason` 字段。）

5. PermissionContext（第 614-625 行）：`saveRule` 内补来源记录，对象末尾加两映射：

```ts
          saveRule: r => {
            addUserAllowRule(r)
            if (!settings.permissions.allow.includes(r)) settings.permissions.allow.push(r)
            ruleSources[r] = 'user'
            fireConfigChange()
          },
          ask,
          ruleSources,
          denySources,
```

- [ ] **Step 4: Run test to verify it passes + tsc**

Run: `npx vitest run test/useChat.permission.test.ts && npx tsc --noEmit`
Expected: PASS；tsc 干净

- [ ] **Step 5: Commit**

```bash
git add src/tui/useChat.ts test/useChat.permission.test.ts
git commit -m "feat(5.6): useChat 注入来源映射 + PendingAsk.reason 透传"
```

---

### Task 7: headless 接线（来源映射）

**Files:**
- Modify: `src/headless.ts:16`（import）、`39`（settings 加载）、`113-120`（PermissionContext）
- Test: `test/headless.test.ts`（追加最小断言：硬拒绝 reason 含来源）

**Interfaces:**
- Consumes: `loadLayeredSettings`、`buildDenySourceMap`
- Produces: headless PermissionContext 注入 `ruleSources`/`denySources`

- [ ] **Step 1: Write the failing test**

在 `test/headless.test.ts` 末尾追加（直接验 checkPermission 行为，避免起整套 headless）：

```ts
import { checkPermission } from '../src/permissions.js'
import { buildDenySourceMap } from '../src/deny.js'

describe('headless deny 文本含来源', () => {
  it('内置私钥路径硬拒绝文本带 来自 内置规则', async () => {
    const tool: any = { name: 'Read', isReadOnly: false, needsPermission: () => 'x', deniablePaths: () => ['/h/.ssh/id_rsa'] }
    const r = await checkPermission(tool, {}, {
      mode: 'default', rules: [], saveRule: () => {}, ask: async () => 'no',
      deny: ['~/.ssh/**'], denySources: buildDenySourceMap(),
    })
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.reason).toContain('来自 内置规则')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/headless.test.ts -t "headless deny 文本含来源"`
Expected: 该用例本身已可过（依赖 Task 3/5），但 headless 实接线缺失需 tsc 验证；若 Task 5 已合则此用例 PASS、进入实接线。

- [ ] **Step 3: Write minimal implementation**

`src/headless.ts`：

1. import（第 16 行）：

```ts
import { loadLayeredSettings } from './settingsLayers.js'
import { resolveDenyList, buildDenySourceMap } from './deny.js'
```

（把现有 `import { resolveDenyList } from './deny.js'`(第 24 行) 合并到上面这一行，删去重复 import。）

2. settings 加载（第 39 行）：

```ts
  const layered = loadLayeredSettings(process.cwd(), opts.flagSettingsPath)
  const settings = layered.settings
  const denySources = buildDenySourceMap(layered.permissionSources.deny)
```

3. PermissionContext（第 113-120 行）末尾加：

```ts
      saveRule: () => { /* headless 不持久化规则 */ },
      ask: async () => 'no',
      ruleSources: layered.permissionSources.allow,
      denySources,
```

- [ ] **Step 4: Run test to verify it passes + tsc**

Run: `npx vitest run test/headless.test.ts && npx tsc --noEmit`
Expected: PASS；tsc 干净

- [ ] **Step 5: Commit**

```bash
git add src/headless.ts test/headless.test.ts
git commit -m "feat(5.6): headless 注入来源映射，硬拒绝文本带来源"
```

---

### Task 8: PermissionDialog 渲染来源行

**Files:**
- Modify: `src/tui/components/PermissionDialog.tsx:7-8`（import）、`42-44`（高危警告下方）
- Test: `test/tui.permission.test.tsx`

**Interfaces:**
- Consumes: `PendingAsk.reason`、`permissionSourceName`、`PermissionDecisionReason`

- [ ] **Step 1: Write the failing test**

在 `test/tui.permission.test.tsx` 追加（用 ink-testing-library `render`，与现有用例同风格）：

```ts
import { render } from 'ink-testing-library'
import React from 'react'
import { PermissionDialog } from '../src/tui/components/PermissionDialog.js'

describe('PermissionDialog 来源行', () => {
  it('deny rule 显示规则与来源', () => {
    const ask: any = { toolName: 'Bash', desc: 'cat ~/.ssh/id_rsa', dangerous: false,
      reason: { type: 'rule', rule: { source: 'builtin', behavior: 'deny', value: '~/.ssh/**' } }, resolve: () => {} }
    const { lastFrame } = render(<PermissionDialog ask={ask} onDecide={() => {}} />)
    expect(lastFrame()).toContain('命中 deny 规则 ~/.ssh/**')
    expect(lastFrame()).toContain('来自 内置规则')
  })
  it('无 reason 不渲染来源行', () => {
    const ask: any = { toolName: 'Bash', desc: 'npm test', dangerous: false, resolve: () => {} }
    const { lastFrame } = render(<PermissionDialog ask={ask} onDecide={() => {}} />)
    expect(lastFrame()).not.toContain('命中 deny 规则')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/tui.permission.test.tsx -t "来源行"`
Expected: FAIL（未渲染来源行）

- [ ] **Step 3: Write minimal implementation**

`src/tui/components/PermissionDialog.tsx`：

1. import（第 7-8 行附近）：

```tsx
import type { PendingAsk } from '../useChat.js'
import { type Decision, permissionSourceName } from '../../permissions.js'
```

2. 在高危警告块（第 42-44 行）之后插入：

```tsx
      {ask.reason?.type === 'rule' && ask.reason.rule.behavior === 'deny' && (
        <Text color={T.err}>⚠ 命中 deny 规则 {ask.reason.rule.value}（来自 {permissionSourceName(ask.reason.rule.source)}）</Text>
      )}
      {ask.reason?.type === 'hook' && (
        <Text dimColor>权限被 hook {ask.reason.hookName} 拒绝</Text>
      )}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/tui.permission.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/components/PermissionDialog.tsx test/tui.permission.test.tsx
git commit -m "feat(5.6): PermissionDialog 渲染 deny 规则来源行"
```

---

### Task 9: 全量门禁 + 真机冒烟

**Files:** 无（验证）

- [ ] **Step 1: 全量回归**

Run: `npm test && npx tsc --noEmit && npm run build`
Expected: 全绿。

- [ ] **Step 2: TUI 真机冒烟（按 deepcode-tui-smoke-workflow）**

冒烟场景（用户复制粘贴命令 + 截图回传）：
1. 在 `~/.deepcode/settings.json` 临时加 `"permissions": { "deny": ["~/secret/**"] }`。
2. `npm start`，让模型 `Bash(cat ~/secret/x)` → 弹窗应显示「⚠ 命中 deny 规则 ~/secret/**（来自 用户设置）」。
3. 让模型 `Read` 一个内置私钥路径（如 `~/.ssh/id_rsa`）→ 工具错误文本应含「来自 内置规则」。
4. 普通 ask（`Bash(npm test)` 未授权）→ 弹窗无来源行，维持现状。

- [ ] **Step 3: 收尾**

按 `superpowers:finishing-a-development-branch` 合 main；更新 master roadmap 5.6 状态 + 记忆。

---

## Self-Review

**1. Spec coverage:**
- §3 类型 → Task 1 ✓
- §4.1 settingsLayers per-rule provenance → Task 2 ✓
- §4.2 buildDenySourceMap → Task 3 ✓
- §4.3 findMatchingRule/checkPermission → Task 4 + 5 ✓
- §4.4 permissionSourceName → Task 1 ✓
- §4.5 useChat 接线 → Task 6 ✓
- §4.6 headless 接线 → Task 7 ✓
- §4.7 PermissionDialog 渲染 → Task 8 ✓
- §6 测试 + 真机冒烟 → 各任务 + Task 9 ✓

**2. Placeholder scan:** 无 TBD/TODO；每个代码步骤含完整代码。

**3. Type consistency:** `PermissionRuleSource`/`PermissionRule`/`PermissionDecisionReason`/`permissionSourceName`/`findMatchingRule`/`findBashMatchingRule`/`buildDenySourceMap`/`permissionSources` 跨任务命名一致；`ask` 三参签名在 Task 5/6/7/8 一致；`PendingAsk.reason` 在 Task 6/8 一致。

**注意点（实现者必读）：** Task 5 须把 `test/permissions.test.ts` 第 51 行的精确 `toEqual` 改为 `toMatchObject`（新增 decisionReason 字段会破坏精确相等）。
