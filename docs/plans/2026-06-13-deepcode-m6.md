# deepcode M6 实现计划：可发布 v0.6 + WebFetch

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 deepcode 变成能 `npm i -g ds-code` 装来用的工具（命令 `deepcode`），并加 CC 式 WebFetch 工具。

**Architecture:** tsc 编译 src→dist 出可发布 ESM 包；首跑无 key 时 TUI 向导写 `~/.deepcode/settings.json`；删 `--plain`/repl.ts 收敛为单一交互路径；WebFetch 复用 `runLoop` 做「抓取+子模型按问题作答」。核心 loop/api/tools/session 逻辑零改动。

**Tech Stack:** TypeScript(ESM/NodeNext)、ink 5、React 18、undici、zod、vitest、tsx(dev)。

**设计依据:** `docs/specs/2026-06-13-deepcode-m6-design.md`

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `tsconfig.build.json` | 创建 | 编译配置：emit 到 dist，只含 src |
| `package.json` | 改 | name→ds-code、去 private、bin/files/engines、build/prepublishOnly 脚本 |
| `src/version.ts` | 创建 | 运行时读 package.json 导出 VERSION |
| `src/tui/components/Banner.tsx` | 改 | 标题显示 `deepcode v{VERSION}` |
| `src/config.ts` | 改 | Settings 加 apiKey + hasApiKey/saveApiKey |
| `src/api.ts` | 改 | createClient 读 key：env ?? settings.apiKey |
| `src/tui/setup.tsx` | 创建 | 首跑向导组件 + runSetup() |
| `src/index.ts` | 改 | 加 shebang、删 --plain 分支、TTY 分支向导+延后 createClient |
| `src/repl.ts` | 删 | 旧 readline REPL（--plain），TUI 已接管 |
| `src/tui/useChat.ts` | 改 | 仅改一处指向 repl.ts 的注释 + 加 WebFetch 进工具列表 |
| `src/headless.ts` | 改 | 加 WebFetch 进工具列表 |
| `src/tools/webfetch.ts` | 创建 | CC 式 WebFetch 工厂工具 |
| `README.md` | 改 | 公开向：安装/配 key/用法/模型/代理/roadmap |
| `test/version.test.ts` | 创建 | VERSION 导出 |
| `test/config.test.ts` | 改 | apiKey load/save/hasApiKey |
| `test/tui.setup.test.tsx` | 创建 | 向导渲染 + 输入保存 |
| `test/tools.webfetch.test.ts` | 创建 | 抓取剥离/截断/作答/错误/权限 |

---

## Task 1: 构建管线 + 版本号源头

把项目变成能 `npm run build` 出 dist、版本号从 package.json 来。

**Files:**
- Create: `tsconfig.build.json`
- Create: `src/version.ts`
- Create: `test/version.test.ts`
- Modify: `package.json`
- Modify: `src/index.ts:1`（shebang）
- Modify: `src/tui/components/Banner.tsx`

- [ ] **Step 1: 写 version 失败测试**

`test/version.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import { VERSION } from '../src/version.js'

describe('version', () => {
  it('导出与 package.json 一致的非空版本号', () => {
    const pkg = JSON.parse(readFileSync(new URL('../package.json', import.meta.url), 'utf8'))
    expect(VERSION).toBe(pkg.version)
    expect(VERSION).toMatch(/^\d+\.\d+\.\d+/)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/version.test.ts`
Expected: FAIL（`Cannot find module '../src/version.js'`）

- [ ] **Step 3: 写 version.ts**

`src/version.ts`:
```ts
// src/version.ts
// 运行时读 package.json 的 version。dev(src/version.ts) 与 dist(dist/version.js) 下
// '../package.json' 都指向包根 package.json，无需 import assertion。
import { readFileSync } from 'node:fs'

const pkg = JSON.parse(readFileSync(new URL('../package.json', import.meta.url), 'utf8'))
export const VERSION: string = pkg.version
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/version.test.ts`
Expected: PASS

- [ ] **Step 5: Banner 显示版本号**

`src/tui/components/Banner.tsx`：顶部 import 区加 `import { VERSION } from '../version.js'`，把左列首行从
```tsx
    { text: '🐳 deepcode', color: T.accent, bold: true },
```
改为
```tsx
    { text: `🐳 deepcode v${VERSION}`, color: T.accent, bold: true },
```

- [ ] **Step 6: 创建 tsconfig.build.json**

`tsconfig.build.json`:
```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"],
  "exclude": ["test", "scripts"]
}
```

- [ ] **Step 7: index.ts 首行加 shebang**

`src/index.ts` 第 1 行（在 `// src/index.ts` 注释之前）插入：
```ts
#!/usr/bin/env node
```

- [ ] **Step 8: 改 package.json 发布字段**

把 `package.json` 改为（保留 version 0.1.0，发布时再 bump；保留 dependencies/devDependencies 不变）：
```json
{
  "name": "ds-code",
  "version": "0.1.0",
  "type": "module",
  "bin": { "deepcode": "dist/index.js" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  ...
}
```
（删掉 `"private": true`；`...` 处 dependencies/devDependencies 原样保留。）

- [ ] **Step 9: 验证 build 出 dist 且可加载**

Run: `npm run build && head -1 dist/index.js && node -e "import('./dist/version.js').then(m=>console.log('VERSION',m.VERSION))"`
Expected: 第一行打印 `#!/usr/bin/env node`，最后打印 `VERSION 0.1.0`

- [ ] **Step 10: 全量回归 + typecheck**

Run: `npm test && npm run typecheck`
Expected: 测试全过（211：原 210 + version 1）、typecheck 无输出

- [ ] **Step 11: 提交**

```bash
git add tsconfig.build.json src/version.ts test/version.test.ts package.json src/index.ts src/tui/components/Banner.tsx
git commit -m "feat(M6): tsc→dist 构建管线 + 版本号源头（banner 显 v{version}）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 配置 apiKey + key 解析顺序

apiKey 并入现有 settings.json；createClient 改为 env 优先、settings 兜底。

**Files:**
- Modify: `src/config.ts`
- Modify: `src/api.ts`（createClient，约 89 行）
- Modify: `test/config.test.ts`

- [ ] **Step 1: 写 config apiKey 失败测试**

在 `test/config.test.ts` 末尾追加（文件已 mock node:os.homedir 到临时目录）：
```ts
import { loadSettings, saveApiKey, hasApiKey } from '../src/config.js'

describe('apiKey 持久化', () => {
  it('saveApiKey 写入并能读回，loadSettings 含 apiKey', () => {
    saveApiKey('sk-test-123')
    expect(loadSettings().apiKey).toBe('sk-test-123')
  })

  it('hasApiKey：env 优先，否则看 settings', () => {
    delete process.env.DEEPSEEK_API_KEY
    saveApiKey('sk-from-settings')
    expect(hasApiKey()).toBe(true)
    // 清掉 settings 里的 key 再验 env
    saveApiKey('')
    expect(hasApiKey()).toBe(false)
    process.env.DEEPSEEK_API_KEY = 'sk-from-env'
    expect(hasApiKey()).toBe(true)
    delete process.env.DEEPSEEK_API_KEY
  })
})
```
（若 `test/config.test.ts` 顶部已有 import 行，把上面三个新符号并入现有 `from '../src/config.js'` 的 import，不重复 import。）

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/config.test.ts`
Expected: FAIL（`saveApiKey`/`hasApiKey` 未导出）

- [ ] **Step 3: 扩展 config.ts**

`src/config.ts`：`Settings` 接口加字段：
```ts
  /** DeepSeek API key（首跑向导写入；env DEEPSEEK_API_KEY 优先级更高） */
  apiKey?: string
```
`loadSettings()` 的 return 对象加：
```ts
    apiKey: raw?.apiKey,
```
文件末尾加两个导出：
```ts
export function hasApiKey(): boolean {
  return !!(process.env.DEEPSEEK_API_KEY ?? loadSettings().apiKey)
}

export function saveApiKey(key: string): void {
  const s = loadSettings()
  s.apiKey = key || undefined
  saveSettings(s)
  try { fs.chmodSync(FILE, 0o600) } catch { /* 尽力而为 */ }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/config.test.ts`
Expected: PASS

- [ ] **Step 5: createClient 改 key 解析**

`src/api.ts` 的 `createClient` 中：
```ts
  const apiKey = process.env.DEEPSEEK_API_KEY
  if (!apiKey) throw new Error('缺少 DEEPSEEK_API_KEY 环境变量。请先 export DEEPSEEK_API_KEY=sk-...')
```
改为：
```ts
  const apiKey = process.env.DEEPSEEK_API_KEY ?? loadSettings().apiKey
  if (!apiKey) throw new Error('缺少 DeepSeek API key。运行 deepcode 进入首跑向导配置，或 export DEEPSEEK_API_KEY=sk-...')
```
（`loadSettings` 在 api.ts 顶部已 import，无需新增。）

- [ ] **Step 6: 全量回归 + typecheck**

Run: `npm test && npm run typecheck`
Expected: 全过、无输出

- [ ] **Step 7: 提交**

```bash
git add src/config.ts src/api.ts test/config.test.ts
git commit -m "feat(M6): apiKey 并入 settings.json，createClient env 优先 settings 兜底

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 首跑向导 + 入口简化（删 --plain/repl）

无 key 时 TTY 走向导；删旧 readline REPL，入口收敛单一路径。

**Files:**
- Create: `src/tui/setup.tsx`
- Create: `test/tui.setup.test.tsx`
- Modify: `src/index.ts`
- Delete: `src/repl.ts`
- Modify: `src/tui/useChat.ts`（仅一处注释）

- [ ] **Step 1: 写 Setup 失败测试**

`test/tui.setup.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'

const saveApiKey = vi.fn()
vi.mock('../src/config.js', () => ({ saveApiKey }))

import { Setup } from '../src/tui/setup.js'

const delay = (ms = 20) => new Promise(r => setTimeout(r, ms))

describe('Setup 首跑向导', () => {
  it('渲染欢迎与 key 提示', () => {
    const f = render(<Setup onDone={() => {}} />).lastFrame()!
    expect(f).toContain('deepcode')
    expect(f).toContain('API key')
  })

  it('输入 key 回车 → 调 saveApiKey 并 onDone', async () => {
    saveApiKey.mockClear()
    const onDone = vi.fn()
    const { stdin } = render(<Setup onDone={onDone} />)
    await delay()
    stdin.write('sk-abc123')
    await delay()
    stdin.write('\r')
    await delay()
    expect(saveApiKey).toHaveBeenCalledWith('sk-abc123')
    expect(onDone).toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/tui.setup.test.tsx`
Expected: FAIL（`../src/tui/setup.js` 不存在）

- [ ] **Step 3: 写 setup.tsx**

`src/tui/setup.tsx`:
```tsx
// src/tui/setup.tsx
// 首跑向导：无 API key 时（仅 TTY）在 startTui 之前独立 render，收集 key 写 settings.json。
// 独立于 App，因为 createClient 需要 key 才能构造——向导跑在 client 创建之前。
import React, { useState, useRef } from 'react'
import { render, Box, Text, useInput, useApp } from 'ink'
import { saveApiKey } from '../config.js'
import { T } from './theme.js'

export function Setup(props: { onDone: () => void }) {
  const [val, setVal] = useState('')
  const ref = useRef('')
  const { exit } = useApp()
  const set = (v: string) => { ref.current = v; setVal(v) }
  useInput((input, key) => {
    if (key.return) {
      const k = ref.current.trim()
      if (!k) return
      saveApiKey(k)
      props.onDone()
      exit()
      return
    }
    if (key.backspace || key.delete) { set(ref.current.slice(0, -1)); return }
    if (key.ctrl || key.meta || key.tab) return
    if (input) set(ref.current + input)
  })
  return (
    <Box flexDirection="column" paddingX={1}>
      <Text color={T.accent} bold>🐳 欢迎使用 deepcode</Text>
      <Text> </Text>
      <Text>首次使用，请粘贴你的 DeepSeek API key（写入 ~/.deepcode/settings.json）：</Text>
      <Box borderStyle="round" borderColor={T.accent} borderLeft={false} borderRight={false} paddingX={1}>
        <Text color={T.accent}>{'❯ '}</Text>
        <Text>{'•'.repeat(val.length)}<Text inverse> </Text></Text>
      </Box>
      <Text dimColor>回车保存 · key 形如 sk-... · Ctrl+C 取消</Text>
    </Box>
  )
}

export async function runSetup(): Promise<void> {
  const { waitUntilExit } = render(<Setup onDone={() => {}} />, { exitOnCtrlC: true })
  await waitUntilExit()
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/tui.setup.test.tsx`
Expected: PASS

- [ ] **Step 5: 改 index.ts（删 --plain、TTY 分支向导、延后 createClient）**

把 `src/index.ts` 的整个 try 块改为（shebang 与注释保留在文件顶部）：
```ts
const argv = process.argv
const yolo = argv.includes('--yolo')
const continueSession = argv.includes('--continue') || argv.includes('-c')
const pIdx = argv.indexOf('-p')

try {
  if (pIdx !== -1) {
    const prompt = argv[pIdx + 1]
    if (!prompt || prompt.startsWith('-')) throw new Error('用法：deepcode -p "<任务>" [--json] [--yolo]')
    const client = createClient()
    const { runHeadless } = await import('./headless.js')
    const r = await runHeadless({ client, prompt, yolo })
    if (argv.includes('--json')) {
      console.log(JSON.stringify({ text: r.text, status: r.status, turns: r.turns, usage: r.usage, costUSD: r.costUSD }))
    } else {
      console.log(r.text)
    }
    process.exitCode = r.status === 'done' ? 0 : 1
  } else if (!process.stdin.isTTY) {
    // 管道喂入无 -p：读 stdin 全文当 prompt 走 headless（CC 同款行为）
    const chunks: Buffer[] = []
    for await (const c of process.stdin) chunks.push(c)
    const prompt = Buffer.concat(chunks).toString('utf8').trim()
    if (!prompt) throw new Error('stdin 为空。交互模式请直接运行 deepcode，或用 -p "<任务>"')
    const client = createClient()
    const { runHeadless } = await import('./headless.js')
    const r = await runHeadless({ client, prompt, yolo })
    console.log(r.text)
    process.exitCode = r.status === 'done' ? 0 : 1
  } else {
    // TTY 交互：无 key 先走首跑向导，再创建 client
    if (!hasApiKey()) {
      const { runSetup } = await import('./tui/setup.js')
      await runSetup()
    }
    const client = createClient()
    const { startTui } = await import('./tui/index.js')
    await startTui({ client, yolo, continueSession })
    process.exit(0) // ink 卸载后 stdin raw 监听可能残留；显式退出兜底
  }
} catch (e: any) {
  console.error(e?.message ?? e)
  process.exitCode = 1
}
```
顶部 import 改为：
```ts
import { createClient } from './api.js'
import { hasApiKey } from './config.js'
```
（删除原 `const plain = ...`、原顶部 `const client = createClient()`、整个 `else if (plain)` 分支。）

- [ ] **Step 6: 删 repl.ts + 修注释**

Run: `git rm src/repl.ts`

`src/tui/useChat.ts:227` 附近注释 `（对齐 repl.ts 48-72）` 改为 `（恢复会话逻辑）`：
```ts
  /** 恢复会话到内存：消息、模型设置、fileState（mtime 校验）、usage，并续写该文件。返回恢复的 user 轮数。 */
```

- [ ] **Step 7: 全量回归 + typecheck + build**

Run: `npm test && npm run typecheck && npm run build`
Expected: 测试全过（删 repl 无测试引用）、typecheck 无输出、build 成功

- [ ] **Step 8: 提交**

```bash
git add src/tui/setup.tsx test/tui.setup.test.tsx src/index.ts src/tui/useChat.ts
git rm src/repl.ts
git commit -m "feat(M6): 首跑向导（无 key→TUI 配 key 写 settings）+ 删 --plain/repl 单一路径

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: WebFetch 工具（CC 式）

抓取 URL + 子模型按问题作答，复用 runLoop 模式。

**Files:**
- Create: `src/tools/webfetch.ts`
- Create: `test/tools.webfetch.test.ts`
- Modify: `src/headless.ts`、`src/tui/useChat.ts`（工具列表加 WebFetch）

- [ ] **Step 1: 写 WebFetch 失败测试**

`test/tools.webfetch.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// mock undici fetch
const fetchMock = vi.fn()
vi.mock('undici', () => ({
  fetch: (...a: any[]) => fetchMock(...a),
  ProxyAgent: class { constructor(_: string) {} },
}))

// mock runLoop：把网页内容透出，推一条 assistant 回复
let lastMessages: any[] = []
vi.mock('../src/loop.js', () => ({
  runLoop: async function* (messages: any[]) {
    lastMessages = messages
    messages.push({ role: 'assistant', content: 'MOCK 答案' })
    yield { type: 'turn_end', usage: { prompt_tokens: 10, completion_tokens: 5, prompt_cache_hit_tokens: 0 } }
  },
}))

import { makeWebFetchTool } from '../src/tools/webfetch.js'

const ctx: any = { cwd: () => '/tmp', setCwd: () => {}, signal: new AbortController().signal, fileState: new Map() }
const onUsage = vi.fn()
const tool = makeWebFetchTool({ client: {} as any, onUsage })

function resp(body: string, ct = 'text/html', ok = true, status = 200) {
  return { ok, status, statusText: ok ? 'OK' : 'ERR', headers: { get: () => ct }, text: async () => body }
}

describe('WebFetch', () => {
  beforeEach(() => { fetchMock.mockReset(); onUsage.mockClear(); lastMessages = [] })

  it('权限：返回 host 描述', () => {
    expect(tool.needsPermission({ url: 'https://example.com/x', prompt: 'q' })).toContain('example.com')
    expect(tool.isReadOnly).toBe(false)
  })

  it('抓取 HTML→剥标签→子模型作答，content 进 user 消息', async () => {
    fetchMock.mockResolvedValue(resp('<html><body><script>x</script><h1>标题</h1>正文</body></html>'))
    const out = await tool.call({ url: 'https://example.com', prompt: '讲了啥' }, ctx)
    expect(out).toBe('MOCK 答案')
    const user = lastMessages.find(m => m.role === 'user')!
    expect(user.content).toContain('标题')
    expect(user.content).toContain('正文')
    expect(user.content).not.toContain('<script>')
    expect(user.content).toContain('讲了啥')
    expect(onUsage).toHaveBeenCalled()
  })

  it('非 http(s) 拒绝', async () => {
    const out = await tool.call({ url: 'ftp://x', prompt: 'q' } as any, ctx)
    expect(out).toContain('仅支持 http')
    expect(fetchMock).not.toHaveBeenCalled()
  })

  it('非 200 返回错误', async () => {
    fetchMock.mockResolvedValue(resp('', 'text/html', false, 404))
    const out = await tool.call({ url: 'https://example.com', prompt: 'q' }, ctx)
    expect(out).toContain('404')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/tools.webfetch.test.ts`
Expected: FAIL（`makeWebFetchTool` 未导出）

- [ ] **Step 3: 写 webfetch.ts**

`src/tools/webfetch.ts`:
```ts
// src/tools/webfetch.ts
// CC 式 WebFetch：抓取 URL 正文，用子模型（flash）按用户 prompt 提取/总结后返回。
// 复用 runLoop（tools:[]、maxTurns:1、thinking:false），与 agent.ts 同款，自带重试/代理/thinking 关闭。
import { z } from 'zod'
import type OpenAI from 'openai'
import { fetch as undiciFetch, ProxyAgent } from 'undici'
import type { Tool, ToolContext } from './types.js'
import type { Usage } from '../api.js'
import { runLoop } from '../loop.js'

const SUB_MODEL = 'deepseek-v4-flash'
const MAX_CHARS = 30_000

const schema = z.object({
  url: z.string().describe('要抓取的 http(s) URL'),
  prompt: z.string().min(1).describe('针对该页要回答/提取什么'),
})

function htmlToText(html: string): string {
  return html
    .replace(/<script[\s\S]*?<\/script>/gi, '')
    .replace(/<style[\s\S]*?<\/style>/gi, '')
    .replace(/<[^>]+>/g, ' ')
    .replace(/&nbsp;/gi, ' ')
    .replace(/\s+/g, ' ')
    .trim()
}

export function makeWebFetchTool(deps: { client: OpenAI; onUsage: (u: Usage, model: string) => void }): Tool<typeof schema> {
  return {
    name: 'WebFetch',
    description: '抓取一个 http(s) URL 的内容，并按 prompt 从中提取或总结信息后返回（用于读取网页/在线文档）。',
    inputSchema: schema,
    isReadOnly: false,
    needsPermission: input => `WebFetch ${(() => { try { return new URL(input.url).host } catch { return input.url } })()}`,
    async call(input, ctx) {
      if (!/^https?:\/\//i.test(input.url)) return '错误：仅支持 http(s) URL'
      let body: string, ctype: string
      try {
        const proxy =
          process.env.https_proxy ?? process.env.HTTPS_PROXY ?? process.env.http_proxy ?? process.env.HTTP_PROXY
        const res = await undiciFetch(input.url, {
          signal: ctx.signal,
          ...(proxy ? { dispatcher: new ProxyAgent(proxy) } : {}),
        } as any)
        if (!res.ok) return `错误：HTTP ${res.status} ${res.statusText}`
        ctype = res.headers.get('content-type') ?? ''
        body = await res.text()
      } catch (e: any) {
        return `错误：抓取失败 ${e?.message ?? e}`
      }
      const text = /html/i.test(ctype) ? htmlToText(body) : body
      const clipped = text.length > MAX_CHARS ? text.slice(0, MAX_CHARS) + '\n…（内容已截断）' : text

      const messages: any[] = [
        { role: 'system', content: '你从网页内容中按用户问题提取或总结信息。只依据给定内容作答，不编造；内容里找不到就明说没有。' },
        { role: 'user', content: `URL：${input.url}\n问题：${input.prompt}\n\n网页内容：\n${clipped}` },
      ]
      const subCtx: ToolContext = {
        cwd: ctx.cwd,
        setCwd: () => {},
        get signal() { return ctx.signal },
        fileState: new Map(),
      }
      const gen = runLoop(messages, {
        client: deps.client,
        tools: [],
        model: SUB_MODEL,
        thinking: false,
        permission: { mode: 'default', rules: [], saveRule: () => {}, ask: async () => 'no' },
        ctx: subCtx,
        maxTurns: 1,
      })
      let step
      while (!(step = await gen.next()).done) {
        if (step.value.type === 'turn_end') deps.onUsage(step.value.usage, SUB_MODEL)
      }
      const final = [...messages].reverse().find(m => m.role === 'assistant' && typeof m.content === 'string' && m.content)
      return final?.content ?? '（无返回）'
    },
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/tools.webfetch.test.ts`
Expected: PASS

- [ ] **Step 5: 装配进工具列表**

`src/headless.ts:6` 附近加 import：
```ts
import { makeWebFetchTool } from './tools/webfetch.js'
```
`src/headless.ts:49` 的工具列表：
```ts
    tools: [...allTools, todoWriteTool, makeAgentTool({ client: opts.client, onUsage: (u, _model) => addUsage(u) })],
```
改为（在 makeAgentTool 后追加）：
```ts
    tools: [
      ...allTools,
      todoWriteTool,
      makeAgentTool({ client: opts.client, onUsage: (u, _model) => addUsage(u) }),
      makeWebFetchTool({ client: opts.client, onUsage: (u, _model) => addUsage(u) }),
    ],
```

`src/tui/useChat.ts:15` 附近加 import：
```ts
import { makeWebFetchTool } from '../tools/webfetch.js'
```
`src/tui/useChat.ts:266-272` 的工具列表里，在 makeAgentTool({...}) 之后追加一项：
```ts
    makeWebFetchTool({
      client,
      onUsage: (u, m) => { usageLog.push({ usage: u, model: m }); session.appendUsage(u, m) },
    }),
```
（与同处 makeAgentTool 的 client/onUsage 写法保持一致——照抄该处 client 变量名与 onUsage 闭包。）

- [ ] **Step 6: 全量回归 + typecheck**

Run: `npm test && npm run typecheck`
Expected: 全过、无输出

- [ ] **Step 7: 提交**

```bash
git add src/tools/webfetch.ts test/tools.webfetch.test.ts src/headless.ts src/tui/useChat.ts
git commit -m "feat(M6): WebFetch 工具（CC 式：抓取+flash 按问题作答，权限闸门+计费）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: README + roadmap

公开向 README，可发布的门面。

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 读现有 README 摸结构**

Run: `cat README.md`
（在其基础上改，保留仓库既有风格与已准确的内容；只更新安装/key/用法/roadmap 段。）

- [ ] **Step 2: 重写 README 核心段**

`README.md` 必须包含以下小节（用准确命令，勿宣传未实现功能）：
```markdown
# deepcode

给 DeepSeek API 写的 CC 风格终端编码助手（微缩版 Claude Code）。

## 安装

    npm i -g ds-code

安装后命令为 `deepcode`。需要 Node ≥ 18。

## 配置 API key

三选一（优先级 env > settings）：
- 首次运行 `deepcode`，按向导粘贴 key（写入 `~/.deepcode/settings.json`，权限 600）
- 或 `export DEEPSEEK_API_KEY=sk-...`
- 或手写 `~/.deepcode/settings.json` 的 `apiKey` 字段

> 若你的网络需代理访问 DeepSeek，设置 `https_proxy`（如 `http://127.0.0.1:8118`），deepcode 会自动经它请求。

## 用法

    deepcode                  # 交互式 TUI
    deepcode -p "<任务>"       # 一次性 headless 输出
    deepcode -p "<任务>" --json # headless + JSON（text/status/turns/usage/costUSD）
    echo "<任务>" | deepcode   # 管道喂入走 headless

交互中：
- `@文件` 引用文件、`!命令` 直跑 shell
- 斜杠命令：`/model`（flash↔pro）、`/think`、`/accept`、`/cost`、`/context`、`/compact`、`/clear`、`/resume`、`/permissions`、`/init`（生成 DEEPCODE.md）、`/help`、`/exit`
- 工具：Read/Glob/Grep/Bash/Edit/Write/TodoWrite/Agent（只读子代理）/WebFetch

## 模型

默认 `deepseek-v4-flash`，`/model pro` 切 `deepseek-v4`，`/think` 开关 thinking。

## Roadmap

- M1–M5（已发布 tag）：核心 harness、Edit/Write、持久化、子代理/headless/bench、CC UI 1:1 复刻
- **M6（本版）**：npm 可发布、首跑向导、WebFetch
- M7：`/rewind`、可写 subagent + git worktree
```

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs(M6): 公开向 README（安装/配 key/用法/模型/roadmap）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: 集成验收

确认发布产物正确、首跑链路通、全回归绿。

**Files:** 无（验证 + 验收文档）

- [ ] **Step 1: build + pack 产物检查**

Run: `npm run build && npm pack --dry-run`
Expected: 产物清单只含 `dist/**`、`README.md`、`package.json`（无 `src/`、`test/`、`docs/`、`bench/`、`node_modules`）

- [ ] **Step 2: bin 可执行性**

Run: `node dist/index.js -p "回复 OK 两个字" --json`
Expected: 若环境有 key/代理 → 输出含 `"text"` 的 JSON；若无 key → stderr 打印「缺少 DeepSeek API key」并退出码 1（两者都算入口接线正确）

- [ ] **Step 3: 首跑向导 pty 冒烟（无 key）**

写 `/tmp/ds-smoke/m6-setup.py`（用临时 HOME 隔离、清空 env key）：
```python
#!/usr/bin/env python3
# M6 首跑向导冒烟：无 key → 向导 → 写 settings.json(600) → 二次启动直进。
import os, pty, select, time, fcntl, termios, struct, json, tempfile, stat, sys
DIST = '/Users/silas/loop/deepcode/dist/index.js'
HOME = tempfile.mkdtemp(prefix='dc-m6-home-')

def run(send_key):
    master, slave = pty.openpty()
    fcntl.ioctl(slave, termios.TIOCSWINSZ, struct.pack('HHHH', 30, 100, 0, 0))
    pid = os.fork()
    if pid == 0:
        os.setsid(); os.dup2(slave,0); os.dup2(slave,1); os.dup2(slave,2)
        env = dict(os.environ); env.pop('DEEPSEEK_API_KEY', None); env['HOME'] = HOME
        os.chdir(HOME); os.execvpe('node', ['node', DIST], env); os._exit(127)
    os.close(slave)
    buf = bytearray()
    def pump(t):
        end = time.time()+t
        while time.time()<end:
            r,_,_ = select.select([master],[],[],0.2)
            if r:
                try: d = os.read(master,8192)
                except OSError: break
                if not d: break
                buf.extend(d)
    pump(12)
    txt = buf.decode('utf-8','replace')
    if send_key:
        os.write(master, b'sk-pty-test\r'); pump(3)
    try: os.write(master, b'\x03\x03')
    except OSError: pass
    pump(1)
    try: os.close(master)
    except OSError: pass
    try: os.waitpid(pid, 0)
    except ChildProcessError: pass
    return txt

ok = []
t1 = run(send_key=True)
ok.append(('首跑出现向导(API key 提示)', 'API key' in t1))
cfg = os.path.join(HOME, '.deepcode', 'settings.json')
exists = os.path.exists(cfg)
ok.append(('settings.json 写出', exists))
if exists:
    ok.append(('apiKey 写入', json.load(open(cfg)).get('apiKey') == 'sk-pty-test'))
    ok.append(('权限 600', stat.S_IMODE(os.stat(cfg).st_mode) == 0o600))
t2 = run(send_key=False)
ok.append(('二次启动不再出现向导', 'API key' not in t2))
ok.append(('二次启动进 TUI 欢迎框', '🐳 deepcode' in t2))
print('\n=== M6 首跑冒烟 ===')
p = 0
for n, c in ok:
    print(('  ✓ ' if c else '  ✗ ')+n); p += c
print(f'{p}/{len(ok)} 通过')
sys.exit(0 if p == len(ok) else 1)
```

Run: `npm run build && python3 /tmp/ds-smoke/m6-setup.py`
Expected: `6/6 通过`（向导出现 → 写 key 600 → 二次直进 TUI）

- [ ] **Step 4: 全量回归 + typecheck**

Run: `npm test && npm run typecheck`
Expected: 测试全过（约 215：210 + version/setup/webfetch/config 新增）、typecheck 无输出

- [ ] **Step 5: 写验收文档**

`docs/specs/m6-acceptance.md`：记录范围、各模块落地与 commit、测试数、pack 产物清单、首跑冒烟结果、版本 bump 与 tag 计划（v0.6.0-m6）、带入 M7（/rewind + worktree）。

- [ ] **Step 6: 提交验收文档**

```bash
git add docs/specs/m6-acceptance.md
git commit -m "docs(M6): 验收报告

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 7: 发布动作（用户确认后，主控执行）**

发布是面向外部、不可逆的动作，**须用户明确点头后**再做：bump `package.json` 0.1.0→0.6.0 → commit → tag `v0.6.0-m6` → `npm login`（用户）→ `npm publish`（首次公开包用 `--access public`）→ 验证 `npm i -g ds-code` 装得上、`deepcode` 跑得起。

---

## 自查

- **Spec 覆盖**：构建管线(§1→T1)、版本号(§5→T1)、config/key(§2→T2)、首跑向导(§3→T3)、删 --plain(§4→T3)、WebFetch(§6→T4)、README(§7→T5)、测试与集成验收(§测试→T1-T6)。全部有对应任务。
- **顺序依赖**：T1 地基（build/version）→ T2 key 解析 → T3 向导（依赖 T2 的 hasApiKey/saveApiKey）→ T4 WebFetch（独立）→ T5 README → T6 验收。T4 可与 T3 并行（互不依赖），但单线执行按序即可。
- **类型一致**：`hasApiKey()`/`saveApiKey(key)`/`Settings.apiKey`（T2 定义，T3 用）；`makeWebFetchTool({client,onUsage})` 返回 `Tool`（T4 定义，装配处用）；`VERSION`（T1 定义，Banner 用）；`runSetup()`（T3 定义，index.ts 用）。前后一致。
- **无占位**：每步含真实代码/命令/期望输出。
- **不碰**：loop/api(除 createClient key 行)/session/tools(除新增 webfetch)/permissions/compact/headless(除工具列表) 逻辑零改动；非 TTY headless 路径不受影响。
- **破坏性**：删 --plain/repl（用户已同意）；package name deepcode→ds-code（命令仍 deepcode）；发布为外部不可逆动作，单列 T6 Step 7 待用户点头。
