# 3.9 Settings 分层 + hook SSRF 加固 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 deepcode 加 CC 式 settings 分层（user<project<local<flag）+ project 不可信来源危险字段剥离 + hook SSRF 两层守卫 + 只读 `/config` 源追溯，且写路径不污染 user 文件。

**Architecture:** 新 `src/settingsLayers.ts`（scope 路径 / 单 scope 解析 + 剥离 / git 闸门 / 合并 + provenance / 顶层装配）复用 `config.ts` 现有 `parseXxx`；新 `src/ssrfGuard.ts`（IP 网段判定 + dns lookup 守卫）接进 `hooks.ts` 的 `execHttpHook`（undici dispatcher 两路 + redirect:error + URL/env 白名单）；`config.ts` 拆 `loadRawUserSettings`/`saveRawUserSettings` 退役裸 `saveSettings`，迁移全部写者防洗白。

**Tech Stack:** TypeScript/ESM、Node 内置 `dns`/`net`/`child_process`、`undici`（`Agent`/`ProxyAgent`/`fetch`，已是依赖）、vitest。

## Global Constraints

- ESM：所有相对 import 带 `.js` 后缀；测试在 `test/*.test.ts`，`import { x } from '../src/x.js'`。
- 测试单跑：`npx vitest run test/<file>.test.ts`；全量：`npm test`。提交前 `npm test` + `npx tsc --noEmit` + `npm run build` 全绿。
- undici 铁律：注入 `dispatcher` 必须配同一 undici 包的 `fetch`（混用 Node 内置 fetch 报 `InvalidArgumentError`）——见 `api.ts:100`。
- 危险字段集（仅从 project 来源、及 git-tracked 降级的 local 剥离）：整键 `apiKey`/`baseURL`/`hooks`/`mcpServers`/`webSearch`/`allowedHttpHookUrls`/`httpHookAllowedEnvVars`；嵌套删 `permissions.allow`（保留 `permissions.deny`）、`skills.sources`（保留 `skills.deny`/`listingBudgetChars`）。
- Scope 优先级低→高：`user < project < local < flag`。合并：数组 concat+去重、对象深合并、标量高优先级胜。剥离在解析前对 raw 对象做。
- 安全件：架构任务（Task 3/5/6/7/8/9）走 opus 全量终审，SSRF/信任边界部分实跑对抗用例。
- 现有单文件 `~/.deepcode/settings.json` 行为字节级不回归（user scope = 旧路径）。
- spec：`docs/specs/2026-06-18-deepcode-3.9-settings-layering-design.md`。

---

### Task 1: `src/ssrfGuard.ts` — IP 网段判定 + DNS lookup 守卫

**Files:**
- Create: `src/ssrfGuard.ts`
- Test: `test/ssrfGuard.test.ts`

**Interfaces:**
- Consumes: 无（纯 Node `dns`/`net`）。
- Produces:
  - `isBlockedAddress(address: string): boolean`
  - `ssrfGuardedLookup(hostname: string, options: object, callback: (err: Error|null, address: any, family?: number) => void): void`
  - `shouldBypassProxy(url: string, noProxy?: string): boolean`

- [ ] **Step 1: 写失败测试 — isBlockedAddress 网段表**

```ts
// test/ssrfGuard.test.ts
import { describe, it, expect } from 'vitest'
import { isBlockedAddress, shouldBypassProxy } from '../src/ssrfGuard.js'

describe('isBlockedAddress', () => {
  it('放行 loopback', () => {
    expect(isBlockedAddress('127.0.0.1')).toBe(false)
    expect(isBlockedAddress('127.5.5.5')).toBe(false)
    expect(isBlockedAddress('::1')).toBe(false)
  })
  it('封私网/链路本地/CGNAT/元数据 IPv4', () => {
    for (const a of ['0.0.0.0', '10.1.2.3', '169.254.169.254', '172.16.0.1', '172.31.255.255', '100.100.100.200', '192.168.1.1']) {
      expect(isBlockedAddress(a)).toBe(true)
    }
  })
  it('放行公网 IPv4', () => {
    expect(isBlockedAddress('8.8.8.8')).toBe(false)
    expect(isBlockedAddress('172.15.0.1')).toBe(false) // 12 边界外
    expect(isBlockedAddress('172.32.0.1')).toBe(false)
  })
  it('封 IPv6 私有/链路本地/unspecified', () => {
    for (const a of ['::', 'fc00::1', 'fd12::3', 'fe80::1']) expect(isBlockedAddress(a)).toBe(true)
  })
  it('封 IPv4-mapped-IPv6 内嵌私网（各表示形态）', () => {
    expect(isBlockedAddress('::ffff:169.254.169.254')).toBe(true)
    expect(isBlockedAddress('::ffff:a9fe:a9fe')).toBe(true) // = 169.254.169.254 hex
    expect(isBlockedAddress('::ffff:10.0.0.1')).toBe(true)
  })
  it('放行 mapped 公网', () => {
    expect(isBlockedAddress('::ffff:8.8.8.8')).toBe(false)
  })
})

describe('shouldBypassProxy', () => {
  it('NO_PROXY 命中 host 返回 true', () => {
    expect(shouldBypassProxy('http://internal.corp/x', 'internal.corp,localhost')).toBe(true)
  })
  it('未命中返回 false', () => {
    expect(shouldBypassProxy('http://example.com/x', 'internal.corp')).toBe(false)
  })
  it('空 NO_PROXY 返回 false', () => {
    expect(shouldBypassProxy('http://example.com', '')).toBe(false)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/ssrfGuard.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 `src/ssrfGuard.ts`**

移植 CC `~/Desktop/src/utils/hooks/ssrfGuard.ts`（已实读）：

```ts
// src/ssrfGuard.ts
import { lookup as dnsLookup } from 'node:dns'
import { isIP } from 'node:net'

/** true = HTTP hook 不应到达该地址（私网/链路本地/CGNAT/元数据）。loopback 放行。 */
export function isBlockedAddress(address: string): boolean {
  const v = isIP(address)
  if (v === 4) return isBlockedV4(address)
  if (v === 6) return isBlockedV6(address)
  return false
}

function isBlockedV4(address: string): boolean {
  const parts = address.split('.').map(Number)
  const [a, b] = parts
  if (parts.length !== 4 || a === undefined || b === undefined || parts.some(n => Number.isNaN(n))) return false
  if (a === 127) return false           // loopback 放行
  if (a === 0) return true              // 0.0.0.0/8
  if (a === 10) return true             // 10/8
  if (a === 169 && b === 254) return true   // 169.254/16 元数据
  if (a === 172 && b >= 16 && b <= 31) return true // 172.16/12
  if (a === 100 && b >= 64 && b <= 127) return true // 100.64/10 CGNAT
  if (a === 192 && b === 168) return true   // 192.168/16
  return false
}

function isBlockedV6(address: string): boolean {
  const lower = address.toLowerCase()
  if (lower === '::1') return false     // loopback 放行
  if (lower === '::') return true
  const mapped = extractMappedIPv4(lower)
  if (mapped !== null) return isBlockedV4(mapped)
  if (lower.startsWith('fc') || lower.startsWith('fd')) return true // fc00::/7
  const first = lower.split(':')[0]
  if (first && first.length === 4 && first >= 'fe80' && first <= 'febf') return true // fe80::/10
  return false
}

function expandIPv6Groups(addr: string): number[] | null {
  let tail: number[] = []
  if (addr.includes('.')) {
    const lastColon = addr.lastIndexOf(':')
    const v4 = addr.slice(lastColon + 1)
    addr = addr.slice(0, lastColon)
    const oct = v4.split('.').map(Number)
    if (oct.length !== 4 || oct.some(n => !Number.isInteger(n) || n < 0 || n > 255)) return null
    tail = [(oct[0]! << 8) | oct[1]!, (oct[2]! << 8) | oct[3]!]
  }
  const dbl = addr.indexOf('::')
  let head: string[]; let rest: string[]
  if (dbl === -1) { head = addr.split(':'); rest = [] }
  else {
    const h = addr.slice(0, dbl); const t = addr.slice(dbl + 2)
    head = h === '' ? [] : h.split(':'); rest = t === '' ? [] : t.split(':')
  }
  const target = 8 - tail.length
  const fill = target - head.length - rest.length
  if (fill < 0) return null
  const hex = [...head, ...new Array<string>(fill).fill('0'), ...rest]
  const nums = hex.map(h => parseInt(h, 16))
  if (nums.some(n => Number.isNaN(n) || n < 0 || n > 0xffff)) return null
  nums.push(...tail)
  return nums.length === 8 ? nums : null
}

function extractMappedIPv4(addr: string): string | null {
  const g = expandIPv6Groups(addr)
  if (!g) return null
  if (g[0] === 0 && g[1] === 0 && g[2] === 0 && g[3] === 0 && g[4] === 0 && g[5] === 0xffff) {
    const hi = g[6]!; const lo = g[7]!
    return `${hi >> 8}.${hi & 0xff}.${lo >> 8}.${lo & 0xff}`
  }
  return null
}

function ssrfError(hostname: string, address: string): NodeJS.ErrnoException {
  const err = new Error(`HTTP hook blocked: ${hostname} 解析到 ${address}（私网/链路本地）。loopback(127.0.0.1, ::1) 放行。`)
  return Object.assign(err, { code: 'ERR_HTTP_HOOK_BLOCKED_ADDRESS', hostname, address })
}

/** dns.lookup 兼容签名：用作 undici Agent connect.lookup，使「校验 IP = 连接 IP」消灭 rebinding 窗口。 */
export function ssrfGuardedLookup(
  hostname: string,
  options: object,
  callback: (err: Error | null, address: any, family?: number) => void,
): void {
  const wantsAll = 'all' in options && (options as any).all === true
  const v = isIP(hostname)
  if (v !== 0) {
    if (isBlockedAddress(hostname)) { callback(ssrfError(hostname, hostname), ''); return }
    const family = v === 6 ? 6 : 4
    if (wantsAll) callback(null, [{ address: hostname, family }])
    else callback(null, hostname, family)
    return
  }
  dnsLookup(hostname, { all: true }, (err, addresses) => {
    if (err) { callback(err, ''); return }
    for (const { address } of addresses) {
      if (isBlockedAddress(address)) { callback(ssrfError(hostname, address), ''); return }
    }
    const first = addresses[0]
    if (!first) { callback(Object.assign(new Error(`ENOTFOUND ${hostname}`), { code: 'ENOTFOUND', hostname }), ''); return }
    const family = first.family === 6 ? 6 : 4
    if (wantsAll) callback(null, addresses.map(a => ({ address: a.address, family: a.family === 6 ? 6 : 4 })))
    else callback(null, first.address, family)
  })
}

/** NO_PROXY 是否豁免该 URL 的 host（豁免 = 直连，不走代理）。逗号分隔后缀匹配。 */
export function shouldBypassProxy(url: string, noProxy = process.env.NO_PROXY ?? process.env.no_proxy ?? ''): boolean {
  if (!noProxy.trim()) return false
  let host: string
  try { host = new URL(url).hostname } catch { return false }
  return noProxy.split(',').map(s => s.trim().toLowerCase()).filter(Boolean).some(entry => {
    const e = entry.replace(/^\*?\.?/, '') // 去前导 * 或 .
    return host === e || host.endsWith('.' + e) || host === entry
  })
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/ssrfGuard.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/ssrfGuard.ts test/ssrfGuard.test.ts
git commit -m "feat(ssrf): IP 网段判定 + DNS lookup 守卫 + NO_PROXY 豁免（移植 CC）"
```

---

### Task 2: `config.ts` — 新 Settings 字段 allowedHttpHookUrls / httpHookAllowedEnvVars

**Files:**
- Modify: `src/config.ts`（Settings 接口 +字段；新增 `parseStringArray`；`loadSettings` 接入）
- Test: `test/config.test.ts`（若无则新建）

**Interfaces:**
- Consumes: 现有 `Settings`、`loadSettings`。
- Produces:
  - `Settings.allowedHttpHookUrls?: string[]`、`Settings.httpHookAllowedEnvVars?: string[]`
  - `parseStringArray(raw: unknown): string[] | undefined`（导出）

- [ ] **Step 1: 写失败测试**

```ts
// test/config.test.ts（追加或新建）
import { describe, it, expect } from 'vitest'
import { parseStringArray } from '../src/config.js'

describe('parseStringArray', () => {
  it('过滤非字符串、trim、去空', () => {
    expect(parseStringArray(['https://a.com', '  https://b.com  ', 42, ''])).toEqual(['https://a.com', 'https://b.com'])
  })
  it('非数组返回 undefined', () => {
    expect(parseStringArray('x')).toBeUndefined()
    expect(parseStringArray(undefined)).toBeUndefined()
  })
  it('空数组返回空数组（区分「全禁」语义）', () => {
    expect(parseStringArray([])).toEqual([])
  })
})
```

> 注意：`[]` 必须保留为 `[]`（allowedHttpHookUrls 的「全禁」语义），不可塌成 undefined。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/config.test.ts`
Expected: FAIL（`parseStringArray` 未导出）

- [ ] **Step 3: 实现**

在 `src/config.ts` `Settings` 接口加两字段（紧跟 `webSearch?` 后）：

```ts
  /** hook URL 白名单（SSRF）：undefined=不限制；[]=全禁；非空=须匹配通配模式。 */
  allowedHttpHookUrls?: string[]
  /** http hook header env 插值的全局白名单；设了则与每个 hook 自身 allowedEnvVars 取交集。 */
  httpHookAllowedEnvVars?: string[]
```

新增导出函数（放在 `parseWebSearchConfig` 后）：

```ts
/** 解析 string[]：过滤非 string、trim、去空。非数组 → undefined；空数组保留为 []（语义区分）。 */
export function parseStringArray(raw: unknown): string[] | undefined {
  if (!Array.isArray(raw)) return undefined
  return raw.filter((s): s is string => typeof s === 'string').map(s => s.trim()).filter(s => s.length > 0)
}
```

`loadSettings()` 返回对象追加两字段：

```ts
    webSearch: parseWebSearchConfig(raw?.webSearch),
    allowedHttpHookUrls: parseStringArray(raw?.allowedHttpHookUrls),
    httpHookAllowedEnvVars: parseStringArray(raw?.httpHookAllowedEnvVars),
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/config.test.ts && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/config.ts test/config.test.ts
git commit -m "feat(config): 新增 allowedHttpHookUrls/httpHookAllowedEnvVars 字段 + parseStringArray"
```

---

### Task 3: `settingsLayers.ts` — 危险字段剥离 stripUntrustedScope

**Files:**
- Create: `src/settingsLayers.ts`
- Test: `test/settingsLayers.test.ts`

**Interfaces:**
- Consumes: 无（操作 raw plain object）。
- Produces:
  - `type SettingScope = 'user' | 'project' | 'local' | 'flag'`
  - `const DANGEROUS_TOP_KEYS: string[]`
  - `stripUntrustedScope(raw: any): { raw: any; stripped: string[] }`（深拷后剥离，返回被剥键名用于 /config 显示）

- [ ] **Step 1: 写失败测试**

```ts
// test/settingsLayers.test.ts
import { describe, it, expect } from 'vitest'
import { stripUntrustedScope } from '../src/settingsLayers.js'

describe('stripUntrustedScope', () => {
  it('剥整键危险字段', () => {
    const { raw, stripped } = stripUntrustedScope({
      model: 'flash', apiKey: 'sk-x', baseURL: 'http://evil', hooks: { Stop: [] },
      mcpServers: { x: { command: 'y' } }, webSearch: { bocha: { apiKey: 'k' } },
      allowedHttpHookUrls: ['http://*'], httpHookAllowedEnvVars: ['SECRET'],
    })
    expect(raw.model).toBe('flash')
    for (const k of ['apiKey', 'baseURL', 'hooks', 'mcpServers', 'webSearch', 'allowedHttpHookUrls', 'httpHookAllowedEnvVars']) {
      expect(raw[k]).toBeUndefined()
      expect(stripped).toContain(k)
    }
  })
  it('嵌套删 permissions.allow 保留 deny', () => {
    const { raw, stripped } = stripUntrustedScope({ permissions: { allow: ['Bash(rm:*)'], deny: ['**/.env'] } })
    expect(raw.permissions.allow).toBeUndefined()
    expect(raw.permissions.deny).toEqual(['**/.env'])
    expect(stripped).toContain('permissions.allow')
  })
  it('嵌套删 skills.sources 保留 deny/listingBudgetChars', () => {
    const { raw, stripped } = stripUntrustedScope({ skills: { sources: ['deepcode'], deny: ['cso'], listingBudgetChars: 4000 } })
    expect(raw.skills.sources).toBeUndefined()
    expect(raw.skills.deny).toEqual(['cso'])
    expect(raw.skills.listingBudgetChars).toBe(4000)
    expect(stripped).toContain('skills.sources')
  })
  it('permissions 只有 allow 时删 allow 后留空对象不报错；不改原入参', () => {
    const input = { permissions: { allow: ['x'] } }
    const { raw } = stripUntrustedScope(input)
    expect(raw.permissions.allow).toBeUndefined()
    expect(input.permissions.allow).toEqual(['x']) // 深拷，原对象不变
  })
  it('无危险字段 stripped 为空', () => {
    const { stripped } = stripUntrustedScope({ model: 'pro', compactTokens: 100 })
    expect(stripped).toEqual([])
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/settingsLayers.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现剥离**

```ts
// src/settingsLayers.ts
export type SettingScope = 'user' | 'project' | 'local' | 'flag'

/** 整键剥离的危险字段（仅 project / git-tracked local）。 */
export const DANGEROUS_TOP_KEYS = [
  'apiKey', 'baseURL', 'hooks', 'mcpServers', 'webSearch',
  'allowedHttpHookUrls', 'httpHookAllowedEnvVars',
] as const

/** 深拷 raw 后剥离危险字段；嵌套删 permissions.allow / skills.sources。返回剥掉的键名（含嵌套路径）。 */
export function stripUntrustedScope(raw: any): { raw: any; stripped: string[] } {
  const out = raw && typeof raw === 'object' ? structuredClone(raw) : raw
  const stripped: string[] = []
  if (!out || typeof out !== 'object') return { raw: out, stripped }
  for (const k of DANGEROUS_TOP_KEYS) {
    if (out[k] !== undefined) { delete out[k]; stripped.push(k) }
  }
  if (out.permissions && typeof out.permissions === 'object' && out.permissions.allow !== undefined) {
    delete out.permissions.allow; stripped.push('permissions.allow')
  }
  if (out.skills && typeof out.skills === 'object' && out.skills.sources !== undefined) {
    delete out.skills.sources; stripped.push('skills.sources')
  }
  return { raw: out, stripped }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/settingsLayers.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/settingsLayers.ts test/settingsLayers.test.ts
git commit -m "feat(settings): stripUntrustedScope 危险字段剥离（含嵌套 allow/sources 删除）"
```

---

### Task 4: `settingsLayers.ts` — git-tracked 信任闸门 isGitTracked

**Files:**
- Modify: `src/settingsLayers.ts`
- Test: `test/settingsLayers.test.ts`

**Interfaces:**
- Consumes: Node `child_process`。
- Produces: `isGitTracked(filePath: string, cwd: string): boolean`（同步 spawn；非 git/出错→false）

- [ ] **Step 1: 写失败测试（用真临时 git 仓库）**

```ts
// test/settingsLayers.test.ts（追加）
import { isGitTracked } from '../src/settingsLayers.js'
import { mkdtempSync, writeFileSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { execFileSync } from 'node:child_process'

describe('isGitTracked', () => {
  it('未在 git 仓库 → false', () => {
    const dir = mkdtempSync(join(tmpdir(), 'dc-nogit-'))
    try {
      writeFileSync(join(dir, 'settings.local.json'), '{}')
      expect(isGitTracked(join(dir, 'settings.local.json'), dir)).toBe(false)
    } finally { rmSync(dir, { recursive: true, force: true }) }
  })
  it('被 git 跟踪 → true；未跟踪 → false', () => {
    const dir = mkdtempSync(join(tmpdir(), 'dc-git-'))
    try {
      execFileSync('git', ['init', '-q'], { cwd: dir })
      execFileSync('git', ['config', 'user.email', 't@t'], { cwd: dir })
      execFileSync('git', ['config', 'user.name', 't'], { cwd: dir })
      const tracked = join(dir, 'tracked.json')
      writeFileSync(tracked, '{}')
      execFileSync('git', ['add', 'tracked.json'], { cwd: dir })
      execFileSync('git', ['commit', '-qm', 'x'], { cwd: dir })
      const untracked = join(dir, 'untracked.json')
      writeFileSync(untracked, '{}')
      expect(isGitTracked(tracked, dir)).toBe(true)
      expect(isGitTracked(untracked, dir)).toBe(false)
    } finally { rmSync(dir, { recursive: true, force: true }) }
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/settingsLayers.test.ts -t isGitTracked`
Expected: FAIL

- [ ] **Step 3: 实现**

在 `src/settingsLayers.ts` 顶部加 import 与函数：

```ts
import { execFileSync } from 'node:child_process'

/** 文件是否被 git 跟踪（= repo 随身携带、非用户手写）。非 git 仓库 / git 不可用 → false。 */
export function isGitTracked(filePath: string, cwd: string): boolean {
  try {
    execFileSync('git', ['ls-files', '--error-unmatch', filePath], { cwd, stdio: 'ignore' })
    return true
  } catch {
    return false
  }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/settingsLayers.test.ts -t isGitTracked`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/settingsLayers.ts test/settingsLayers.test.ts
git commit -m "feat(settings): isGitTracked 闸门（git ls-files --error-unmatch）"
```

---

### Task 5: `settingsLayers.ts` — 合并 + provenance mergeScopePartials

**Files:**
- Modify: `src/settingsLayers.ts`
- Test: `test/settingsLayers.test.ts`

**Interfaces:**
- Consumes: `config.ts` 的 `DEFAULT` 常量（本任务在 settingsLayers 内定义 `DEFAULT_SETTINGS`，避免循环依赖）。
- Produces:
  - `interface ScopePartial { scope: SettingScope; partial: Record<string, unknown> }`
  - `function mergeScopePartials(layers: ScopePartial[]): { settings: any; provenance: Record<string, SettingScope | 'merged'> }`
  - `const DEFAULT_SETTINGS`（compactTokens 200000 / costWarnCNY 15 / maxToolResultChars 100000 / permissions {allow:[]}）

> 合并规则：按 layers 顺序（已是 user→project→local→flag）折叠。每个顶层 key：值为数组 → concat+去重（provenance 若 >1 scope 贡献则 'merged'）；值为对象 → 深合并（数组子字段同样 concat+去重）；标量 → 后者覆盖。provenance[key] = 贡献该 key 的最后一个 scope，或 'merged'（多 scope 贡献数组/对象时）。

- [ ] **Step 1: 写失败测试**

```ts
// test/settingsLayers.test.ts（追加）
import { mergeScopePartials } from '../src/settingsLayers.js'

describe('mergeScopePartials', () => {
  it('标量高优先级胜 + provenance', () => {
    const { settings, provenance } = mergeScopePartials([
      { scope: 'user', partial: { model: 'flash', compactTokens: 100 } },
      { scope: 'project', partial: { model: 'pro' } },
    ])
    expect(settings.model).toBe('pro')
    expect(provenance.model).toBe('project')
    expect(provenance.compactTokens).toBe('user')
  })
  it('数组 concat 去重 + provenance=merged', () => {
    const { settings, provenance } = mergeScopePartials([
      { scope: 'user', partial: { permissions: { allow: ['A'], deny: ['D1'] } } },
      { scope: 'project', partial: { permissions: { deny: ['D1', 'D2'] } } },
    ])
    expect(settings.permissions.allow).toEqual(['A'])
    expect(settings.permissions.deny).toEqual(['D1', 'D2'])
    expect(provenance.permissions).toBe('merged')
  })
  it('缺省值兜底（无 scope 设 compactTokens）', () => {
    const { settings } = mergeScopePartials([{ scope: 'user', partial: { model: 'x' } }])
    expect(settings.compactTokens).toBe(200000)
    expect(settings.maxToolResultChars).toBe(100000)
    expect(settings.permissions.allow).toEqual([])
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/settingsLayers.test.ts -t mergeScopePartials`
Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/settingsLayers.ts（追加）
export interface ScopePartial { scope: SettingScope; partial: Record<string, unknown> }

const DEFAULT_SETTINGS: Record<string, unknown> = {
  permissions: { allow: [] as string[] },
  compactTokens: 200_000,
  costWarnCNY: 15,
  maxToolResultChars: 100_000,
}

function uniq<T>(arr: T[]): T[] { return [...new Set(arr)] }
function isPlainObject(v: unknown): v is Record<string, unknown> {
  return !!v && typeof v === 'object' && !Array.isArray(v)
}

/** 深合并 src 进 target（就地）：数组 concat+去重、对象递归、标量覆盖。 */
function deepMergeInto(target: Record<string, unknown>, src: Record<string, unknown>): void {
  for (const [k, v] of Object.entries(src)) {
    if (v === undefined) continue
    const cur = target[k]
    if (Array.isArray(cur) && Array.isArray(v)) target[k] = uniq([...cur, ...v])
    else if (isPlainObject(cur) && isPlainObject(v)) { const c = { ...cur }; deepMergeInto(c, v); target[k] = c }
    else target[k] = Array.isArray(v) ? [...v] : isPlainObject(v) ? { ...v } : v
  }
}

export function mergeScopePartials(layers: ScopePartial[]): {
  settings: any
  provenance: Record<string, SettingScope | 'merged'>
} {
  const settings: Record<string, unknown> = structuredClone(DEFAULT_SETTINGS)
  const contributors: Record<string, Set<SettingScope>> = {}
  for (const { scope, partial } of layers) {
    for (const k of Object.keys(partial)) {
      if (partial[k] === undefined) continue
      ;(contributors[k] ??= new Set()).add(scope)
    }
    deepMergeInto(settings, partial)
  }
  const provenance: Record<string, SettingScope | 'merged'> = {}
  for (const [k, set] of Object.entries(contributors)) {
    provenance[k] = set.size > 1 ? 'merged' : [...set][0]!
  }
  return { settings, provenance }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/settingsLayers.test.ts -t mergeScopePartials`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/settingsLayers.ts test/settingsLayers.test.ts
git commit -m "feat(settings): mergeScopePartials 分层合并 + 顶层 key provenance"
```

---

### Task 6: `settingsLayers.ts` — scope 路径 + 单 scope 解析 + loadLayeredSettings 装配

**Files:**
- Modify: `src/settingsLayers.ts`
- Test: `test/settingsLayers.test.ts`

**Interfaces:**
- Consumes: `config.ts` 的 `parsePermissions`/`parseHooksConfig`/`parseMcpServers`/`parseSkillsConfig`/`parseWebSearchConfig`/`parseStringArray`、`SETTINGS_FILE`；本模块 `stripUntrustedScope`/`isGitTracked`/`mergeScopePartials`。
- Produces:
  - `interface LoadedScope { scope: SettingScope; path: string; present: boolean; demoted: boolean; stripped: string[] }`
  - `interface LayeredResult { settings: Settings; provenance: Record<string, SettingScope|'merged'>; scopes: LoadedScope[] }`
  - `function loadLayeredSettings(cwd?: string, flagPath?: string): LayeredResult`

> loadScope 只对 raw 中**实际存在**的 key 跑对应 parser（保 provenance 干净；不注入默认）。project 必剥离；local 若 `isGitTracked` 为 true 则降级剥离（demoted=true）。

- [ ] **Step 1: 写失败测试（用临时目录写三层文件）**

```ts
// test/settingsLayers.test.ts（追加）
import { loadLayeredSettings } from '../src/settingsLayers.js'
import { mkdtempSync as mkd, writeFileSync as wf, rmSync as rm } from 'node:fs'
import { tmpdir as td } from 'node:os'
import { join as jn } from 'node:path'

describe('loadLayeredSettings', () => {
  it('project 危险字段被剥、安全字段生效、deny 合并', () => {
    const dir = mkd(jn(td(), 'dc-layer-'))
    try {
      wf(jn(dir, '.deepcode'), '') // 占位，下面建真目录
    } catch {}
    rm(jn(dir, '.deepcode'), { force: true })
    const { mkdirSync } = require('node:fs')
    mkdirSync(jn(dir, '.deepcode'), { recursive: true })
    try {
      wf(jn(dir, '.deepcode', 'settings.json'), JSON.stringify({
        model: 'pro', apiKey: 'sk-evil', hooks: { Stop: [{ hooks: [{ type: 'command', command: 'rm -rf /' }] }] },
        permissions: { allow: ['Bash(rm:*)'], deny: ['**/.secret'] },
      }))
      const res = loadLayeredSettings(dir, undefined)
      expect(res.settings.model).toBe('pro')          // 安全字段生效
      expect(res.settings.apiKey).toBeUndefined()      // 危险整键剥
      expect(res.settings.hooks).toBeUndefined()
      expect(res.settings.permissions.allow).toEqual([]) // project allow 剥（只剩 user 默认空）
      expect(res.settings.permissions.deny).toEqual(['**/.secret']) // deny 保留
      const proj = res.scopes.find(s => s.scope === 'project')!
      expect(proj.stripped).toEqual(expect.arrayContaining(['apiKey', 'hooks', 'permissions.allow']))
    } finally { rm(dir, { recursive: true, force: true }) }
  })
})
```

> 注：上面占位写法繁琐，实现测试时可直接 `mkdirSync(jn(dir,'.deepcode'),{recursive:true})` 一行；user scope 指向 `SETTINGS_FILE`（真 home），测试聚焦 project，断言不依赖 user 内容（用默认空 allow）。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/settingsLayers.test.ts -t loadLayeredSettings`
Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/settingsLayers.ts（追加）
import fs from 'node:fs'
import path from 'node:path'
import os from 'node:os'
import {
  parsePermissions, parseHooksConfig, parseMcpServers, parseSkillsConfig,
  parseWebSearchConfig, parseStringArray, type Settings,
} from './config.js'

export interface LoadedScope {
  scope: SettingScope; path: string; present: boolean; demoted: boolean; stripped: string[]
}
export interface LayeredResult {
  settings: Settings
  provenance: Record<string, SettingScope | 'merged'>
  scopes: LoadedScope[]
}

function scopePaths(cwd: string, flagPath?: string): { scope: SettingScope; path: string }[] {
  const out: { scope: SettingScope; path: string }[] = [
    { scope: 'user', path: path.join(os.homedir(), '.deepcode', 'settings.json') },
    { scope: 'project', path: path.join(cwd, '.deepcode', 'settings.json') },
    { scope: 'local', path: path.join(cwd, '.deepcode', 'settings.local.json') },
  ]
  if (flagPath) out.push({ scope: 'flag', path: flagPath })
  return out
}

/** 只对 raw 中实际存在的 key 跑对应 parser，产出 partial（不注入默认）。 */
function parsePresent(raw: any): Record<string, unknown> {
  const p: Record<string, unknown> = {}
  if (!raw || typeof raw !== 'object') return p
  if ('permissions' in raw) p.permissions = parsePermissions(raw)
  for (const k of ['compactTokens', 'costWarnCNY', 'maxToolResultChars', 'model', 'baseURL', 'apiKey', 'inline'] as const) {
    if (raw[k] !== undefined) p[k] = raw[k]
  }
  if ('hooks' in raw) { const h = parseHooksConfig(raw.hooks); if (h) p.hooks = h }
  if ('mcpServers' in raw) { const m = parseMcpServers(raw.mcpServers); if (m) p.mcpServers = m }
  if ('skills' in raw) { const s = parseSkillsConfig(raw.skills); if (s) p.skills = s }
  if ('webSearch' in raw) { const w = parseWebSearchConfig(raw.webSearch); if (w) p.webSearch = w }
  if ('allowedHttpHookUrls' in raw) { const a = parseStringArray(raw.allowedHttpHookUrls); if (a) p.allowedHttpHookUrls = a }
  if ('httpHookAllowedEnvVars' in raw) { const a = parseStringArray(raw.httpHookAllowedEnvVars); if (a) p.httpHookAllowedEnvVars = a }
  return p
}

function readRaw(file: string): any | undefined {
  try { return JSON.parse(fs.readFileSync(file, 'utf8')) } catch { return undefined }
}

export function loadLayeredSettings(cwd: string = process.cwd(), flagPath?: string): LayeredResult {
  const layers: ScopePartial[] = []
  const scopes: LoadedScope[] = []
  for (const { scope, path: file } of scopePaths(cwd, flagPath)) {
    const raw = readRaw(file)
    const present = raw !== undefined
    let stripped: string[] = []
    let demoted = false
    let effective = raw
    if (present) {
      const untrusted = scope === 'project' || (scope === 'local' && isGitTracked(file, cwd))
      if (scope === 'local' && untrusted) demoted = true
      if (untrusted) { const r = stripUntrustedScope(raw); effective = r.raw; stripped = r.stripped }
      layers.push({ scope, partial: parsePresent(effective) })
    }
    scopes.push({ scope, path: file, present, demoted, stripped })
  }
  const { settings, provenance } = mergeScopePartials(layers)
  return { settings: settings as Settings, provenance, scopes }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/settingsLayers.test.ts && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/settingsLayers.ts test/settingsLayers.test.ts
git commit -m "feat(settings): loadLayeredSettings 装配（scope 路径/单 scope 解析/project+tracked-local 剥离降级）"
```

---

### Task 7: `config.ts` — loadSettings 委托分层 + 拆 loadRawUserSettings/saveRawUserSettings + 退役 saveSettings

**Files:**
- Modify: `src/config.ts`
- Test: `test/config.test.ts`

**Interfaces:**
- Consumes: `settingsLayers.ts` `loadLayeredSettings`。
- Produces:
  - `loadSettings(cwd?: string, flagPath?: string): Settings`（委托 `loadLayeredSettings(...).settings`）
  - `loadRawUserSettings(): Settings`（= 旧 loadSettings 单文件实现）
  - `saveRawUserSettings(s: Settings): void`（= 旧 saveSettings + chmod 600）
  - 移除 `saveSettings`（改名 `saveRawUserSettings`；全 caller 迁移见 Task 8）

> 循环依赖处理：`config.ts` 现已被 `settingsLayers.ts` import（取 parsers）。`config.ts` 反向 import `loadLayeredSettings` 形成环。解法：`loadSettings` 内用**动态 import**（`await import`）不可行（loadSettings 同步）。改为 `settingsLayers.ts` 不 import config 的 loadSettings（只 import parsers，无环），而 `config.ts` 顶部静态 `import { loadLayeredSettings } from './settingsLayers.js'`——ESM 允许循环，只要 `loadLayeredSettings` 在调用时已定义（运行时调用，非模块初始化期），安全。验证 typecheck + 测试通过即可。

- [ ] **Step 1: 写失败测试（回归：单文件行为不变 + 委托）**

```ts
// test/config.test.ts（追加）
import { loadRawUserSettings, saveRawUserSettings } from '../src/config.js'

describe('raw user settings 读写', () => {
  it('saveRawUserSettings 后 loadRawUserSettings 往返', () => {
    const before = loadRawUserSettings()
    const probe = { ...before, costWarnCNY: 42 }
    saveRawUserSettings(probe)
    try {
      expect(loadRawUserSettings().costWarnCNY).toBe(42)
    } finally {
      saveRawUserSettings(before) // 还原，避免污染真 ~/.deepcode
    }
  })
})
```

> 注：此测试读写真 `~/.deepcode/settings.json`，用 try/finally 还原。若 CI 无该文件，`loadRawUserSettings` 返回默认对象、`saveRawUserSettings` 创建之——还原写回默认即可。subagent 执行时确认不破坏用户真实 key（before 已含则还原含）。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/config.test.ts -t "raw user settings"`
Expected: FAIL（函数未导出）

- [ ] **Step 3: 实现**

`src/config.ts`：顶部加 `import { loadLayeredSettings } from './settingsLayers.js'`。

把现 `loadSettings` 函数体整体改名为 `loadRawUserSettings`（签名不变、逻辑不变）；新 `loadSettings` 委托：

```ts
/** 单文件原始 user scope 解析（写路径用；= 旧 loadSettings 实现）。 */
export function loadRawUserSettings(): Settings {
  let raw: any = {}
  try { raw = JSON.parse(fs.readFileSync(FILE, 'utf8')) } catch { /* 用默认 */ }
  return {
    permissions: parsePermissions(raw),
    compactTokens: raw?.compactTokens ?? 200_000,
    costWarnCNY: raw?.costWarnCNY ?? raw?.costWarnUSD ?? 15,
    maxToolResultChars: raw?.maxToolResultChars ?? 100_000,
    model: raw?.model, baseURL: raw?.baseURL, apiKey: raw?.apiKey, inline: raw?.inline,
    hooks: parseHooksConfig(raw?.hooks), mcpServers: parseMcpServers(raw?.mcpServers),
    skills: parseSkillsConfig(raw?.skills), webSearch: parseWebSearchConfig(raw?.webSearch),
    allowedHttpHookUrls: parseStringArray(raw?.allowedHttpHookUrls),
    httpHookAllowedEnvVars: parseStringArray(raw?.httpHookAllowedEnvVars),
  }
}

/** 运行时合并配置（分层）。所有只读消费者用此。 */
export function loadSettings(cwd?: string, flagPath?: string): Settings {
  return loadLayeredSettings(cwd, flagPath).settings
}
```

把现 `saveSettings` 改名 `saveRawUserSettings`（逻辑不变）：

```ts
/** 只写 user scope 原始文件（分层下唯一写目标，防洗白）。 */
export function saveRawUserSettings(s: Settings): void {
  fs.mkdirSync(DIR, { recursive: true })
  fs.writeFileSync(FILE, JSON.stringify(s, null, 2))
}
```

`saveApiKey` 内部改用 raw：

```ts
export function saveApiKey(key: string): void {
  const s = loadRawUserSettings()
  const hadKey = !!s.apiKey
  s.apiKey = key || undefined
  saveRawUserSettings(s)
  try { fs.chmodSync(FILE, 0o600) } catch { /* 尽力 */ }
  if (s.hooks) void runHooks('Setup', { hook_event_name: 'Setup', cwd: process.cwd(), trigger: hadKey ? 'maintenance' : 'init' }, s.hooks).catch(() => {})
}
```

`hasApiKey` 内 `loadSettings().apiKey` 改 `loadRawUserSettings().apiKey`（key 只该来自 user/env，不该被 project 影响——虽 apiKey 已从 project 剥，但读 raw 更语义正确且省一次分层）。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/config.test.ts && npx tsc --noEmit`
Expected: PASS（注意 `saveSettings` 已改名 → 下个 Task 修剩余 caller）

- [ ] **Step 5: 提交**

```bash
git add src/config.ts test/config.test.ts
git commit -m "feat(config): loadSettings 委托分层 + 拆 loadRawUserSettings/saveRawUserSettings 退役 saveSettings"
```

---

### Task 8: 写路径迁移 — useChat saveRule + /permissions rm 防洗白

**Files:**
- Modify: `src/tui/useChat.ts`（`:23` import、`:535` saveRule、`:771-783` /permissions）
- Test: `test/settingsLayers.test.ts`（新增写路径不污染单测，针对纯逻辑提取）

**Interfaces:**
- Consumes: `config.ts` `loadRawUserSettings`/`saveRawUserSettings`。
- Produces: 新纯函数 `addUserAllowRule(rule: string): void` / `removeUserAllowRule(index: number): string | undefined`（放 `config.ts`，raw-user RMW，便于单测）。

> 设计：把「写 user-scope allow 规则」抽成 config.ts 纯函数做 raw RMW，useChat 调用之；同时同步更新内存合并 `settings.permissions.allow`（保本会话即时生效）。`/permissions` 列表/删除改对 **user-scope** allow（保 rm 索引一致）。

- [ ] **Step 1: 写失败测试（config.ts allow 规则 raw RMW 不动其它字段）**

```ts
// test/config.test.ts（追加）
import { addUserAllowRule, removeUserAllowRule, loadRawUserSettings, saveRawUserSettings } from '../src/config.js'

describe('user-scope allow 规则 RMW', () => {
  it('add 只动 user.permissions.allow，不引入其它 scope 字段', () => {
    const before = loadRawUserSettings()
    try {
      addUserAllowRule('Bash(echo:*)')
      const after = loadRawUserSettings()
      expect(after.permissions.allow).toContain('Bash(echo:*)')
      const removed = removeUserAllowRule(after.permissions.allow.indexOf('Bash(echo:*)'))
      expect(removed).toBe('Bash(echo:*)')
      expect(loadRawUserSettings().permissions.allow).not.toContain('Bash(echo:*)')
    } finally { saveRawUserSettings(before) }
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/config.test.ts -t "allow 规则 RMW"`
Expected: FAIL

- [ ] **Step 3: 实现 config.ts 纯函数 + 迁移 useChat**

`src/config.ts` 加：

```ts
/** 往 user scope allow 列表加规则（raw RMW，不触其它 scope）。返回更新后的 user allow 数组。 */
export function addUserAllowRule(rule: string): string[] {
  const s = loadRawUserSettings()
  if (!s.permissions.allow.includes(rule)) s.permissions.allow.push(rule)
  saveRawUserSettings(s)
  return s.permissions.allow
}
/** 按 user scope allow 索引删除一条；返回被删规则或 undefined。 */
export function removeUserAllowRule(index: number): string | undefined {
  const s = loadRawUserSettings()
  if (s.permissions.allow[index] === undefined) return undefined
  const [removed] = s.permissions.allow.splice(index, 1)
  saveRawUserSettings(s)
  return removed
}
/** 读 user scope allow 列表（/permissions 显示用，保 rm 索引一致）。 */
export function listUserAllowRules(): string[] {
  return loadRawUserSettings().permissions.allow
}
```

`src/tui/useChat.ts:23` import 改：去掉 `saveSettings`，加 `loadRawUserSettings, addUserAllowRule, removeUserAllowRule, listUserAllowRules`（保留 `loadSettings, SETTINGS_FILE`）。

`:535` saveRule 改（写 user scope + 同步内存合并 allow）：

```ts
          saveRule: r => {
            const userAllow = addUserAllowRule(r)        // 持久化到 user scope（raw RMW）
            if (!settings.permissions.allow.includes(r)) settings.permissions.allow.push(r) // 内存合并即时生效
            void userAllow
            fireConfigChange()
          },
```

`:771-783` `/permissions` 改对 user scope：

```ts
    if (line === '/permissions' || line.startsWith('/permissions ')) {
      const arg = line.slice('/permissions'.length).trim()
      const m = arg.match(/^rm\s+(\d+)$/)
      const userRules = listUserAllowRules()
      if (m) {
        const i = Number(m[1]) - 1
        const removed = removeUserAllowRule(i)
        if (removed !== undefined) {
          const mem = settings.permissions.allow.indexOf(removed)
          if (mem >= 0) settings.permissions.allow.splice(mem, 1) // 内存合并同步
          notice('info', `已删除：${removed}`)
          fireConfigChange()
        } else notice('warn', '编号无效')
      } else if (userRules.length) {
        notice('info', userRules.map((r, i) => `  ${i + 1}. ${r}`).join('\n') + '\n（/permissions rm <编号> 删除对应规则）')
      } else notice('info', '没有已保存的权限规则')
    }
```

检查 `useChat.ts` 是否还有别的 `saveSettings(` 调用（grep）。若 `:778` 之外还有，逐一迁移到 raw-user 写。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/config.test.ts && npx tsc --noEmit && npx vitest run`
Expected: PASS（全量绿；确认 useChat 无残留 `saveSettings` 引用）

- [ ] **Step 5: 提交**

```bash
git add src/config.ts src/tui/useChat.ts test/config.test.ts
git commit -m "fix(settings): 写路径迁移 saveRule//permissions rm 走 user-scope raw RMW 防洗白"
```

---

### Task 9: `execHttpHook` SSRF 接线（URL 白名单 + undici dispatcher 两路 + redirect:error + env 交集）

**Files:**
- Modify: `src/hooks.ts`（`execHttpHook`、`ResolvedHookDeps`、`interpolateEnvVars` 调用处、默认 deps fetch、`urlMatchesPattern` 新增）
- Test: `test/hooks.http.test.ts`（新建）

**Interfaces:**
- Consumes: `ssrfGuard.ts` `ssrfGuardedLookup`/`shouldBypassProxy`；undici `Agent`/`ProxyAgent`/`fetch`。
- Produces:
  - `urlMatchesPattern(url: string, pattern: string): boolean`（导出，便于测）
  - `ResolvedHookDeps` 加 `allowedHttpHookUrls?: string[]`、`httpHookAllowedEnvVars?: string[]`
  - `execHttpHook` 行为：URL 不在白名单 → blocking（不发请求）；env 插值取交集；dispatcher 两路；redirect:error。

> deepcode `execHttpHook` 现用 `deps.fetch`（全局 fetch，默认跟随重定向、无法注入 lookup）。改：默认 `deps.fetch` = undici `fetch`；选 dispatcher（代理→ProxyAgent 跳守卫；否则 `new Agent({connect:{lookup:ssrfGuardedLookup}})`）；`redirect:'error'`。

- [ ] **Step 1: 写失败测试**

```ts
// test/hooks.http.test.ts
import { describe, it, expect, vi } from 'vitest'
import { urlMatchesPattern } from '../src/hooks.js'

describe('urlMatchesPattern', () => {
  it('* 通配 + 正则元字符转义', () => {
    expect(urlMatchesPattern('https://hooks.example.com/x', 'https://hooks.example.com/*')).toBe(true)
    expect(urlMatchesPattern('https://evil.com/x', 'https://hooks.example.com/*')).toBe(false)
    expect(urlMatchesPattern('https://a.b.com', 'https://*.b.com')).toBe(true)
    expect(urlMatchesPattern('https://a.bXcom', 'https://a.b.com')).toBe(false) // . 不当通配
  })
})
```

> 注：execHttpHook 的 URL-白名单阻断 + env 交集走集成测试（mock `deps.fetch`，断言白名单外不调用 fetch、交集后只插值白名单变量）；dispatcher 真连不在单测（网络），由 Task 1 的 `isBlockedAddress`/`ssrfGuardedLookup` + opus 终审覆盖。下面 Step 3 一并加该集成测试。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/hooks.http.test.ts`
Expected: FAIL（`urlMatchesPattern` 未导出）

- [ ] **Step 3: 实现 + 加集成测试**

`src/hooks.ts` 顶部 import：

```ts
import { Agent, ProxyAgent, fetch as undiciFetch } from 'undici'
import { ssrfGuardedLookup, shouldBypassProxy } from './ssrfGuard.js'
```

新增导出（放 `interpolateEnvVars` 附近）：

```ts
/** URL 通配匹配（* → 任意字符），其余正则元字符转义。对齐 CC urlMatchesPattern。 */
export function urlMatchesPattern(url: string, pattern: string): boolean {
  const escaped = pattern.replace(/[.+?^${}()|[\]\\]/g, '\\$&')
  return new RegExp(`^${escaped.replace(/\*/g, '.*')}$`).test(url)
}

/** 选 dispatcher：代理激活→ProxyAgent（跳 IP 守卫，代理做 DNS）；否则 Agent({connect:{lookup}})。 */
function selectHttpDispatcher(url: string): Agent | ProxyAgent {
  const proxy = process.env.https_proxy ?? process.env.HTTPS_PROXY ?? process.env.http_proxy ?? process.env.HTTP_PROXY
  if (proxy && !shouldBypassProxy(url)) return new ProxyAgent(proxy)
  return new Agent({ connect: { lookup: ssrfGuardedLookup as any } })
}
```

`ResolvedHookDeps` 接口加（在 `fetch: typeof fetch` 附近）：

```ts
  allowedHttpHookUrls?: string[]
  httpHookAllowedEnvVars?: string[]
```

`execHttpHook` 改造（替换现 `:336-357` 函数体核心）：

```ts
async function execHttpHook(hook: HttpHook, payload: Record<string, unknown>, deps: ResolvedHookDeps): Promise<HookResult> {
  const base: HookResult = { outcome: 'success', label: '', durationMs: 0 }
  // 1. URL 白名单（I/O 前）
  const allow = deps.allowedHttpHookUrls
  if (allow !== undefined && !allow.some(p => urlMatchesPattern(hook.url, p))) {
    return { ...base, outcome: 'blocking', preventContinuation: true, blockingError: `HTTP hook blocked: ${hook.url} 不匹配 allowedHttpHookUrls` }
  }
  // 2. env 插值白名单：hook 自身 ∩ policy（若设）
  const hookVars = hook.allowedEnvVars ?? []
  const effectiveVars = deps.httpHookAllowedEnvVars !== undefined
    ? hookVars.filter(v => deps.httpHookAllowedEnvVars!.includes(v))
    : hookVars
  const allowed = new Set(effectiveVars)
  const headers: Record<string, string> = { 'Content-Type': 'application/json' }
  for (const [k, v] of Object.entries(hook.headers ?? {})) headers[k] = interpolateEnvVars(v, allowed)
  // 3. dispatcher 两路 + redirect:error
  const ac = new AbortController()
  const timer = setTimeout(() => ac.abort(), (hook.timeout ?? 30) * 1000)
  try {
    const res = await deps.fetch(hook.url, {
      method: 'POST', headers, body: JSON.stringify(payload), signal: ac.signal,
      redirect: 'error',
      dispatcher: selectHttpDispatcher(hook.url),
    } as any)
    const bodyText = (await res.text()).trim()
    let json: any
    if (bodyText) { try { json = JSON.parse(bodyText) } catch { /* 非 JSON */ } }
    let r = (json && typeof json === 'object' && !Array.isArray(json)) ? applyHookJson(json, base) : { ...base }
    if (res.status < 200 || res.status >= 300) r = { ...r, outcome: 'blocking', preventContinuation: true, blockingError: r.blockingError ?? `HTTP ${res.status}` }
    return r
  } catch (e) {
    if (ac.signal.aborted) return { ...base, outcome: 'cancelled' }
    return { ...base, outcome: 'non_blocking_error', blockingError: String((e as any)?.message ?? e) }
  } finally { clearTimeout(timer) }
}
```

默认 deps.fetch 改 undici（`makeHookDeps` 处 `:409` 附近）：`fetch: deps.fetch ?? (undiciFetch as unknown as typeof fetch)`。

接线把 settings 的两字段透传进 hook deps：在 `useChat.ts`/`headless.ts` 构造 hookDeps 处加 `allowedHttpHookUrls: settings.allowedHttpHookUrls, httpHookAllowedEnvVars: settings.httpHookAllowedEnvVars`（grep `makeHookDeps`/`hookDeps` 找构造点）。

集成测试追加（mock fetch 断言白名单阻断 + 交集）：

```ts
// test/hooks.http.test.ts（追加）— 通过 runHooks 或直接构造 deps 调 execHttpHook 的导出包装
// 若 execHttpHook 非导出，测 runHooks 的 http 路径：构造 hooks 配置 + mock deps.fetch，
// 断言：allowedHttpHookUrls 不含该 url 时 fetch 未被调用、结果 outcome='blocking'；
// httpHookAllowedEnvVars=['A'] 且 hook.allowedEnvVars=['A','B'] 时 header 只插值 A。
// （实现 subagent 按 hooks.ts 现有 runHooks/makeHookDeps 测试惯例补齐 mock。）
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/hooks.http.test.ts && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/hooks.ts test/hooks.http.test.ts src/tui/useChat.ts src/headless.ts
git commit -m "feat(ssrf): execHttpHook 接 URL 白名单 + undici dispatcher 两路 + redirect:error + env 交集"
```

---

### Task 10: flag scope `--settings <path>` CLI 接线

**Files:**
- Modify: `src/index.ts`（解析 `--settings`）、`src/headless.ts`、`src/tui/useChat.ts`（透传 flagPath 给 loadSettings/loadLayeredSettings）
- Test: `test/settingsLayers.test.ts`（flag 最高优先级覆盖，已可由 loadLayeredSettings 直接测）

**Interfaces:**
- Consumes: `loadLayeredSettings(cwd, flagPath)`（已支持 flagPath）。
- Produces: CLI `--settings <path>` 解析 + 透传。

- [ ] **Step 1: 写失败测试（flag 覆盖 user）**

```ts
// test/settingsLayers.test.ts（追加）
describe('flag scope', () => {
  it('--settings 文件最高优先级覆盖', () => {
    const { mkdtempSync, writeFileSync, rmSync } = require('node:fs')
    const { tmpdir } = require('node:os'); const { join } = require('node:path')
    const dir = mkdtempSync(join(tmpdir(), 'dc-flag-'))
    const flagFile = join(dir, 'flag.json')
    try {
      writeFileSync(flagFile, JSON.stringify({ model: 'flag-model', apiKey: 'sk-flag' }))
      const res = loadLayeredSettings(dir, flagFile)
      expect(res.settings.model).toBe('flag-model')
      expect(res.settings.apiKey).toBe('sk-flag') // flag 可信，apiKey 不剥
      expect(res.provenance.model).toBe('flag')
    } finally { rmSync(dir, { recursive: true, force: true }) }
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/settingsLayers.test.ts -t "flag scope"`
Expected: 大概率已 PASS（loadLayeredSettings 早支持 flagPath）。若 PASS 跳到 Step 3 做 CLI 接线；若 FAIL 修 loadLayeredSettings。

- [ ] **Step 3: CLI 接线**

`src/index.ts`：在现有 flag 解析处（`yolo`/`inline`/`continueSession` 附近）加：

```ts
const settingsFlagIdx = process.argv.indexOf('--settings')
const flagSettingsPath = settingsFlagIdx >= 0 ? process.argv[settingsFlagIdx + 1] : undefined
```

把 `flagSettingsPath` 透传：`loadSettings()` 调用点改 `loadSettings(process.cwd(), flagSettingsPath)`（`index.ts:44` 的 `loadSettings().inline` 改 `loadSettings(process.cwd(), flagSettingsPath).inline`），并传入 `startTui`/headless 入口让其内部 `loadSettings` 也带 flagPath。

`src/headless.ts:39` `loadSettings()` → 接受并透传 flagPath 参数（headless 入口函数签名加可选 `flagSettingsPath`，由 index.ts 传入）。

`src/tui/useChat.ts:200` `loadSettings()` → 同样透传（useChat 入参或 startTui props 带 flagSettingsPath）。

> 实现 subagent：grep `loadSettings(` 全部调用点，运行时读取处统一带上 `(cwd, flagPath)`；写路径（saveApiKey 等）不受影响（用 raw user）。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run && npx tsc --noEmit && npm run build`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/index.ts src/headless.ts src/tui/useChat.ts test/settingsLayers.test.ts
git commit -m "feat(settings): --settings <path> flag scope CLI 接线（最高优先级）"
```

---

### Task 11: `/config` 只读源追溯命令

**Files:**
- Create: `src/configReport.ts`（纯函数 `formatConfigReport(result: LayeredResult): string`）
- Modify: `src/tui/useChat.ts`（`/config` 斜杠分发）
- Test: `test/configReport.test.ts`

**Interfaces:**
- Consumes: `settingsLayers.ts` `LayeredResult`。
- Produces: `formatConfigReport(result: LayeredResult): string`。

> 渲染：每顶层 key 合并值 + 来源；数组字段标条数 + [merged: …]；被剥 project 字段警告行；project 贡献 deny 计数；local 降级提示；末尾文件列表；apiKey 打码。

- [ ] **Step 1: 写失败测试**

```ts
// test/configReport.test.ts
import { describe, it, expect } from 'vitest'
import { formatConfigReport } from '../src/configReport.js'

describe('formatConfigReport', () => {
  it('标来源、打码 apiKey、警告剥离、列文件', () => {
    const out = formatConfigReport({
      settings: { permissions: { allow: [], deny: ['**/.env'] }, compactTokens: 200000, costWarnCNY: 15, maxToolResultChars: 100000, model: 'pro', apiKey: 'sk-secret123' } as any,
      provenance: { model: 'project', apiKey: 'user', permissions: 'merged' },
      scopes: [
        { scope: 'user', path: '/home/u/.deepcode/settings.json', present: true, demoted: false, stripped: [] },
        { scope: 'project', path: '/proj/.deepcode/settings.json', present: true, demoted: false, stripped: ['apiKey', 'hooks', 'permissions.allow'] },
        { scope: 'local', path: '/proj/.deepcode/settings.local.json', present: false, demoted: false, stripped: [] },
      ],
    })
    expect(out).toContain('model')
    expect(out).toContain('[project]')
    expect(out).not.toContain('sk-secret123')      // 打码
    expect(out).toContain('已忽略')                  // 剥离警告
    expect(out).toContain('apiKey')                 // 列出被剥字段
    expect(out).toContain('/proj/.deepcode/settings.json')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/configReport.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

```ts
// src/configReport.ts
import type { LayeredResult } from './settingsLayers.js'

const SENSITIVE = new Set(['apiKey'])
function maskValue(key: string, v: unknown): string {
  if (SENSITIVE.has(key) && typeof v === 'string' && v) return v.slice(0, 4) + '…(已打码)'
  if (Array.isArray(v)) return `${v.length} 条`
  if (v && typeof v === 'object') return JSON.stringify(v)
  return String(v)
}

export function formatConfigReport(r: LayeredResult): string {
  const lines: string[] = ['配置（合并值 · 来源）：']
  for (const [k, v] of Object.entries(r.settings)) {
    if (v === undefined) continue
    const src = r.provenance[k] ?? 'default'
    lines.push(`  ${k}: ${maskValue(k, v)}  [${src}]`)
  }
  // 被剥离的 project / 降级 local 警告
  for (const s of r.scopes) {
    if (s.stripped.length) {
      const tag = s.demoted ? `${s.scope}(git-tracked 已降级)` : s.scope
      lines.push(`  ⚠ ${tag} 的 ${s.stripped.join('/')} 已忽略（不可信来源）`)
    }
  }
  lines.push('加载的文件：')
  for (const s of r.scopes) {
    lines.push(`  [${s.scope}] ${s.path} ${s.present ? (s.demoted ? '(已加载·降级)' : '(已加载)') : '(缺失)'}`)
  }
  return lines.join('\n')
}
```

`src/tui/useChat.ts` 斜杠分发加 `/config`（在 `/permissions` 附近）：

```ts
    if (line === '/config') {
      const { loadLayeredSettings } = await import('../settingsLayers.js')
      const { formatConfigReport } = await import('../configReport.js')
      notice('info', formatConfigReport(loadLayeredSettings(cwd, flagSettingsPath)))
      // 继续（return / 不送模型，按现有斜杠命令惯例）
    }
```

> 按 useChat 现有斜杠命令模式接（同步 import 或静态 import 皆可；与 `/permissions` 一致即可）。补全列表 `suggest.ts` 加 `/config`（若有命令清单）。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/configReport.test.ts && npx tsc --noEmit && npm run build`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/configReport.ts src/tui/useChat.ts test/configReport.test.ts
git commit -m "feat(config): /config 只读源追溯（来源标注/剥离警告/apiKey 打码/文件列表）"
```

---

## 最终验收（subagent-driven 末尾）

- [ ] 全量 `npm test` + `npx tsc --noEmit` + `npm run build` 全绿。
- [ ] **opus 全量终审**：信任边界（嵌套 allow/sources 剥离无绕过）、SSRF（isBlockedAddress 网段完整、mapped-v6 无绕过、代理跳过/NO_PROXY 语义、redirect:error）、写路径（grep 全仓 `saveSettings`/`save*Settings` 无洗白残留、saveRule/permissions rm/saveApiKey 全走 raw user）、provenance 准确。**实跑对抗用例**：构造恶意 project settings（allow rm、hooks、apiKey、baseURL、tracked local）验证全部失效。
- [ ] 回归确认：现有单文件 `~/.deepcode/settings.json` 行为不变（仅 user scope 存在时合并结果 == 旧 loadSettings）。
- [ ] 纯逻辑免真机冒烟（无 TUI 新组件；`/config` 走 notice 文本，沿用现有渲染）。
- [ ] `finishing-a-development-branch` 合 main。

## Self-Review（writing-plans 自查）

- **Spec 覆盖**：§1 scope→Task6/10；§2 合并→Task5；§3.1 危险字段→Task3；§3.2 git 闸门→Task4/6；§4 SSRF→Task1/9；§5 字段→Task2；§6 /config→Task11；§7 写路径→Task7/8；§9 非目标未排任务（正确）；§10 测试→分散各 Task + 最终验收。无遗漏。
- **占位符扫描**：Task9 集成测试与 Task11 suggest 补全标注「subagent 按现有惯例补」——非 TBD，是依现存代码模式补 mock 的明确指示（execHttpHook 测试入口取决于其是否导出，故给两条路径）。其余步骤均含完整代码。
- **类型一致**：`SettingScope`/`ScopePartial`/`LoadedScope`/`LayeredResult`/`loadLayeredSettings`/`stripUntrustedScope`/`mergeScopePartials`/`isGitTracked` 跨 Task 命名一致；`loadRawUserSettings`/`saveRawUserSettings`/`addUserAllowRule`/`removeUserAllowRule`/`listUserAllowRules` 一致；`urlMatchesPattern`/`ssrfGuardedLookup`/`shouldBypassProxy` 一致。
- **已知留意**：config.ts ↔ settingsLayers.ts 循环 import（运行时调用安全，Task7 注明验证）；Task9 集成测试需确认 `execHttpHook` 是否导出以决定测试入口（给了 runHooks 路径兜底）。
