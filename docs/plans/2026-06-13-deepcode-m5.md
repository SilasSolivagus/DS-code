# deepcode M5（好看好用）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把交互层从裸 readline 升级为 ink TUI——完全对标 CC 的手感（输入框、斜杠/文件自动补全、markdown 渲染、工具行 spinner、diff 权限弹窗、状态行），同时做出 DeepSeek 辨识度（缓存命中率/tok/s 常驻、reasoning 思考块、鲸鱼蓝主题）；顺带落地与输入层天然耦合的 M5 候选：`!` 直跑、@文件引用、模型/baseURL 可配。

**Architecture:** loop.ts/session/compact/headless **零改动**（loop 本就是 async generator yield 事件，天然适配 React 状态流）。新增 `src/tui/`：`useChat` hook 把 repl.ts 的状态机改造成 reducer + 事件桥，组件层纯展示。旧 repl.ts **冻结保留**作 `--plain` 逃生舱（不再演进，M6 评估删除）；非 TTY 无 `-p` 时读 stdin 全文转 headless（CC 同款行为）。bench/管道兼容性由 headless 路径保证，TUI 不需要兼容管道。

**Tech Stack:** ink 5 + React 18（ESM，Node 22 原生支持）、marked（md 词法）+ cli-highlight（代码高亮）、diff（权限弹窗 diff 预览）、ink-testing-library（组件测试）。其余同前（TypeScript/ESM、vitest、zod）。

**Scope 决策记录:** 差距分析（docs/specs/2026-06-12-cc-gap-analysis.md）原定「明确不抄 TUI」，2026-06-13 用户明确推翻：「完全抄 CC 的 UI 但要能看出 DeepSeek 的特点」。可写 subagent+worktree、WebFetch、/rewind 快照 → **推 M6**。

**前置:** M4 已完结（v0.4.0-m4，116 测试）。本计划读过的关键契约：`LoopEvent`（src/loop.ts:8-12，四种事件）、repl.ts 全部状态机（v0.4.0-m4 版 338 行）、`PermissionContext.ask(toolName, desc) → Promise<Decision>`（src/permissions.ts:7-12）、`Settings`（src/config.ts:6-12）、`createClient`（src/api.ts:87，baseURL 硬编码）。

**项目路径:** `/Users/silas/loop/deepcode`。测试基线 116（23 文件），`npm test` / `npm run typecheck`。

**执行注意:** 12 个任务建议分 3 个执行会话（Task 1-4 基础设施与核心 hook / Task 5-9 组件 / Task 10-12 接线与验收）。每任务双门（规格审查 + 质量审查）。

**执行进度（2026-06-13 会话一，Task 1-11 全部完成）:** ✅ T1 `b306865` ✅ T2 `9a26bf7`+`351b890`（有序列表编号/未知语言 stderr 守卫）✅ T3 `ac8b916`+`f5214d2`（**关键修复**：interrupt 自卫式 resolve pendingAsk 防死锁、seal 悬空块、sessionDir 注入）✅ T4 `413772f`+`cb66720`（**关键修复**：tool_start 先 seal 文本块，保证 Static done 列表纯追加——否则工具行重复+正文丢失）✅ T5 `1538188`+`5a42628`（busy guard、refs 防 stale closure、setVal 统一入口）✅ T6 `db8723b`+`c3e3c54`（@dir/ 全路径匹配）✅ T7 `f8b1e7c`+`e94f7ab`（**安全修复**：预览层剥控制字符防 ANSI 注入、字面量替换防 $& 偏差、空 diff 提示）✅ T8 `578f06d` ✅ T9 `73242b1`+`f7f535d`（valueOverride 复活修复、@引用词边界+读取失败静默化）✅ T10 `278b2d8`（pricing 未知模型返 0）✅ T11 `6abea1b`。测试 116→173，typecheck 干净。bench 回归抽测 3/3（s1/s6/s12）。pty 冒烟过（banner/输入框/状态行渲染、/exit 退出）。**剩 Task 12：15 项视觉清单人工验收（需真终端）→ 功能回归 → 验收文档 → README → tag v0.5.0-m5。下一会话从 Task 12 开始。**

**Task 12 前置补丁批（2026-06-13 用户试玩反馈，已确认切法）：**
- [ ] P1 权限弹窗改 CC 式方向键菜单（复用 SelectList 语义：`❯ 允许 / 总是允许 / 拒绝`，↑↓+Enter，y/n/a 快捷键保留；高危警告行不变）
- [ ] P2 工具结果预览 ANSI 清洗（loop.ts previewOf 或 ToolLine 渲染层剥控制字符——试玩日志证实贪吃蛇全屏程序的 `\x1b[?1049h` 等序列直接灌进画面；diffPreview 的 sanitize 可提出共用）
- [ ] P3 系统提示词两条新增（src/prompt.ts）：①需求有歧义时先一句话确认再动手；②Bash 工具无 tty，curses/交互式程序跑不动、用户无法向子进程输入，此类需求让用户自己开终端。改完跑 bench 抽测 s1/s6/s12 防提示词退化
- 修完三补丁再做 Task 12 人工验收。**M6 已定方向**（用户 2026-06-13 拍板）：安装/首跑向导（npm 全局包 + key 配置 + logo 欢迎页）、AskUserQuestion 工具（TUI 菜单/headless 禁用）、IME 光标定位攻坚、行内编辑（←→）、`!` 交互透传（stdio inherit + ink 挂起）、可写 subagent+worktree、WebFetch、/rewind**ink 组件测试约定**：用 ink-testing-library 的 `render()` + `lastFrame()` 断言渲染输出、`stdin.write()` 模拟按键；测试文件后缀 `.test.tsx`。

---

## DeepSeek 辨识度（设计基准，验收对照）

| 元素 | CC | deepcode M5 |
|---|---|---|
| 主题色 | 橙/棕 accent | **鲸鱼蓝 `#4D6BFE`** accent（边框、提示符、选中态） |
| Banner | `✻ Welcome to Claude Code` | `🐳 deepcode` + 版本 + 模型名 |
| 状态行 | 无常驻 token 信息 | **常驻**：`模型·标志 │ 缓存命中 87% │ $0.0042 │ 38 tok/s`——缓存命中率与实时吞吐是 DeepSeek 核心卖点 |
| 思考流 | 不可见 | `✻ 思考中…` 紫色斜体折叠块（v4 thinking 特色），完成后折叠为一行 |
| 花费 | 不显示 | turn 结束行显示 入/缓存命中/出/累计 $（沿用 M1-M4 习惯） |
| 语言 | 英文 | 全中文 UI |

---

## 文件结构（M5 增量）

| 文件 | 动作 | 职责 |
|---|---|---|
| `package.json` `tsconfig.json` | 修改 | 新依赖；`"jsx": "react-jsx"` |
| `src/tui/theme.ts` | 新建 | 颜色常量（鲸鱼蓝/紫/绿/红/dim）+ spinner 帧 |
| `src/tui/markdown.ts` | 新建 | marked 词法 → ANSI 字符串（标题/粗体/行内码/代码块高亮/列表/表格/引用） |
| `src/tui/useChat.ts` | 新建 | ★ 核心：transcript reducer + runLoop 事件桥 + 斜杠命令语义（repl.ts 状态机的 React 化） |
| `src/tui/components/Banner.tsx` | 新建 | 启动横幅 |
| `src/tui/components/Transcript.tsx` | 新建 | `<Static>` 已完成块 + 进行中动态区（assistant 流/思考块/工具行） |
| `src/tui/components/ToolLine.tsx` | 新建 | ⏺ 工具行：运行中 spinner，完成 ⎿ 预览+耗时 |
| `src/tui/components/InputBox.tsx` | 新建 | 圆角边框输入框：历史↑↓、`\` 续行、Esc/Ctrl+C 语义、placeholder |
| `src/tui/components/Suggestions.tsx` | 新建 | 斜杠命令 + @文件 浮动补全菜单（↑↓ Tab Enter） |
| `src/tui/components/PermissionDialog.tsx` | 新建 | y/n/a 权限弹窗 + Edit/Write diff 预览 + Bash 高危警告 |
| `src/tui/components/StatusLine.tsx` | 新建 | 常驻底部状态行 |
| `src/tui/components/SelectList.tsx` | 新建 | 通用 ↑↓ Enter 列表（/resume 用） |
| `src/tui/App.tsx` | 新建 | 装配：useChat + 全部组件 + 焦点路由 |
| `src/tui/index.tsx` | 新建 | `startTui(opts)` 入口（render + 退出清理） |
| `src/config.ts` | 修改 | Settings 增加 `model?` `baseURL?` |
| `src/api.ts` | 修改 | `createClient` 读 settings baseURL |
| `src/index.ts` | 修改 | 分流：TTY→TUI / `--plain`→旧 repl / 非 TTY stdin→headless |
| `src/repl.ts` | 冻结 | 仅文件头加「已冻结」注释，不动逻辑 |
| `test/tui.*.test.{ts,tsx}` | 新建 | 见各任务 |

---

### Task 1: 依赖、tsconfig、theme

**Files:**
- Modify: `package.json`, `tsconfig.json`
- Create: `src/tui/theme.ts`
- Test: `test/tui.theme.test.ts`

- [ ] **Step 1: 安装依赖**

```bash
cd /Users/silas/loop/deepcode
npm i ink@^5 react@^18 marked@^12 cli-highlight@^2 diff@^5
npm i -D @types/react@^18 @types/diff@^5 ink-testing-library@^4
```

- [ ] **Step 2: tsconfig 加 JSX**

`tsconfig.json` 的 `compilerOptions` 增加（保留现有全部选项）：

```json
    "jsx": "react-jsx"
```

- [ ] **Step 3: 写失败测试**

```typescript
// test/tui.theme.test.ts
import { describe, it, expect } from 'vitest'
import { T, SPINNER_FRAMES } from '../src/tui/theme.js'

describe('theme', () => {
  it('导出 DeepSeek 主题色与 spinner 帧', () => {
    expect(T.accent).toBe('#4D6BFE')
    expect(T.reasoning).toBeTypeOf('string')
    expect(SPINNER_FRAMES.length).toBeGreaterThan(4)
  })
})
```

- [ ] **Step 4: 运行确认失败**

Run: `npx vitest run test/tui.theme.test.ts`
Expected: FAIL —— 模块不存在

- [ ] **Step 5: 实现 `src/tui/theme.ts`**

```typescript
// src/tui/theme.ts
// DeepSeek 主题：鲸鱼蓝 accent 是全 UI 唯一强调色，其余克制（dim 为主）
export const T = {
  accent: '#4D6BFE',   // 鲸鱼蓝：边框、提示符、选中态、banner
  reasoning: '#9B7EDE', // 思考流紫
  ok: '#4ADE80',
  err: '#F87171',
  warn: '#FBBF24',
  dim: 'gray',
} as const

export const SPINNER_FRAMES = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏']
```

- [ ] **Step 6: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.theme.test.ts && npm test && npm run typecheck`
Expected: 全部 PASS（116+1）。注意：新依赖装入后 typecheck 必须干净（@types/react 与 TS6 兼容性在此步暴露）。

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json tsconfig.json src/tui/theme.ts test/tui.theme.test.ts
git commit -m "feat: M5 deps, jsx config, DeepSeek theme"
```

---

### Task 2: markdown.ts —— 终端 ANSI 渲染

**Files:**
- Create: `src/tui/markdown.ts`
- Test: `test/tui.markdown.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// test/tui.markdown.test.ts
import { describe, it, expect } from 'vitest'
import { renderMarkdown } from '../src/tui/markdown.js'

const strip = (s: string) => s.replace(/\x1b\[[0-9;]*m/g, '')

describe('renderMarkdown', () => {
  it('标题加粗并带 § 前缀，正文原样', () => {
    const out = renderMarkdown('## 计划\n正文一句')
    expect(out).toContain('\x1b[1m')          // 粗体 ANSI
    expect(strip(out)).toContain('§ 计划')
    expect(strip(out)).toContain('正文一句')
  })

  it('行内代码与代码块都有样式，代码块保留多行', () => {
    const out = renderMarkdown('用 `npm test` 跑\n```js\nconst a = 1\nconst b = 2\n```')
    expect(strip(out)).toContain('npm test')
    expect(strip(out)).toContain('const a = 1')
    expect(strip(out)).toContain('const b = 2')
    expect(out).toContain('\x1b[')            // 至少有着色
  })

  it('列表渲染为圆点，表格渲染为对齐行', () => {
    const out = strip(renderMarkdown('- 甲\n- 乙\n\n| a | b |\n|---|---|\n| 1 | 2 |'))
    expect(out).toContain('• 甲')
    expect(out).toContain('• 乙')
    expect(out.split('\n').some(l => l.includes('1') && l.includes('2') && l.includes('│'))).toBe(true)
  })

  it('渲染失败时降级返回原文不抛异常', () => {
    // 构造 marked 也能过、但高亮可能罕见失败的输入——这里直接验证 try/catch 兜底路径可用
    expect(renderMarkdown('普通文本')).toContain('普通文本')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.markdown.test.ts`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现 `src/tui/markdown.ts`**

```typescript
// src/tui/markdown.ts
// 把 assistant 的 markdown 渲染成 ANSI 字符串（在 ink 之外渲染，作为 <Text> 内容插入）。
// 原则：终端不是浏览器——标题用粗体+§，代码块用高亮+左竖线，表格用 │ 对齐。渲染失败降级原文。
import { marked, type Token, type Tokens } from 'marked'
import { highlight } from 'cli-highlight'

const B = '\x1b[1m', DIM = '\x1b[2m', IT = '\x1b[3m', R = '\x1b[0m'
const CODE = '\x1b[36m' // 行内码青色

function inline(text: string): string {
  return text
    .replace(/`([^`]+)`/g, `${CODE}$1${R}`)
    .replace(/\*\*([^*]+)\*\*/g, `${B}$1${R}`)
    .replace(/(?<!\*)\*([^*]+)\*(?!\*)/g, `${IT}$1${R}`)
}

function codeBlock(tok: Tokens.Code): string {
  let body = tok.text
  try { body = highlight(tok.text, { language: tok.lang || 'plaintext', ignoreIllegals: true }) } catch { /* 原文降级 */ }
  return body.split('\n').map(l => `${DIM}│${R} ${l}`).join('\n')
}

function table(tok: Tokens.Table): string {
  const rows = [tok.header.map(c => c.text), ...tok.rows.map(r => r.map(c => c.text))]
  const widths = rows[0].map((_, i) => Math.max(...rows.map(r => [...(r[i] ?? '')].length)))
  const line = (cells: string[], bold = false) =>
    cells.map((c, i) => (bold ? B : '') + c.padEnd(widths[i]) + R).join(` ${DIM}│${R} `)
  return [line(rows[0], true), ...rows.slice(1).map(r => line(r))].join('\n')
}

function block(tok: Token): string {
  switch (tok.type) {
    case 'heading': return `${B}§ ${inline(tok.text)}${R}`
    case 'paragraph': return inline(tok.text)
    case 'code': return codeBlock(tok as Tokens.Code)
    case 'list': return (tok as Tokens.List).items.map(i => `• ${inline(i.text.split('\n')[0])}${i.text.includes('\n') ? '\n  ' + inline(i.text.split('\n').slice(1).join('\n  ')) : ''}`).join('\n')
    case 'table': return table(tok as Tokens.Table)
    case 'blockquote': return (tok as Tokens.Blockquote).text.split('\n').map(l => `${DIM}▎${inline(l)}${R}`).join('\n')
    case 'hr': return `${DIM}${'─'.repeat(40)}${R}`
    case 'space': return ''
    default: return 'raw' in tok ? (tok as any).raw.trimEnd() : ''
  }
}

/** markdown → ANSI。任何异常降级返回原文。 */
export function renderMarkdown(md: string): string {
  try {
    const tokens = marked.lexer(md)
    return tokens.map(block).filter(s => s !== '').join('\n\n')
  } catch {
    return md
  }
}
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.markdown.test.ts && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/markdown.ts test/tui.markdown.test.ts && git commit -m "feat: terminal markdown renderer with syntax highlight"
```

---

### Task 3: useChat —— transcript reducer 与 runLoop 事件桥

repl.ts 状态机的 React 化。**分两步测**：3a 纯 reducer（无 React），3b runTurn 桥（mock chatStream 脚本驱动）。

**Files:**
- Create: `src/tui/useChat.ts`
- Test: `test/tui.useChat.test.ts`

- [ ] **Step 1: 写失败测试（reducer 部分）**

```typescript
// test/tui.useChat.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

const script: Array<{ deltas?: any[]; result: any }> = []
vi.mock('../src/api.js', async orig => ({
  ...(await orig() as any),
  chatStream: vi.fn(() =>
    (async function* () {
      const scene = script.shift()
      if (!scene) throw new Error('script exhausted')
      for (const d of scene.deltas ?? []) yield typeof d === 'string' ? { type: 'text', delta: d } : d
      return scene.result
    })(),
  ),
}))

import { transcriptReducer, type TranscriptItem, createChatCore } from '../src/tui/useChat.js'

const usage = { prompt_tokens: 50, completion_tokens: 20, prompt_cache_hit_tokens: 40 }
beforeEach(() => { script.length = 0 })

describe('transcriptReducer', () => {
  it('text delta 追加到进行中 assistant 块；reasoning delta 进思考块', () => {
    let s = transcriptReducer([], { type: 'delta', delta: '你', reasoning: false })
    s = transcriptReducer(s, { type: 'delta', delta: '好', reasoning: false })
    s = transcriptReducer(s, { type: 'delta', delta: '思', reasoning: true })
    expect(s.find(i => i.kind === 'assistant' && !i.done)!.text).toBe('你好')
    expect(s.find(i => i.kind === 'reasoning' && !i.done)!.text).toBe('思')
  })

  it('tool_start 插入运行中工具行，tool_end 标记完成并带耗时', () => {
    let s = transcriptReducer([], { type: 'tool_start', id: 't1', name: 'Read', desc: '{"file_path":"a.ts"}' })
    expect((s.at(-1) as any).running).toBe(true)
    s = transcriptReducer(s, { type: 'tool_end', id: 't1', ok: true, preview: '1  // a', ms: 120 })
    const t = s.find(i => i.kind === 'tool' && (i as any).id === 't1') as any
    expect(t.running).toBe(false)
    expect(t.ms).toBe(120)
  })

  it('turn_end 关闭进行中块并追加 usage 行', () => {
    let s = transcriptReducer([], { type: 'delta', delta: 'x', reasoning: false })
    s = transcriptReducer(s, { type: 'turn_end', usage })
    expect(s.every(i => i.kind !== 'assistant' || i.done)).toBe(true)
    expect(s.at(-1)!.kind).toBe('usage')
  })
})

describe('createChatCore.runTurn', () => {
  it('完整一轮：脚本驱动事件流，状态可被订阅者观察，usage 落 usageLog', async () => {
    script.push(
      { deltas: ['好', '的'], result: { content: '好的', toolCalls: [], usage, finishReason: 'stop' } },
    )
    const frames: TranscriptItem[][] = []
    const core = createChatCore({ client: {} as any, yolo: true, cwd: '/tmp', onState: s => frames.push(s.transcript) })
    await core.send('随便说点')
    const last = frames.at(-1)!
    expect(last.some(i => i.kind === 'user' && i.text === '随便说点')).toBe(true)
    expect(last.some(i => i.kind === 'assistant' && i.done && i.text === '好的')).toBe(true)
    expect(core.state.usageLog.length).toBe(1)
    expect(core.state.cacheHitRate()).toBeCloseTo(40 / 50)
  })

  it('斜杠命令 /cost /clear 走本地语义不发请求', async () => {
    const core = createChatCore({ client: {} as any, yolo: true, cwd: '/tmp', onState: () => {} })
    await core.send('/cost')
    expect(core.state.transcript.some(i => i.kind === 'notice' && i.text.includes('本会话'))).toBe(true)
    await core.send('/clear')
    expect(core.state.transcript.some(i => i.kind === 'notice' && i.text.includes('已清空'))).toBe(true)
    expect((await import('../src/api.js') as any).chatStream.mock.calls.length).toBe(0)
  })

  it('Esc 中断：abort 后 transcript 出现中断 notice', async () => {
    script.push({
      deltas: ['长', '回', '答'],
      result: { content: '长回答', toolCalls: [], usage, finishReason: 'stop' },
    })
    const core = createChatCore({ client: {} as any, yolo: true, cwd: '/tmp', onState: () => {} })
    const p = core.send('说个长的')
    core.interrupt() // chatStream mock 不感知 signal——这里验证 interrupt 不抛、状态机不卡死
    await p
    expect(core.state.busy).toBe(false)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.useChat.test.ts`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现 `src/tui/useChat.ts`**

要点（实现按此契约写全，禁止 TODO）：

```typescript
// src/tui/useChat.ts
// repl.ts 状态机的 React 化。三层：
//  1. transcriptReducer：纯函数，LoopEvent/本地动作 → TranscriptItem[]（可独立测试）
//  2. createChatCore：与 React 无关的会话核心（session 持久化/compact/斜杠命令/usage——逻辑对齐 repl.ts，
//     权限 ask 通过 pendingAsk 状态暴露给 UI，UI 用 resolveAsk 回答）
//  3. useChat：薄 React 包装（useSyncExternalStore 订阅 core.onState）
import { useSyncExternalStore } from 'react'
import fs from 'node:fs'
import type OpenAI from 'openai'
import { runLoop, type LoopDeps, type LoopEvent } from '../loop.js'
import { allTools } from '../tools/index.js'
import { todoWriteTool } from '../tools/todowrite.js'
import { makeAgentTool } from '../tools/agent.js'
import { buildSystemPrompt } from '../prompt.js'
import { loadSettings, saveSettings } from '../config.js'
import { isDangerous, type Decision, type PermissionMode } from '../permissions.js'
import type { ToolContext } from '../tools/types.js'
import { newSession, openSession, listSessions, loadSession, type SessionHandle, type UsageRecord } from '../session.js'
import { costUSD } from '../pricing.js'
import { summarize, rebuildMessages } from '../compact.js'
import { TodoStore } from '../todo.js'
import { loadCustomCommands, expandCommand, INIT_PROMPT, formatContext } from '../commands.js'

export type TranscriptItem =
  | { kind: 'user'; text: string }
  | { kind: 'assistant'; text: string; done: boolean }
  | { kind: 'reasoning'; text: string; done: boolean }
  | { kind: 'tool'; id: string; name: string; desc: string; running: boolean; ok?: boolean; preview?: string; ms?: number }
  | { kind: 'usage'; in: number; hit: number; out: number; totalIn: number; totalOut: number; cost: number }
  | { kind: 'notice'; level: 'info' | 'warn' | 'error'; text: string }
  | { kind: 'bang'; cmd: string; output: string } // ! 直跑结果块（Task 9 填充）

export type ReducerAction =
  | { type: 'delta'; delta: string; reasoning: boolean }
  | LoopEvent & { type: 'tool_start' | 'tool_end' }
  | { type: 'turn_end'; usage: { prompt_tokens: number; completion_tokens: number; prompt_cache_hit_tokens: number }; totals?: { in: number; out: number; cost: number } }
  | { type: 'push'; item: TranscriptItem }
  | { type: 'clear' }

export function transcriptReducer(state: TranscriptItem[], a: ReducerAction): TranscriptItem[] { /* 纯函数实现 */ }

export interface PendingAsk { toolName: string; desc: string; dangerous: boolean; resolve: (d: Decision) => void }

export interface ChatState {
  transcript: TranscriptItem[]
  busy: boolean
  model: string
  thinking: boolean
  permMode: PermissionMode
  pendingAsk: PendingAsk | null
  usageLog: UsageRecord[]
  lastTokPerSec: number | null
  sessionCost(): number
  cacheHitRate(): number  // usageLog 累计 hit/prompt，DeepSeek 状态行核心指标
}

export interface ChatCore {
  state: ChatState
  send(line: string): Promise<void>      // 斜杠命令本地处理；其余走 runLoop（含边界 reminders、落盘、自动 compact——逐项对齐 repl.ts 157-335）
  interrupt(): void                       // Esc
  resolveAsk(d: Decision): void           // 权限弹窗回答
  resumeList(): { file: string; preview: string }[]
  resume(file: string): void
  customCommands: Map<string, string>
}

export function createChatCore(opts: { client: OpenAI; yolo: boolean; cwd: string; continueSession?: boolean; onState: (s: ChatState) => void }): ChatCore { /* 实现 */ }

export function useChat(core: ChatCore): ChatState {
  return useSyncExternalStore(/* core 订阅 */)
}
```

实现要求（实现者必须逐条对齐 repl.ts 的既有语义，这是本任务的规格本体）：
1. `send` 的斜杠命令分支照搬 repl.ts 161-245 全部语义（/help /model /think /accept /cost /resume(改为返回列表给 UI) /compact /clear /context /permissions /init 自定义命令），输出从 console.log 改为 `push notice`。`/model` 在 Task 10 扩展参数化。
2. 用户消息边界 reminders（compacted 一次性 + fileState mtime 检测）照搬 repl.ts 247-267。
3. runLoop 事件循环照搬 repl.ts 293-319：delta→reducer、turn_end→usageLog+session.appendUsage+lastPromptTokens+costWarn；`lastTokPerSec = completion_tokens / (本轮流式耗时秒)`（在 stream 起止打点）。
4. finally 落盘 `messages.slice(lenBefore)` + appendFileState；自动 compact 条件 `!busy 队列空 && lastPromptTokens > settings.compactTokens`（TUI 没有 lineQueue，直接判 busy）。
5. 权限 ask：`ask = (toolName, desc) => new Promise(res => setState({ pendingAsk: { toolName, desc, dangerous: isDangerous(desc), resolve: res } }))`，resolveAsk 调用后清空 pendingAsk。
6. restoreSession/newSession/--continue 照搬 repl.ts 47-82。
7. 所有 setState 后同步调 `onState`（订阅桥）。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.useChat.test.ts && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/useChat.ts test/tui.useChat.test.ts && git commit -m "feat: chat core — transcript reducer and runLoop bridge"
```

---

### Task 4: Transcript 展示组件（Static + 动态区 + ToolLine spinner）

**Files:**
- Create: `src/tui/components/Transcript.tsx`, `src/tui/components/ToolLine.tsx`
- Test: `test/tui.transcript.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.transcript.test.tsx
import { describe, it, expect } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'
import { Transcript } from '../src/tui/components/Transcript.js'
import type { TranscriptItem } from '../src/tui/useChat.js'

describe('Transcript', () => {
  it('user 块带 › 前缀，完成的 assistant 块走 markdown 渲染', () => {
    const items: TranscriptItem[] = [
      { kind: 'user', text: '你好' },
      { kind: 'assistant', text: '**重点** 内容', done: true },
    ]
    const { lastFrame } = render(<Transcript items={items} />)
    expect(lastFrame()).toContain('› 你好')
    expect(lastFrame()).toContain('重点')   // markdown 渲染后无 ** 字面量
    expect(lastFrame()).not.toContain('**重点**')
  })

  it('运行中工具行显示 spinner 字符，完成后显示 ⎿ 预览与耗时', () => {
    const running: TranscriptItem[] = [{ kind: 'tool', id: 't', name: 'Read', desc: '{"f":1}', running: true }]
    const f1 = render(<Transcript items={running} />).lastFrame()!
    expect(f1).toContain('Read')
    expect(/[⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏]/.test(f1)).toBe(true)
    const done: TranscriptItem[] = [{ kind: 'tool', id: 't', name: 'Read', desc: '', running: false, ok: true, preview: '42 行', ms: 230 }]
    const f2 = render(<Transcript items={done} />).lastFrame()!
    expect(f2).toContain('⎿ 42 行')
    expect(f2).toContain('0.2s')
  })

  it('进行中 reasoning 块带思考前缀，完成后折叠为一行', () => {
    const live: TranscriptItem[] = [{ kind: 'reasoning', text: '第一行\n第二行\n第三行', done: false }]
    expect(render(<Transcript items={live} />).lastFrame()).toContain('✻ 思考中')
    const done: TranscriptItem[] = [{ kind: 'reasoning', text: '第一行\n第二行', done: true }]
    const f = render(<Transcript items={done} />).lastFrame()!
    expect(f).toContain('✻ 已思考')
    expect(f).not.toContain('第二行')   // 折叠
  })

  it('usage 行渲染入/缓存/出/累计', () => {
    const items: TranscriptItem[] = [{ kind: 'usage', in: 100, hit: 80, out: 9, totalIn: 100, totalOut: 9, cost: 0.0001 }]
    const f = render(<Transcript items={items} />).lastFrame()!
    expect(f).toContain('100')
    expect(f).toContain('80')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.transcript.test.tsx`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现**

`ToolLine.tsx`：spinner 用 `useState(0)` + `useEffect(setInterval 80ms)` 轮换 `SPINNER_FRAMES`，仅 `running` 时挂 interval。完成态：`<Text color={ok ? T.ok : T.err}>{'  ⎿ '}{preview}（{(ms/1000).toFixed(1)}s）</Text>`。运行态：`<Text color={T.accent}>{frame} {name}({desc.slice(0,80)})</Text>`。

`Transcript.tsx`：`<Static items={doneItems}>` 渲染已完成块（ink Static——只渲染一次，是流式 CLI 的正确姿势，CC 同款），动态区渲染未完成块（进行中 assistant 文本直接 `<Text>`，done 后才走 `renderMarkdown`；进行中 reasoning 显示 `✻ 思考中…` + 最近 3 行紫色斜体，done 后折叠为 `✻ 已思考（N 行）`）。已完成/进行中的分界：`item.done === false || running === true` 的项进动态区。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.transcript.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/components/Transcript.tsx src/tui/components/ToolLine.tsx test/tui.transcript.test.tsx
git commit -m "feat: transcript display with static blocks, tool spinner, reasoning fold"
```

---

### Task 5: InputBox —— CC 手感输入框

**Files:**
- Create: `src/tui/components/InputBox.tsx`
- Test: `test/tui.input.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.input.test.tsx
import { describe, it, expect, vi } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'
import { InputBox } from '../src/tui/components/InputBox.js'

const noop = () => {}

describe('InputBox', () => {
  it('空闲时显示 placeholder，输入后显示内容与光标', () => {
    const r = render(<InputBox onSubmit={noop} onInterrupt={noop} history={[]} busy={false} />)
    expect(r.lastFrame()).toContain('随便问点什么')
    r.stdin.write('你好')
    expect(r.lastFrame()).toContain('你好')
  })

  it('Enter 提交并清空；行尾反斜杠续行不提交', () => {
    const onSubmit = vi.fn()
    const r = render(<InputBox onSubmit={onSubmit} onInterrupt={noop} history={[]} busy={false} />)
    r.stdin.write('第一行\\')
    r.stdin.write('\r')
    expect(onSubmit).not.toHaveBeenCalled()
    r.stdin.write('第二行')
    r.stdin.write('\r')
    expect(onSubmit).toHaveBeenCalledWith('第一行\n第二行')
  })

  it('↑↓ 翻历史', () => {
    const r = render(<InputBox onSubmit={noop} onInterrupt={noop} history={['旧命令A', '旧命令B']} busy={false} />)
    r.stdin.write('\x1b[A') // ↑
    expect(r.lastFrame()).toContain('旧命令B')
    r.stdin.write('\x1b[A')
    expect(r.lastFrame()).toContain('旧命令A')
    r.stdin.write('\x1b[B') // ↓
    expect(r.lastFrame()).toContain('旧命令B')
  })

  it('busy 时 Esc 触发 onInterrupt；空闲时 Esc 清空输入', () => {
    const onInterrupt = vi.fn()
    const r = render(<InputBox onSubmit={noop} onInterrupt={onInterrupt} history={[]} busy={true} />)
    r.stdin.write('\x1b') // Esc
    expect(onInterrupt).toHaveBeenCalled()
    const r2 = render(<InputBox onSubmit={noop} onInterrupt={noop} history={[]} busy={false} />)
    r2.stdin.write('abc')
    r2.stdin.write('\x1b')
    expect(r2.lastFrame()).not.toContain('abc')
  })

  it('onChange 上报当前值（供补全菜单消费）', () => {
    const onChange = vi.fn()
    const r = render(<InputBox onSubmit={noop} onInterrupt={noop} onChange={onChange} history={[]} busy={false} />)
    r.stdin.write('/mo')
    expect(onChange).toHaveBeenLastCalledWith('/mo')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.input.test.tsx`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现 `src/tui/components/InputBox.tsx`**

```tsx
// src/tui/components/InputBox.tsx
// CC 手感输入框：圆角蓝边框、placeholder、历史↑↓、行尾 \ 续行、Esc 双语义、busy 态提示。
// 不用 ink-text-input：自管 value/cursor，否则历史与续行语义插不进去。
import React, { useState } from 'react'
import { Box, Text, useInput } from 'ink'
import { T } from '../theme.js'

export function InputBox(props: {
  onSubmit: (text: string) => void
  onInterrupt: () => void
  onChange?: (value: string) => void
  /** 补全菜单可见时，↑↓/Tab/Enter 由菜单接管（App 传入） */
  suggestionsActive?: boolean
  history: string[]
  busy: boolean
}) {
  const [value, setValue] = useState('')
  const [pending, setPending] = useState('')        // \ 续行累积
  const [histIdx, setHistIdx] = useState(-1)        // -1 = 不在历史
  const set = (v: string) => { setValue(v); props.onChange?.(v) }

  useInput((input, key) => {
    if (key.escape) {
      if (props.busy) props.onInterrupt()
      else { set(''); setPending(''); setHistIdx(-1) }
      return
    }
    if (key.return) {
      if (props.suggestionsActive) return            // 菜单接管 Enter
      if (value.endsWith('\\')) { setPending(p => p + value.slice(0, -1) + '\n'); set(''); return }
      const full = pending + value
      if (!full.trim()) return
      setPending(''); set(''); setHistIdx(-1)
      props.onSubmit(full)
      return
    }
    if (key.upArrow || key.downArrow) {
      if (props.suggestionsActive) return            // 菜单接管 ↑↓
      const h = props.history
      if (!h.length) return
      const next = key.upArrow ? Math.min(histIdx + 1, h.length - 1) : Math.max(histIdx - 1, -1)
      setHistIdx(next)
      set(next === -1 ? '' : h[h.length - 1 - next])
      return
    }
    if (key.backspace || key.delete) { set(value.slice(0, -1)); return }
    if (key.ctrl || key.meta || key.tab) return      // tab 留给菜单
    if (input) set(value + input)
  })

  return (
    <Box flexDirection="column">
      {pending !== '' && <Text dimColor>…续行中（{pending.split('\n').length - 1 + 1} 行）</Text>}
      <Box borderStyle="round" borderColor={T.accent} paddingX={1}>
        <Text color={T.accent}>› </Text>
        {value === '' && pending === ''
          ? <Text dimColor>{props.busy ? '生成中… Esc 中断' : '随便问点什么，/ 看命令，@ 引用文件，! 直跑 shell'}</Text>
          : <Text>{value}<Text inverse> </Text></Text>}
      </Box>
    </Box>
  )
}
```

注意：ink-testing-library 的 `stdin.write('\x1b[A')` 会被 ink 解析为 upArrow；`'\x1b'` 单独写入解析为 escape。退格在不同终端是 `\x7f`（key.backspace）——测试里若需要可用 `r.stdin.write('\x7f')`。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.input.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/components/InputBox.tsx test/tui.input.test.tsx && git commit -m "feat: CC-style input box with history, continuation, esc semantics"
```

---

### Task 6: Suggestions —— 斜杠命令 + @文件补全菜单

**Files:**
- Create: `src/tui/components/Suggestions.tsx`, `src/tui/suggest.ts`
- Test: `test/tui.suggest.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.suggest.test.tsx
import { describe, it, expect, vi } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'
import { computeSuggestions, BUILTIN_COMMANDS } from '../src/tui/suggest.js'
import { Suggestions } from '../src/tui/components/Suggestions.js'
import { mkdtempSync, writeFileSync, mkdirSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'

describe('computeSuggestions', () => {
  it('"/" 列出全部内置命令，"/mo" 前缀过滤，自定义命令并入', () => {
    const all = computeSuggestions('/', { cwd: '/tmp', customCommands: new Map([['review', 'x']]) })
    expect(all.map(s => s.value)).toContain('/model')
    expect(all.map(s => s.value)).toContain('/review')
    expect(all.length).toBe(BUILTIN_COMMANDS.length + 1)
    const filtered = computeSuggestions('/mo', { cwd: '/tmp', customCommands: new Map() })
    expect(filtered.map(s => s.value)).toEqual(['/model'])
  })

  it('"@" 后缀按文件名模糊匹配 cwd 下文件', () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'sg-'))
    mkdirSync(path.join(dir, 'src'))
    writeFileSync(path.join(dir, 'src', 'main.ts'), '')
    writeFileSync(path.join(dir, 'readme.md'), '')
    const s = computeSuggestions('看一下 @ma', { cwd: dir, customCommands: new Map() })
    expect(s.some(x => x.value.endsWith('src/main.ts'))).toBe(true)
    expect(s.some(x => x.value.endsWith('readme.md'))).toBe(false)
  })

  it('非 / 非 @ 输入不出菜单', () => {
    expect(computeSuggestions('普通话', { cwd: '/tmp', customCommands: new Map() })).toEqual([])
  })
})

describe('Suggestions 组件', () => {
  it('渲染候选列表并高亮选中项，Tab/Enter 回调补全值', () => {
    const onPick = vi.fn()
    const items = [{ value: '/model', hint: 'flash↔pro' }, { value: '/think', hint: 'thinking 开关' }]
    const r = render(<Suggestions items={items} onPick={onPick} />)
    expect(r.lastFrame()).toContain('/model')
    r.stdin.write('\x1b[B')   // ↓ 选中第二项
    r.stdin.write('\t')       // Tab 补全
    expect(onPick).toHaveBeenCalledWith('/think')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.suggest.test.tsx`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现**

`src/tui/suggest.ts`（纯函数，可测）：

```typescript
// src/tui/suggest.ts
import fs from 'node:fs'
import path from 'node:path'

export interface Suggestion { value: string; hint: string }

export const BUILTIN_COMMANDS: Suggestion[] = [
  { value: '/model', hint: 'flash↔pro 切换（/model <名> 指定）' },
  { value: '/think', hint: 'thinking 模式开关' },
  { value: '/accept', hint: 'acceptEdits 开关' },
  { value: '/cost', hint: '本会话花费明细' },
  { value: '/context', hint: '上下文占比' },
  { value: '/compact', hint: '手动压缩历史' },
  { value: '/clear', hint: '清空对话' },
  { value: '/resume', hint: '恢复历史会话' },
  { value: '/permissions', hint: '权限规则管理' },
  { value: '/init', hint: '生成 CLAUDE.md' },
  { value: '/help', hint: '帮助' },
  { value: '/exit', hint: '退出' },
]

/** 遍历 cwd 下文件（深度≤3，跳过 node_modules/.git，上限 2000 个）做 @ 补全候选源 */
function listFiles(cwd: string, depth = 3): string[] {
  const out: string[] = []
  const walk = (dir: string, d: number) => {
    if (d > depth || out.length > 2000) return
    let entries: fs.Dirent[] = []
    try { entries = fs.readdirSync(dir, { withFileTypes: true }) } catch { return }
    for (const e of entries) {
      if (e.name === 'node_modules' || e.name.startsWith('.git')) continue
      const p = path.join(dir, e.name)
      if (e.isDirectory()) walk(p, d + 1)
      else out.push(path.relative(cwd, p))
    }
  }
  walk(cwd, 0)
  return out
}

export function computeSuggestions(input: string, env: { cwd: string; customCommands: Map<string, string> }): Suggestion[] {
  if (input.startsWith('/') && !input.includes(' ')) {
    const all = [...BUILTIN_COMMANDS, ...[...env.customCommands.keys()].map(n => ({ value: `/${n}`, hint: '自定义命令' }))]
    return all.filter(s => s.value.startsWith(input)).slice(0, 8)
  }
  const at = input.match(/@([\w./-]*)$/)
  if (at) {
    const q = at[1].toLowerCase()
    return listFiles(env.cwd)
      .filter(f => path.basename(f).toLowerCase().includes(q))
      .slice(0, 8)
      .map(f => ({ value: `@${f}`, hint: '' }))
  }
  return []
}
```

`Suggestions.tsx`：useState 选中索引 + useInput（↑↓ 移动、Tab/Enter 调 `onPick(items[idx].value)`）；渲染为 accent 边框小面板，选中行 `backgroundColor={T.accent}`。App 层（Task 9）负责：菜单可见时给 InputBox 传 `suggestionsActive`，onPick 后把输入框值替换为补全结果（@ 补全是替换末尾 `@...` 片段，/ 补全是整行替换）。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.suggest.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/suggest.ts src/tui/components/Suggestions.tsx test/tui.suggest.test.tsx
git commit -m "feat: slash and @file autocomplete"
```

---

### Task 7: PermissionDialog —— diff 预览权限弹窗

**Files:**
- Create: `src/tui/components/PermissionDialog.tsx`, `src/tui/diffPreview.ts`
- Test: `test/tui.permission.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.permission.test.tsx
import { describe, it, expect, vi } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'
import { buildPreview } from '../src/tui/diffPreview.js'
import { PermissionDialog } from '../src/tui/components/PermissionDialog.js'
import { mkdtempSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'

describe('buildPreview', () => {
  it('Edit：对现有文件产出 ±行 diff', () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dp-'))
    const f = path.join(dir, 'a.ts')
    writeFileSync(f, 'const a = 1\nconst b = 2\n')
    const p = buildPreview('Edit', JSON.stringify({ file_path: f, old_string: 'const b = 2', new_string: 'const b = 3' }))
    expect(p.lines.some(l => l.sign === '-' && l.text.includes('const b = 2'))).toBe(true)
    expect(p.lines.some(l => l.sign === '+' && l.text.includes('const b = 3'))).toBe(true)
  })

  it('Write 新文件：全部 + 行；Write 覆盖：与现有内容 diff', () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dp-'))
    const p1 = buildPreview('Write', JSON.stringify({ file_path: path.join(dir, 'new.ts'), content: 'line1\nline2' }))
    expect(p1.lines.every(l => l.sign === '+')).toBe(true)
    const f = path.join(dir, 'old.ts')
    writeFileSync(f, 'keep\ndrop\n')
    const p2 = buildPreview('Write', JSON.stringify({ file_path: f, content: 'keep\nnew\n' }))
    expect(p2.lines.some(l => l.sign === '-' && l.text === 'drop')).toBe(true)
    expect(p2.lines.some(l => l.sign === '+' && l.text === 'new')).toBe(true)
  })

  it('Bash/非法参数：降级为 desc 原文展示，不抛异常', () => {
    const p = buildPreview('Bash', '{"command":"rm -rf /tmp/x"}')
    expect(p.lines.length).toBeGreaterThan(0)
    expect(buildPreview('Edit', '不是json').lines.length).toBeGreaterThan(0)
  })
})

describe('PermissionDialog', () => {
  const base = { toolName: 'Edit', desc: '{"file_path":"/tmp/x","old_string":"a","new_string":"b"}', dangerous: false }
  it('y/n/a 按键回调对应决策', () => {
    const onDecide = vi.fn()
    const r = render(<PermissionDialog ask={{ ...base, resolve: onDecide }} onDecide={onDecide} />)
    r.stdin.write('a')
    expect(onDecide).toHaveBeenCalledWith('always')
  })
  it('高危显示红色警告', () => {
    const r = render(<PermissionDialog ask={{ ...base, toolName: 'Bash', desc: '{"command":"sudo rm -rf /"}', dangerous: true, resolve: () => {} }} onDecide={() => {}} />)
    expect(r.lastFrame()).toContain('高危')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.permission.test.tsx`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现**

`src/tui/diffPreview.ts`：

```typescript
// src/tui/diffPreview.ts
// 权限弹窗的 diff 预览：Edit → old/new 替换后全文 diff；Write → 现有内容 vs 新内容（新文件全 +）。
// 任何解析失败降级为 desc 原文。预览上限 40 行（超出截断并标注）。
import fs from 'node:fs'
import { diffLines } from 'diff'

export interface PreviewLine { sign: '+' | '-' | ' '; text: string }
export interface Preview { title: string; lines: PreviewLine[]; truncated: boolean }

const MAX = 40

function toLines(diff: ReturnType<typeof diffLines>): PreviewLine[] {
  const out: PreviewLine[] = []
  for (const part of diff) {
    const sign = part.added ? '+' : part.removed ? '-' : ' '
    for (const l of part.value.replace(/\n$/, '').split('\n')) {
      if (sign === ' ' && out.length && out[out.length - 1].sign === ' ') continue // 折叠连续上下文为 1 行
      out.push({ sign, text: l })
    }
  }
  return out
}

export function buildPreview(toolName: string, desc: string): Preview {
  try {
    const args = JSON.parse(desc)
    if (toolName === 'Edit' && args.file_path) {
      const cur = fs.readFileSync(args.file_path, 'utf8')
      const next = args.replace_all
        ? cur.replaceAll(args.old_string, args.new_string)
        : cur.replace(args.old_string, args.new_string)
      const lines = toLines(diffLines(cur, next)).filter(l => l.sign !== ' ')
      return { title: `Edit ${args.file_path}`, lines: lines.slice(0, MAX), truncated: lines.length > MAX }
    }
    if (toolName === 'Write' && args.file_path) {
      let cur = ''
      try { cur = fs.readFileSync(args.file_path, 'utf8') } catch { /* 新文件 */ }
      const lines = toLines(diffLines(cur, args.content ?? '')).filter(l => l.sign !== ' ')
      return { title: `Write ${args.file_path}`, lines: lines.slice(0, MAX), truncated: lines.length > MAX }
    }
    if (toolName === 'Bash' && args.command) {
      return { title: 'Bash', lines: String(args.command).split('\n').map(l => ({ sign: ' ' as const, text: l })), truncated: false }
    }
  } catch { /* 降级 */ }
  return { title: toolName, lines: [{ sign: ' ', text: desc.slice(0, 200) }], truncated: false }
}
```

`PermissionDialog.tsx`：accent 边框面板，标题 `允许 {title} ？`，diff 行按 sign 着色（+绿 −红 空格 dim），`dangerous` 时顶部红色 `⚠ 高危操作；always 也只精确放行这一条`；useInput 捕获 `y`/`n`/`a`（及 Enter=y、Esc=n），调 `onDecide('yes'|'no'|'always')`。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.permission.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/diffPreview.ts src/tui/components/PermissionDialog.tsx test/tui.permission.test.tsx
git commit -m "feat: permission dialog with diff preview"
```

---

### Task 8: StatusLine + Banner + SelectList

**Files:**
- Create: `src/tui/components/StatusLine.tsx`, `src/tui/components/Banner.tsx`, `src/tui/components/SelectList.tsx`
- Test: `test/tui.status.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.status.test.tsx
import { describe, it, expect, vi } from 'vitest'
import React from 'react'
import { render } from 'ink-testing-library'
import { StatusLine } from '../src/tui/components/StatusLine.js'
import { Banner } from '../src/tui/components/Banner.js'
import { SelectList } from '../src/tui/components/SelectList.js'

describe('StatusLine', () => {
  it('渲染模型短名、缓存命中率、花费、tok/s', () => {
    const f = render(
      <StatusLine model="deepseek-v4-flash" thinking={false} permMode="default" cacheHitRate={0.87} cost={0.0042} tokPerSec={38} />,
    ).lastFrame()!
    expect(f).toContain('flash')
    expect(f).toContain('87%')
    expect(f).toContain('$0.0042')
    expect(f).toContain('38 tok/s')
  })
  it('thinking/accept 标志与无数据占位', () => {
    const f = render(
      <StatusLine model="deepseek-v4-pro" thinking={true} permMode="acceptEdits" cacheHitRate={0} cost={0} tokPerSec={null} />,
    ).lastFrame()!
    expect(f).toContain('pro')
    expect(f).toContain('think')
    expect(f).toContain('accept')
  })
})

describe('Banner', () => {
  it('显示鲸鱼、名称与模型', () => {
    const f = render(<Banner model="deepseek-v4-flash" yolo={false} version="0.5.0" />).lastFrame()!
    expect(f).toContain('🐳')
    expect(f).toContain('deepcode')
    expect(f).toContain('0.5.0')
  })
})

describe('SelectList', () => {
  it('↑↓ 移动选中，Enter 回调，Esc 取消', () => {
    const onPick = vi.fn(); const onCancel = vi.fn()
    const r = render(<SelectList items={['会话A', '会话B']} onPick={onPick} onCancel={onCancel} />)
    r.stdin.write('\x1b[B')
    r.stdin.write('\r')
    expect(onPick).toHaveBeenCalledWith(1)
    const r2 = render(<SelectList items={['x']} onPick={onPick} onCancel={onCancel} />)
    r2.stdin.write('\x1b')
    expect(onCancel).toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.status.test.tsx`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现**

`StatusLine.tsx`（DeepSeek 辨识度核心——CC 没有这条）：

```tsx
// src/tui/components/StatusLine.tsx
// 常驻底部状态行：模型·标志 │ 缓存命中 │ 花费 │ tok/s。缓存命中率是 DeepSeek 的招牌指标。
import React from 'react'
import { Box, Text } from 'ink'
import { T } from '../theme.js'

export function StatusLine(p: {
  model: string; thinking: boolean; permMode: string
  cacheHitRate: number; cost: number; tokPerSec: number | null
}) {
  const tags = [p.model.includes('pro') ? 'pro' : 'flash']
  if (p.thinking) tags.push('think')
  if (p.permMode === 'acceptEdits') tags.push('accept')
  if (p.permMode === 'yolo') tags.push('yolo')
  return (
    <Box>
      <Text color={T.accent}>{tags.join('·')}</Text>
      <Text dimColor>{' │ 缓存命中 '}{Math.round(p.cacheHitRate * 100)}%</Text>
      <Text dimColor>{' │ $'}{p.cost.toFixed(4)}</Text>
      <Text dimColor>{' │ '}{p.tokPerSec === null ? '— tok/s' : `${Math.round(p.tokPerSec)} tok/s`}</Text>
    </Box>
  )
}
```

`Banner.tsx`：`🐳 deepcode v{version}`（accent 粗体）+ 第二行 dim：`{model}{yolo ? '（yolo）' : ''} · / 命令 · @ 文件 · ! shell · Esc 中断 · Ctrl+C×2 退出`。version 从 package.json 读（`createRequire` 或 fs 读 JSON）。

`SelectList.tsx`：useState idx + useInput（↑↓ Enter Esc），选中行 accent 反色，`onPick(index)`。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.status.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/components/StatusLine.tsx src/tui/components/Banner.tsx src/tui/components/SelectList.tsx test/tui.status.test.tsx
git commit -m "feat: status line with cache-hit and tok/s, banner, select list"
```

---

### Task 9: App 装配 + `!` 直跑 + @引用展开

**Files:**
- Create: `src/tui/App.tsx`, `src/tui/index.tsx`
- Modify: `src/tui/useChat.ts`（bang/atExpand 两个纯函数 + send 接入）
- Test: `test/tui.app.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
// test/tui.app.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'

const script: Array<{ deltas?: any[]; result: any }> = []
vi.mock('../src/api.js', async orig => ({
  ...(await orig() as any),
  chatStream: vi.fn(() =>
    (async function* () {
      const scene = script.shift()
      if (!scene) throw new Error('script exhausted')
      for (const d of scene.deltas ?? []) yield typeof d === 'string' ? { type: 'text', delta: d } : d
      return scene.result
    })(),
  ),
}))

import React from 'react'
import { render } from 'ink-testing-library'
import { App } from '../src/tui/App.js'
import { runBang, expandAtRefs } from '../src/tui/useChat.js'
import { mkdtempSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'

const usage = { prompt_tokens: 10, completion_tokens: 5, prompt_cache_hit_tokens: 0 }
beforeEach(() => { script.length = 0 })

describe('runBang', () => {
  it('执行命令返回输出与退出码，超时/失败不抛', () => {
    const r = runBang('echo hi', '/tmp')
    expect(r.output).toContain('hi')
    expect(r.code).toBe(0)
    expect(runBang('exit 3', '/tmp').code).toBe(3)
  })
})

describe('expandAtRefs', () => {
  it('@路径 展开为文件内容块，缺失文件标注读取失败', () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'at-'))
    writeFileSync(path.join(dir, 'x.ts'), 'export const x = 1')
    const out = expandAtRefs('看看 @x.ts 怎么写的', dir)
    expect(out).toContain('export const x = 1')
    expect(out).toContain('<file path=')
    expect(expandAtRefs('@不存在.ts', dir)).toContain('读取失败')
  })
})

describe('App 集成', () => {
  it('启动渲染 banner+输入框；输入一句话回车后出现回复与 usage 行', async () => {
    script.push({ deltas: ['答'], result: { content: '答', toolCalls: [], usage, finishReason: 'stop' } })
    const r = render(<App client={{} as any} yolo={true} cwd={process.cwd()} />)
    expect(r.lastFrame()).toContain('🐳')
    r.stdin.write('问')
    r.stdin.write('\r')
    await vi.waitFor(() => expect(r.lastFrame()).toContain('答'))
    expect(r.lastFrame()).toContain('缓存命中')
  })

  it('输入 "/" 浮出补全菜单', () => {
    const r = render(<App client={{} as any} yolo={true} cwd={process.cwd()} />)
    r.stdin.write('/')
    expect(r.lastFrame()).toContain('/model')
  })

  it('"!ls" 直跑：结果以 bang 块呈现，不发 API 请求', async () => {
    const r = render(<App client={{} as any} yolo={true} cwd={process.cwd()} />)
    r.stdin.write('!echo bang测试')
    r.stdin.write('\r')
    await vi.waitFor(() => expect(r.lastFrame()).toContain('bang测试'))
    const { chatStream } = await import('../src/api.js') as any
    expect(chatStream.mock.calls.length).toBe(0)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tui.app.test.tsx`
Expected: FAIL —— App/runBang/expandAtRefs 不存在

- [ ] **Step 3: 实现**

`useChat.ts` 追加两个导出（纯函数）：

```typescript
import { execSync } from 'node:child_process'

/** ! 直跑：同步执行（CC 同款体验），30s 超时，stdout+stderr 合并，超 20k 截断 */
export function runBang(cmd: string, cwd: string): { output: string; code: number } {
  try {
    const out = execSync(cmd, { cwd, timeout: 30_000, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] })
    return { output: out.slice(0, 20_000), code: 0 }
  } catch (e: any) {
    const out = `${e.stdout ?? ''}${e.stderr ?? ''}` || String(e.message)
    return { output: out.slice(0, 20_000), code: e.status ?? 1 }
  }
}

/** @path 展开为 <file> 块（≤400 行/文件；bang 结果与 @ 内容都会进入 user 消息，模型可直接引用） */
export function expandAtRefs(text: string, cwd: string): string {
  return text.replace(/@([\w./-]+)/g, (m, p) => {
    try {
      const lines = fs.readFileSync(path.resolve(cwd, p), 'utf8').split('\n')
      const body = lines.slice(0, 400).join('\n') + (lines.length > 400 ? '\n…（截断）' : '')
      return `\n<file path="${p}">\n${body}\n</file>\n`
    } catch { return `${m}（读取失败：文件不存在或不可读）` }
  })
}
```

`send()` 接入：`!` 开头 → runBang，结果 push 为 `bang` transcript 块，**同时**作为 user 消息入上下文（`<bash-input>{cmd}</bash-input>\n<bash-output>\n{output}\n</bash-output>`，不触发模型回复——CC 同款：只入上下文，下次提问时模型可见）；其余输入先 `expandAtRefs` 再走原流程。

`App.tsx` 装配：

```tsx
// src/tui/App.tsx
import React, { useMemo, useState } from 'react'
import { Box, useApp, useInput } from 'ink'
import { createChatCore, useChat } from './useChat.js'
import { computeSuggestions } from './suggest.js'
import { Banner } from './components/Banner.js'
import { Transcript } from './components/Transcript.js'
import { InputBox } from './components/InputBox.js'
import { Suggestions } from './components/Suggestions.js'
import { PermissionDialog } from './components/PermissionDialog.js'
import { StatusLine } from './components/StatusLine.js'
import { SelectList } from './components/SelectList.js'

export function App(props: { client: any; yolo: boolean; cwd: string; continueSession?: boolean }) {
  const { exit } = useApp()
  const core = useMemo(() => createChatCore({ ...props, onState: () => {} }), [])
  const state = useChat(core)
  const [draft, setDraft] = useState('')
  const [resumeMode, setResumeMode] = useState(false)
  const suggestions = useMemo(
    () => computeSuggestions(draft, { cwd: props.cwd, customCommands: core.customCommands }),
    [draft],
  )
  // Ctrl+C 两次退出（App 层统一管理）
  const [lastSigint, setLastSigint] = useState(0)
  useInput((input, key) => {
    if (key.ctrl && input === 'c') {
      const now = Date.now()
      if (now - lastSigint < 2000) exit()
      else setLastSigint(now)
    }
  })
  const submit = (text: string) => {
    if (text === '/exit') return exit()
    if (text === '/resume') return setResumeMode(true)
    void core.send(text)
  }
  return (
    <Box flexDirection="column">
      <Banner model={state.model} yolo={props.yolo} version="0.5.0" />
      <Transcript items={state.transcript} />
      {state.pendingAsk
        ? <PermissionDialog ask={state.pendingAsk} onDecide={d => core.resolveAsk(d)} />
        : resumeMode
          ? <SelectList items={core.resumeList().map(s => s.preview)} onPick={i => { core.resume(core.resumeList()[i].file); setResumeMode(false) }} onCancel={() => setResumeMode(false)} />
          : <>
              {suggestions.length > 0 && <Suggestions items={suggestions} onPick={v => setDraft(v.startsWith('@') ? draft.replace(/@[\w./-]*$/, v) : v)} />}
              <InputBox onSubmit={submit} onInterrupt={() => core.interrupt()} onChange={setDraft}
                        suggestionsActive={suggestions.length > 0} history={state.transcript.filter(i => i.kind === 'user').map(i => (i as any).text)} busy={state.busy} />
            </>}
      <StatusLine model={state.model} thinking={state.thinking} permMode={state.permMode}
                  cacheHitRate={state.cacheHitRate()} cost={state.sessionCost()} tokPerSec={state.lastTokPerSec} />
    </Box>
  )
}
```

（onPick 的补全值回填要让 InputBox 受控——实现时把 InputBox 的 value 提升到 App 或暴露 setValue ref，实现者任选一种并保持测试通过。）

`src/tui/index.tsx`：

```tsx
// src/tui/index.tsx
import React from 'react'
import { render } from 'ink'
import { App } from './App.js'
import type OpenAI from 'openai'

export async function startTui(opts: { client: OpenAI; yolo: boolean; continueSession?: boolean }): Promise<void> {
  const { waitUntilExit } = render(
    <App client={opts.client} yolo={opts.yolo} cwd={process.cwd()} continueSession={opts.continueSession} />,
    { exitOnCtrlC: false }, // Ctrl+C 语义由 App 自管（双击退出）
  )
  await waitUntilExit()
}
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tui.app.test.tsx && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tui/App.tsx src/tui/index.tsx src/tui/useChat.ts test/tui.app.test.tsx
git commit -m "feat: TUI app assembly, bang commands, @file refs"
```

---

### Task 10: 模型/baseURL 可配 + /model 参数化

**Files:**
- Modify: `src/config.ts`, `src/api.ts`, `src/tui/useChat.ts`（/model 分支）
- Test: `test/config.test.ts`（追加）, `test/api.test.ts`（追加）

- [ ] **Step 1: 追加失败测试**

```typescript
// test/config.test.ts 追加
  it('settings 支持 model/baseURL 自定义，缺省为 undefined', () => {
    const s = loadSettings()
    expect('model' in s).toBe(true)
    expect('baseURL' in s).toBe(true)
  })
```

```typescript
// test/api.test.ts 追加（沿用该文件现有 env mock 设施；若无则新建 describe）
  it('createClient 读 settings.baseURL，缺省 api.deepseek.com', () => {
    // 通过环境变量注入临时 HOME 指向空目录 → settings 缺省
    const c = createClient()
    expect((c as any).baseURL).toContain('api.deepseek.com')
  })
```

（baseURL 自定义路径的验证在 Step 3 用临时 settings 文件 + 子进程冒烟做，单测只锁缺省值不锁文件注入——loadSettings 读固定路径，测试环境不写真实 HOME。）

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/config.test.ts test/api.test.ts`
Expected: FAIL —— Settings 无 model/baseURL 字段

- [ ] **Step 3: 实现**

`config.ts`：Settings 增加 `model?: string`（启动默认模型）与 `baseURL?: string`；loadSettings 返回 `model: raw?.model`、`baseURL: raw?.baseURL`（不设默认，undefined = 内置缺省）。

`api.ts` `createClient`：`baseURL: loadSettings().baseURL ?? 'https://api.deepseek.com'`。

`useChat.ts` 启动模型：`model = settings.model ?? 'deepseek-v4-flash'`；`/model` 分支扩展：`/model` 无参 = flash↔pro 轮换（现状）；`/model <名>` = 直接设为该名（任意字符串，配合自定义 baseURL 可接任何 OpenAI 兼容端点），notice 提示 `已切换到 <名>（非 deepseek 系列计价按 0 估算）`。pricing.costUSD 对未知模型返回 0 的现状行为保持（读 src/pricing.ts 确认，若未知模型会抛错则加兜底）。

冒烟（真实 key）：

```bash
echo '{"model":"deepseek-v4-pro"}' > /tmp/dc-smoke-settings.json
# 临时把 settings 换成上述内容（备份现有的），启动 TUI 看 banner 显示 pro，然后还原
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/config.ts src/api.ts src/tui/useChat.ts test/config.test.ts test/api.test.ts
git commit -m "feat: configurable model and baseURL, parameterized /model"
```

---

### Task 11: index.ts 分流 + repl 冻结标注

**Files:**
- Modify: `src/index.ts`, `src/repl.ts`（仅文件头注释）

- [ ] **Step 1: 实现分流**

`src/index.ts` 整体替换为：

```typescript
// src/index.ts
import { createClient } from './api.js'

const argv = process.argv
const yolo = argv.includes('--yolo')
const continueSession = argv.includes('--continue') || argv.includes('-c')
const plain = argv.includes('--plain')
const pIdx = argv.indexOf('-p')

try {
  const client = createClient()
  if (pIdx !== -1) {
    const prompt = argv[pIdx + 1]
    if (!prompt || prompt.startsWith('-')) throw new Error('用法：deepcode -p "<任务>" [--json] [--yolo]')
    const { runHeadless } = await import('./headless.js')
    const r = await runHeadless({ client, prompt, yolo })
    if (argv.includes('--json')) {
      console.log(JSON.stringify({ text: r.text, status: r.status, turns: r.turns, usage: r.usage, costUSD: r.costUSD }))
    } else {
      console.log(r.text)
    }
    process.exitCode = r.status === 'done' ? 0 : 1
  } else if (!process.stdin.isTTY && !plain) {
    // 管道喂入无 -p：读 stdin 全文当 prompt 走 headless（CC 同款行为）
    const chunks: Buffer[] = []
    for await (const c of process.stdin) chunks.push(c)
    const prompt = Buffer.concat(chunks).toString('utf8').trim()
    if (!prompt) throw new Error('stdin 为空。交互模式请直接运行 deepcode，或用 -p "<任务>"')
    const { runHeadless } = await import('./headless.js')
    const r = await runHeadless({ client, prompt, yolo })
    console.log(r.text)
    process.exitCode = r.status === 'done' ? 0 : 1
  } else if (plain) {
    const { startRepl } = await import('./repl.js')
    await startRepl({ client, yolo, continueSession })
    process.exit(0) // readline 持有 event loop；需显式退出
  } else {
    const { startTui } = await import('./tui/index.js')
    await startTui({ client, yolo, continueSession })
    process.exit(0) // ink 卸载后 stdin raw 监听可能残留；显式退出兜底
  }
} catch (e: any) {
  console.error(e?.message ?? e)
  process.exitCode = 1
}
```

`src/repl.ts` 文件头第 1 行注释后追加：

```typescript
// 【已冻结】M5 起交互层由 src/tui/ 接管，本文件仅作 --plain 逃生舱保留，不再演进。M6 评估删除。
```

**破坏性确认（明示）**：M4 及之前「管道喂多行命令进 REPL」的用法（`printf '/help\n/exit' | deepcode`）在 M5 后走 headless 单 prompt 语义，行为改变。bench 自 M4 起已用 `-p --json`，不受影响；需要旧行为时 `--plain` 完全保留。

- [ ] **Step 2: 冒烟验证三条路径**

```bash
# headless 路径（不变）
npx tsx src/index.ts -p "只回答 ok" --json | python3 -c "import json,sys; print(json.load(sys.stdin)['status'])"
# 期望: done

# 管道转 headless（新行为）
echo "只回答 ok" | npx tsx src/index.ts
# 期望: 输出含 ok，退出码 0

# --plain 旧 repl（不变）
printf '/help\n/exit\n' | DEEPSEEK_API_KEY=sk-dummy npx tsx src/index.ts --plain
# 期望: 12 项命令列表，退出码 0
```

（TUI 路径需要真 TTY，归 Task 12 真机验收。）

- [ ] **Step 3: 全量回归**

Run: `npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 4: Commit**

```bash
git add src/index.ts src/repl.ts && git commit -m "feat: entry routing — TUI default, --plain fallback, stdin-to-headless"
```

---

### Task 12: M5 验收

- [ ] **Step 1: 视觉/手感清单（真 TTY 人工验收，逐项截图）**

新开终端 `cd ~/testDS && npx tsx /Users/silas/loop/deepcode/src/index.ts`，逐项核对：

| # | 项 | 期望 |
|---|---|---|
| 1 | Banner | 🐳 deepcode + 版本 + 模型，鲸鱼蓝 |
| 2 | 输入框 | 圆角蓝边框、placeholder、光标块 |
| 3 | `/` 菜单 | 浮出 12 内置 + 自定义命令，↑↓ Tab 补全 |
| 4 | `@` 菜单 | 文件名模糊匹配补全 |
| 5 | 流式回复 | markdown 渲染（标题/代码高亮/表格） |
| 6 | 工具行 | 运行中 spinner → ⎿ 预览+耗时 |
| 7 | `/think` 后提问 | ✻ 思考中…紫色流 → 完成折叠一行 |
| 8 | Edit 权限弹窗 | ±diff 着色预览，y/n/a |
| 9 | 高危 Bash | 红色 ⚠ 警告 |
| 10 | 状态行 | 缓存命中率随轮次上升、$ 累计、tok/s 刷新 |
| 11 | Esc | 流式中断；空闲清空输入 |
| 12 | Ctrl+C×2 | 提示后退出 |
| 13 | `!ls` | bang 块直显，下一问模型可引用结果 |
| 14 | 历史 ↑ | 调出上一条输入 |
| 15 | `/resume` | 列表选择恢复，上下文连续 |

- [ ] **Step 2: 功能回归（既有能力在 TUI 下不退化）**

交互验证：`/compact` 后 `/context` 数值下降、追问连续；TodoWrite 长任务 todo 渲染正常；`/permissions` 增删规则生效。

- [ ] **Step 3: bench 快速回归（headless 路径未动，抽测不掉分）**

```bash
cd bench && npx tsx run.ts --runs 1 --track deepcode --scenario s1-assembler,s6-fix-bug,s12-injection 2>&1 | tail -15
```

Expected: 3/3 通过（若掉分先查 Task 10 的 createClient 改动是否影响 headless）。

- [ ] **Step 4: 写验收文档 + 收尾**

写 `docs/specs/m5-acceptance.md`：视觉清单逐项结果（贴关键截图说明）、三条入口路径冒烟、bench 抽测、破坏性变更声明（管道语义）、带入 M6 项。
README：M5 行 `- [x]` + `（v0.5.0-m5）`；「使用」节补 TUI 说明与 `--plain`。

```bash
npm test && npm run typecheck
git add -A && git commit -m "docs: M5 acceptance results"
git tag v0.5.0-m5 && git push && git push --tags
```

---

## 自查记录

- **需求覆盖**：完全抄 CC 手感 → 输入框/补全/菜单（Task 5/6）、markdown+高亮（Task 2/4）、spinner 工具行（Task 4）、diff 权限弹窗（Task 7）、`!` 直跑与 @引用（Task 9）、Static 流式架构（Task 4，CC 同款）；DeepSeek 辨识度 → 主题（Task 1）、思考块（Task 4）、缓存命中率/tok/s 状态行（Task 8）、鲸鱼 banner（Task 8）；M5 候选合并 → diff 预览（Task 7）、模型/baseURL 可配（Task 10）、`!`+@（Task 9）；推 M6 → 可写 subagent+worktree、WebFetch、/rewind（计划头部声明）。
- **占位符**：Task 3 的 createChatCore 给的是契约+逐条对齐 repl.ts 行号的实现要求而非全量代码（repl.ts 状态机 200 行的 React 化重排，全文照抄反而锁死实现者的合理布局）；其余任务代码全量给出。Task 3 实现者必须逐条核对 repl.ts 对应行号语义，规格审查按这 7 条要求逐条验。
- **类型一致性**：`TranscriptItem`/`ReducerAction`/`ChatState`/`ChatCore`/`PendingAsk`（Task 3 定义，Task 4/7/9 消费）；`Suggestion`（Task 6 定义，Task 9 消费）；`Preview/PreviewLine`（Task 7 内自洽）；`runBang/expandAtRefs`（Task 9 定义与消费）；`Settings.model/baseURL`（Task 10 定义，useChat/api 消费）；`startTui(opts)`（Task 9 定义，Task 11 调用）✓
- **顺序依赖**：Task 1→全部；Task 3 依赖 1；Task 4 依赖 2/3；Task 5/6/7/8 依赖 1（相互独立可乱序）；Task 9 依赖 3-8；Task 10 依赖 3；Task 11 依赖 9/10；Task 12 最后。
- **已知接受的权衡**：repl.ts 与 useChat 双份状态机逻辑（旧版冻结不演进，M6 评估删除）；管道喂 REPL 语义变更（headless 接管，--plain 保底）；ink 渲染下 vitest 组件测试覆盖不了真实终端宽度/重绘细节（Task 12 人工清单兜底）；`!` 直跑用 execSync 阻塞 UI 至多 30s（v1 接受，与 CC 行为一致级别）；@ 补全文件遍历深度 3/上限 2000（大仓库可能漏文件，提示用户直接写路径）。
