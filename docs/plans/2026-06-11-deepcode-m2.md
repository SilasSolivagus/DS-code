# deepcode M2（能干活）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** deepcode 能改代码：Edit/Write 工具 + fileState 强制 read-before-edit + acceptEdits 模式，并消化 M1 验收与首轮 bench 攒下的全部小项；验收 = 自主修复一个真 bug + 第二轮 bench。

**Architecture:** 在 M1 的工具系统上新增两个写工具（复用 `ToolContext.fileState` 做 read-before-edit 闸门）；权限层新增高危命令识别；API 层把 reasoning 流事件化以支持暗色渲染；bench 新增"修 bug"型场景（按 verify 命令判分，不再用关键词）。

**Tech Stack:** 同 M1（TypeScript/ESM、zod、vitest）；bench fixture 用 `node --test` 零依赖。

**前置输入:** `docs/specs/2026-06-11-deepcode-harness-design.md` §2/§7、`docs/specs/m1-acceptance.md` 待调清单、`bench/results/2026-06-11-m1.md` 解读。
**项目路径:** `/Users/silas/loop/deepcode`（所有命令在此目录运行，另有说明除外）

---

## 文件结构（M2 增量）

| 文件 | 动作 | 职责 |
|---|---|---|
| `src/tools/edit.ts` | 新建 | Edit 工具 + `checkFileState`（read-before-edit 闸门，Write 复用） |
| `src/tools/write.ts` | 新建 | Write 工具（整文件写，覆盖需先 Read，新建免检） |
| `src/tools/index.ts` | 修改 | 注册 Edit/Write |
| `src/permissions.ts` | 修改 | `isDangerous` + 高危命令 always 只存精确规则 |
| `src/api.ts` | 修改 | `maxRetries: 0`；chatStream 改 yield `{type:'text'\|'reasoning', delta}` |
| `src/loop.ts` | 修改 | text 事件加 `reasoning?: boolean` |
| `src/repl.ts` | 修改 | `/accept` 命令、高危警告、reasoning 暗色、排版修复 |
| `src/prompt.ts` | 修改 | 引用纪律强化一行 |
| `scripts/smoke-api.ts` | 修改 | 适配新 yield 类型 |
| `bench/fixtures/buggy-calc/*` | 新建 | 零依赖的含 bug 小项目（node --test） |
| `bench/run.ts` `bench/scenarios.json` | 修改 | fix 型场景：复制 fixture 到临时目录 + verifyCmd 判分 |

---

### Task 1: Edit 工具

**Files:**
- Create: `src/tools/edit.ts`
- Test: `test/tools.edit.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// test/tools.edit.test.ts
import { describe, it, expect } from 'vitest'
import { mkdtempSync, writeFileSync, readFileSync, utimesSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'
import { editTool } from '../src/tools/edit.js'
import { readTool } from '../src/tools/read.js'
import { makeCtx } from './helpers.js'

async function setup(content: string) {
  const dir = mkdtempSync(path.join(tmpdir(), 'dc-'))
  const f = path.join(dir, 'a.ts')
  writeFileSync(f, content)
  const ctx = makeCtx(dir)
  await readTool.call({ file_path: f }, ctx) // 正常流程：先 Read
  return { dir, f, ctx }
}

describe('Edit', () => {
  it('Read 过的文件可以替换，且 fileState 更新（可连续编辑）', async () => {
    const { f, ctx } = await setup('const a = 1\nconst b = 2\n')
    const out1 = await editTool.call({ file_path: f, old_string: 'const a = 1', new_string: 'const a = 10' }, ctx)
    expect(out1).toContain('已编辑')
    expect(readFileSync(f, 'utf8')).toContain('const a = 10')
    // 编辑后无需重新 Read 即可再编辑
    const out2 = await editTool.call({ file_path: f, old_string: 'const b = 2', new_string: 'const b = 20' }, ctx)
    expect(out2).toContain('已编辑')
  })

  it('未 Read 直接编辑被拒绝', async () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dc-'))
    const f = path.join(dir, 'b.ts')
    writeFileSync(f, 'x')
    const out = await editTool.call({ file_path: f, old_string: 'x', new_string: 'y' }, makeCtx(dir))
    expect(out).toContain('必须先用 Read')
  })

  it('Read 之后被外部修改 → 要求重新 Read', async () => {
    const { f, ctx } = await setup('hello')
    writeFileSync(f, 'hello!') // 外部修改
    utimesSync(f, new Date(), new Date(Date.now() + 5000)) // 确保 mtime 变化
    const out = await editTool.call({ file_path: f, old_string: 'hello', new_string: 'hi' }, ctx)
    expect(out).toContain('被外部修改')
  })

  it('old_string 不存在 → 可自我修正的错误', async () => {
    const { f, ctx } = await setup('abc')
    const out = await editTool.call({ file_path: f, old_string: 'xyz', new_string: 'q' }, ctx)
    expect(out).toContain('没有找到')
  })

  it('old_string 不唯一 → 报出现次数', async () => {
    const { f, ctx } = await setup('dup\ndup\n')
    const out = await editTool.call({ file_path: f, old_string: 'dup', new_string: 'one' }, ctx)
    expect(out).toContain('出现了 2 次')
  })

  it('replace_all 全部替换', async () => {
    const { f, ctx } = await setup('dup\ndup\n')
    const out = await editTool.call({ file_path: f, old_string: 'dup', new_string: 'one', replace_all: true }, ctx)
    expect(out).toContain('2 处')
    expect(readFileSync(f, 'utf8')).toBe('one\none\n')
  })

  it('new_string 含 $& 等不被正则替换特殊解释', async () => {
    const { f, ctx } = await setup('price = X')
    await editTool.call({ file_path: f, old_string: 'X', new_string: '$&100$$' }, ctx)
    expect(readFileSync(f, 'utf8')).toBe('price = $&100$$')
  })

  it('old_string 与 new_string 相同 → 拒绝', async () => {
    const { f, ctx } = await setup('same')
    const out = await editTool.call({ file_path: f, old_string: 'same', new_string: 'same' }, ctx)
    expect(out).toContain('相同')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tools.edit.test.ts`
Expected: FAIL —— `Cannot find module '../src/tools/edit.js'`

- [ ] **Step 3: 实现 `src/tools/edit.ts`**

```typescript
// src/tools/edit.ts
import { z } from 'zod'
import fs from 'node:fs'
import path from 'node:path'
import type { Tool, ToolContext } from './types.js'

const schema = z.object({
  file_path: z.string().describe('要编辑的文件路径'),
  old_string: z.string().describe('要被替换的原文（必须与文件内容逐字符一致，含缩进与换行）'),
  new_string: z.string().describe('替换后的新文本'),
  replace_all: z.boolean().optional().describe('替换所有出现处；默认 false（要求 old_string 在文件中唯一）'),
})

/** read-before-edit 闸门：必须 Read 过且此后未被外部修改。通过返回 null，否则返回给模型的错误信息。Write 覆盖已存在文件时复用。 */
export function checkFileState(p: string, ctx: ToolContext): string | null {
  const recorded = ctx.fileState.get(p)
  if (recorded === undefined) return `错误：编辑前必须先用 Read 工具读取 ${p}。`
  let stat: fs.Stats
  try {
    stat = fs.statSync(p)
  } catch {
    return `错误：文件不存在：${p}。`
  }
  if (stat.mtimeMs !== recorded) {
    return `错误：${p} 在你读取之后被外部修改过，请重新用 Read 读取最新内容再编辑。`
  }
  return null
}

export const editTool: Tool<typeof schema> = {
  name: 'Edit',
  description:
    '对文件做精确字符串替换。old_string 必须与文件内容逐字符一致（含缩进与换行）且默认要求唯一；不唯一时请提供更长的包含上下文的片段，或用 replace_all 全部替换。编辑任何文件前必须先用 Read 读取它。',
  inputSchema: schema,
  isReadOnly: false,
  needsPermission: input => `编辑 ${input.file_path}`,
  async call(input, ctx) {
    const p = path.resolve(ctx.cwd(), input.file_path)
    const stateErr = checkFileState(p, ctx)
    if (stateErr) return stateErr
    if (input.old_string === input.new_string) return '错误：new_string 与 old_string 相同，无需编辑。'
    const content = fs.readFileSync(p, 'utf8')
    const count = content.split(input.old_string).length - 1
    if (count === 0) {
      return '错误：old_string 在文件中没有找到。常见原因：缩进或空白与原文不一致。请用 Read 重新确认原文后重试。'
    }
    if (count > 1 && !input.replace_all) {
      return `错误：old_string 在文件中出现了 ${count} 次，无法唯一定位。请提供更长的、包含上下文的唯一片段，或使用 replace_all。`
    }
    // 不用 String.replace：new_string 含 $& $$ 等会被特殊解释
    let updated: string
    if (input.replace_all) {
      updated = content.split(input.old_string).join(input.new_string)
    } else {
      const idx = content.indexOf(input.old_string)
      updated = content.slice(0, idx) + input.new_string + content.slice(idx + input.old_string.length)
    }
    fs.writeFileSync(p, updated)
    ctx.fileState.set(p, fs.statSync(p).mtimeMs)
    return `已编辑 ${p}（替换 ${input.replace_all ? count : 1} 处）。`
  },
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run test/tools.edit.test.ts`
Expected: PASS（8 用例）

- [ ] **Step 5: Commit**

```bash
git add src/tools/edit.ts test/tools.edit.test.ts && git commit -m "feat: Edit tool with enforced read-before-edit"
```

---

### Task 2: Write 工具

**Files:**
- Create: `src/tools/write.ts`
- Test: `test/tools.write.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// test/tools.write.test.ts
import { describe, it, expect } from 'vitest'
import { mkdtempSync, writeFileSync, readFileSync, existsSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'
import { writeTool } from '../src/tools/write.js'
import { readTool } from '../src/tools/read.js'
import { makeCtx } from './helpers.js'

describe('Write', () => {
  it('新建文件无需先 Read，且自动创建父目录', async () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dc-'))
    const f = path.join(dir, 'sub/dir/new.txt')
    const out = await writeTool.call({ file_path: f, content: 'hello' }, makeCtx(dir))
    expect(out).toContain('已写入')
    expect(readFileSync(f, 'utf8')).toBe('hello')
  })

  it('覆盖已存在文件必须先 Read', async () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dc-'))
    const f = path.join(dir, 'a.txt')
    writeFileSync(f, 'old')
    const out = await writeTool.call({ file_path: f, content: 'new' }, makeCtx(dir))
    expect(out).toContain('必须先用 Read')
    expect(readFileSync(f, 'utf8')).toBe('old') // 未被覆盖
  })

  it('Read 后可覆盖，且 fileState 更新（可接着 Edit）', async () => {
    const dir = mkdtempSync(path.join(tmpdir(), 'dc-'))
    const f = path.join(dir, 'b.txt')
    writeFileSync(f, 'old')
    const ctx = makeCtx(dir)
    await readTool.call({ file_path: f }, ctx)
    const out = await writeTool.call({ file_path: f, content: 'brand new' }, ctx)
    expect(out).toContain('已写入')
    expect(readFileSync(f, 'utf8')).toBe('brand new')
    // 写入后 fileState 是新 mtime：继续 Edit 不应被拦
    const { editTool } = await import('../src/tools/edit.js')
    const out2 = await editTool.call({ file_path: f, old_string: 'brand', new_string: 'very' }, ctx)
    expect(out2).toContain('已编辑')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run test/tools.write.test.ts`
Expected: FAIL —— 模块不存在

- [ ] **Step 3: 实现 `src/tools/write.ts`**

```typescript
// src/tools/write.ts
import { z } from 'zod'
import fs from 'node:fs'
import path from 'node:path'
import type { Tool } from './types.js'
import { checkFileState } from './edit.js'

const schema = z.object({
  file_path: z.string().describe('要写入的文件路径'),
  content: z.string().describe('完整的文件内容（整文件覆盖写入）'),
})

export const writeTool: Tool<typeof schema> = {
  name: 'Write',
  description:
    '整文件写入，自动创建父目录。文件已存在时是覆盖操作，必须先用 Read 读取过该文件；新建文件无此要求。对已有文件做局部修改请优先用 Edit。',
  inputSchema: schema,
  isReadOnly: false,
  needsPermission: input => `写入 ${input.file_path}`,
  async call(input, ctx) {
    const p = path.resolve(ctx.cwd(), input.file_path)
    if (fs.existsSync(p)) {
      const stateErr = checkFileState(p, ctx)
      if (stateErr) return stateErr
    }
    fs.mkdirSync(path.dirname(p), { recursive: true })
    fs.writeFileSync(p, input.content)
    ctx.fileState.set(p, fs.statSync(p).mtimeMs)
    return `已写入 ${p}（${input.content.length} 字符）。`
  },
}
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `npx vitest run test/tools.write.test.ts && npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/tools/write.ts test/tools.write.test.ts && git commit -m "feat: Write tool with read-before-overwrite"
```

---

### Task 3: 注册 Edit/Write + REPL 接入 acceptEdits

**Files:**
- Modify: `src/tools/index.ts`, `src/repl.ts`
- Test: `test/tools.registry.test.ts`（更新断言）

- [ ] **Step 1: 更新注册表测试（先改测试，确认 FAIL）**

`test/tools.registry.test.ts` 中：

```typescript
  it('注册了 M2 的六个工具', () => {
    expect(allTools.map(t => t.name).sort()).toEqual(['Bash', 'Edit', 'Glob', 'Grep', 'Read', 'Write'])
  })
```

（替换原"注册了 M1 的四个工具"用例。）

Run: `npx vitest run test/tools.registry.test.ts`
Expected: FAIL —— 数组不等

- [ ] **Step 2: 修改 `src/tools/index.ts`**

```typescript
// src/tools/index.ts
import { zodToJsonSchema } from 'zod-to-json-schema'
import type { Tool } from './types.js'
import { readTool } from './read.js'
import { globTool } from './glob.js'
import { grepTool } from './grep.js'
import { bashTool } from './bash.js'
import { editTool } from './edit.js'
import { writeTool } from './write.js'

export const allTools: Tool<any>[] = [readTool, globTool, grepTool, bashTool, editTool, writeTool]

export function toApiTools(tools: Tool<any>[]) {
  return tools.map(t => ({
    type: 'function' as const,
    function: {
      name: t.name,
      description: t.description,
      parameters: zodToJsonSchema(t.inputSchema, { $refStrategy: 'none' }),
    },
  }))
}
```

Run: `npx vitest run test/tools.registry.test.ts`
Expected: PASS

- [ ] **Step 3: REPL 接入 acceptEdits（`src/repl.ts` 三处修改）**

a. import 行 `import type { Decision } from './permissions.js'` 改为：

```typescript
import type { Decision, PermissionMode } from './permissions.js'
```

b. `let thinking = false` 之后加一行：

```typescript
  let permMode: PermissionMode = opts.yolo ? 'yolo' : 'default'
```

c. `/help` 输出改为：

```typescript
      console.log('/model  flash↔pro 切换\n/think  thinking 模式开关\n/accept acceptEdits 模式开关（Edit/Write 免确认，Bash 仍确认）\n/exit   退出')
```

d. `/think` 命令分支之后新增：

```typescript
    if (line === '/accept') {
      if (opts.yolo) {
        console.log('当前是 yolo 模式，所有操作均已放行')
        continue
      }
      permMode = permMode === 'acceptEdits' ? 'default' : 'acceptEdits'
      console.log(`acceptEdits 模式：${permMode === 'acceptEdits' ? '开（Edit/Write 免确认，Bash 仍需确认）' : '关'}`)
      continue
    }
```

e. deps.permission 中 `mode: opts.yolo ? 'yolo' : 'default',` 改为：

```typescript
          mode: permMode,
```

- [ ] **Step 4: 全量回归 + 管道验证**

Run: `npm test && npm run typecheck`
Run: `printf '/accept\n/accept\n/exit\n' | DEEPSEEK_API_KEY=sk-dummy npx tsx src/index.ts`
Expected: 依次输出"acceptEdits 模式：开…"、"acceptEdits 模式：关"，正常退出

- [ ] **Step 5: Commit**

```bash
git add src/tools/index.ts src/repl.ts test/tools.registry.test.ts && git commit -m "feat: register Edit/Write and wire acceptEdits mode into REPL"
```

---

### Task 4: 高危命令识别

**Files:**
- Modify: `src/permissions.ts`, `src/repl.ts`
- Test: `test/permissions.test.ts`（追加）

- [ ] **Step 1: 写失败测试（追加到 test/permissions.test.ts）**

```typescript
// 新 describe，与现有 imports 并列追加 isDangerous：
// import 行改为 { matchRule, checkPermission, isDangerous, type PermissionContext, type Decision }

describe('isDangerous', () => {
  it('识别高危命令', () => {
    expect(isDangerous('rm -rf /tmp/x')).toBe(true)
    expect(isDangerous('rm -fr node_modules')).toBe(true)
    expect(isDangerous('sudo rm file')).toBe(true)
    expect(isDangerous('git push --force')).toBe(true)
    expect(isDangerous('git reset --hard HEAD~1')).toBe(true)
    expect(isDangerous('DROP TABLE users')).toBe(true)
  })
  it('普通命令不误报', () => {
    expect(isDangerous('npm test')).toBe(false)
    expect(isDangerous('rm file.txt')).toBe(false)
    expect(isDangerous('ls -la')).toBe(false)
    expect(isDangerous('git status --porcelain')).toBe(false)
  })
})

describe('checkPermission 高危分支', () => {
  it('高危命令 always 只持久化精确规则，不做前缀放宽', async () => {
    const rules: string[] = []
    const ctx = pc({ rules, saveRule: r => rules.push(r), ask: async () => 'always' })
    const tool = fakeTool('Bash', false, 'rm -rf /tmp/scratch')
    expect((await checkPermission(tool, {}, ctx)).ok).toBe(true)
    expect(rules).toEqual(['Bash(rm -rf /tmp/scratch)'])
    // 精确规则不匹配其他 rm -rf
    const tool2 = fakeTool('Bash', false, 'rm -rf /etc')
    let asked = false
    const ctx2 = pc({ rules, ask: async () => { asked = true; return 'no' } })
    await checkPermission(tool2, {}, ctx2)
    expect(asked).toBe(true)
  })
})
```

Run: `npx vitest run test/permissions.test.ts`
Expected: FAIL —— isDangerous 未导出

- [ ] **Step 2: 修改 `src/permissions.ts`**

在 `matchRule` 之前加：

```typescript
const DANGEROUS_PATTERNS = [
  /\brm\s+(-\w+\s+)*-\w*r\w*f/i, // rm -rf 及参数变体
  /\brm\s+(-\w+\s+)*-\w*f\w*r/i, // rm -fr
  /\bsudo\b/,
  /--force\b/,
  /\bgit\s+reset\s+--hard\b/,
  /\bdrop\s+(table|database)\b/i,
  /\bmkfs\b/,
  /\bdd\s+if=/,
]

/** 高危命令：权限弹窗加警告，always 不做前缀放宽只存精确规则 */
export function isDangerous(desc: string): boolean {
  return DANGEROUS_PATTERNS.some(re => re.test(desc))
}
```

`checkPermission` 的 always 分支中，`const pat = ...` 改为：

```typescript
    const pat = tool.name === 'Bash'
      ? isDangerous(desc)
        ? firstLine // 高危：只精确放行本条命令
        : firstLine.split(' ').slice(0, 2).join(' ') + ':*'
      : desc.replace(/\n/g, ' ')
```

- [ ] **Step 3: REPL 弹窗加警告（`src/repl.ts`）**

import 行追加 isDangerous：

```typescript
import { isDangerous, type Decision, type PermissionMode } from './permissions.js'
```

`ask` 函数体开头加：

```typescript
    if (isDangerous(desc)) process.stdout.write(`\n${C.red}⚠ 高危操作；选 always 也只会精确放行这一条命令${C.reset}`)
```

- [ ] **Step 4: 全量回归**

Run: `npm test && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/permissions.ts src/repl.ts test/permissions.test.ts && git commit -m "feat: dangerous command detection with exact-only always rules"
```

---

### Task 5: reasoning 流事件化 + maxRetries:0

chatStream 的 yield 类型从 `string` 改为 `{type:'text'|'reasoning', delta}`，让 REPL 能区分渲染；同时关掉 openai SDK 自带重试（与 withRetry 叠加）。**本任务会改 4 个文件 + 3 个测试文件的 mock/断言，必须一次改完跑全量。**

**Files:**
- Modify: `src/api.ts`, `src/loop.ts`, `src/repl.ts`, `scripts/smoke-api.ts`
- Test: `test/api.assembler.test.ts`, `test/loop.test.ts`（更新）

- [ ] **Step 1: 先更新 assembler 测试断言（确认 FAIL）**

`test/api.assembler.test.ts` 全部 `a.push(...)` 的断言改为新返回类型：

```typescript
    // it('拼装文本增量并逐段返回')：
    expect(a.push({ choices: [{ delta: { content: '你' } }] })).toEqual({ text: '你', reasoning: '' })
    expect(a.push({ choices: [{ delta: { content: '好' } }] })).toEqual({ text: '好', reasoning: '' })

    // it('空 choices 分片不崩溃')：
    expect(a.push({ choices: [] })).toEqual({ text: '', reasoning: '' })
    expect(a.push({})).toEqual({ text: '', reasoning: '' })

    // it('content 与 tool_calls 混在同一 delta')：
    expect(a.push({ choices: [{ delta: { content: '我来读', tool_calls: [{ index: 0, id: 'c9', function: { name: 'Read', arguments: '{}' } }] } }] })).toEqual({ text: '我来读', reasoning: '' })

    // it('reasoning_content 流式返回供显示，但不进 content')：
    expect(a.push({ choices: [{ delta: { reasoning_content: '思考中…' } }] })).toEqual({ text: '', reasoning: '思考中…' })
    expect(a.push({ choices: [{ delta: { content: '答案' } }] })).toEqual({ text: '答案', reasoning: '' })
    expect(a.finish().content).toBe('答案')
```

Run: `npx vitest run test/api.assembler.test.ts`
Expected: FAIL（push 仍返回 string）

- [ ] **Step 2: 改 `src/api.ts` 三处**

a. `Assembler.push` 的文本返回段改为：

```typescript
    // reasoning_content（thinking 模式的思考流）只用于显示，不进 content/messages
    const text: string = delta.content ?? ''
    this.content += text
    return { text, reasoning: delta.reasoning_content ?? '' }
  }
```

（方法签名改 `push(chunk: any): { text: string; reasoning: string }`。）

b. `createClient` 的 `new OpenAI({...})` 增加一行 `maxRetries: 0,`（注释：重试统一由 withRetry 负责，避免与 SDK 自带重试叠加）。

c. `chatStream` 签名与消费段改为：

```typescript
export type StreamDelta = { type: 'text' | 'reasoning'; delta: string }

export async function* chatStream(client: OpenAI, opts: ChatOptions): AsyncGenerator<StreamDelta, ChatResult> {
```

```typescript
  const asm = new Assembler()
  for await (const chunk of stream as any) {
    const { text, reasoning } = asm.push(chunk)
    if (reasoning) yield { type: 'reasoning', delta: reasoning }
    if (text) yield { type: 'text', delta: text }
  }
  return asm.finish()
```

- [ ] **Step 3: 改 `src/loop.ts` 两处**

a. LoopEvent 的 text 变体加可选标志：

```typescript
  | { type: 'text'; delta: string; reasoning?: boolean }
```

b. 流消费处 `yield { type: 'text', delta: step.value }` 改为：

```typescript
        yield {
          type: 'text',
          delta: step.value.delta,
          ...(step.value.type === 'reasoning' ? { reasoning: true } : {}),
        }
```

- [ ] **Step 4: 改 `test/loop.test.ts` 的 mock**

脚本化 chatStream 的 `for (const d of scene.deltas ?? []) yield d` 改为：

```typescript
      for (const d of scene.deltas ?? []) yield { type: 'text', delta: d }
```

（断言 `events.filter(e => e.type === 'text').map(e => e.delta)` 无需变。）

- [ ] **Step 5: 改 `src/repl.ts` 渲染与 `scripts/smoke-api.ts`**

repl 的 text 事件分支改为（reasoning 暗色）：

```typescript
        if (ev.type === 'text') process.stdout.write(ev.reasoning ? `${C.dim}${ev.delta}${C.reset}` : ev.delta)
```

smoke-api.ts 的消费循环改为：

```typescript
let step
while (!(step = await gen.next()).done) process.stdout.write(step.value.delta)
```

- [ ] **Step 6: 全量回归 + 真机冒烟**

Run: `npm test && npm run typecheck`
Run: `DEEPSEEK_API_KEY=<真实key> npx tsx scripts/smoke-api.ts "1+1=?只答数字"`
Expected: 测试全绿；冒烟输出 "2" 且 completion_tokens 为个位数（thinking 仍默认关）

- [ ] **Step 7: Commit**

```bash
git add src/api.ts src/loop.ts src/repl.ts scripts/smoke-api.ts test/api.assembler.test.ts test/loop.test.ts && git commit -m "feat: typed reasoning stream events and disable SDK builtin retry"
```

---

### Task 6: REPL 排版修复 + 引用纪律 prompt 强化

**Files:**
- Modify: `src/repl.ts`, `src/prompt.ts`

- [ ] **Step 1: 修排版（`src/repl.ts` 两处）**

a. turn_end 的 write 末尾补换行（解决 usage 行与下轮文本粘连、与提示符双空行）：

```typescript
          process.stdout.write(
            `\n${C.dim}[入 ${ev.usage.prompt_tokens}（缓存命中 ${ev.usage.prompt_cache_hit_tokens}）出 ${ev.usage.completion_tokens} | 累计 入 ${totals.input} 出 ${totals.output}]${C.reset}\n`,
          )
```

b. 主输入提示从 `'\n› '` 改为 `'› '`：

```typescript
    const line = (await question('› ')).trim()
```

- [ ] **Step 2: 引用纪律（`src/prompt.ts`）**

工作守则中 `- 引用代码位置用 file_path:line 格式。` 改为：

```typescript
- 提到任何函数、文件或机制时，必须给出其文件路径（如 src/loop.ts:42），不要只说名字。
```

- [ ] **Step 3: 回归 + 管道排版目测**

Run: `npm test && npm run typecheck`
Run: `printf '/help\n/exit\n' | DEEPSEEK_API_KEY=sk-dummy npx tsx src/index.ts`
Expected: 输出无粘连、无连续空行

- [ ] **Step 4: Commit**

```bash
git add src/repl.ts src/prompt.ts && git commit -m "fix: REPL spacing and strengthen citation discipline in prompt"
```

---

### Task 7: bench 写任务场景（fix 型）

**Files:**
- Create: `bench/fixtures/buggy-calc/package.json`, `bench/fixtures/buggy-calc/calc.mjs`, `bench/fixtures/buggy-calc/calc.test.mjs`
- Modify: `bench/run.ts`, `bench/scenarios.json`

- [ ] **Step 1: 写 fixture（零依赖，node --test）**

`bench/fixtures/buggy-calc/package.json`：

```json
{
  "name": "buggy-calc",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test" }
}
```

`bench/fixtures/buggy-calc/calc.mjs`：

```javascript
// 区间求和：sumRange(1, 3) 应为 1+2+3 = 6
export function sumRange(a, b) {
  let s = 0
  for (let i = a; i < b; i++) s += i
  return s
}
```

`bench/fixtures/buggy-calc/calc.test.mjs`：

```javascript
import test from 'node:test'
import assert from 'node:assert'
import { sumRange } from './calc.mjs'

test('sumRange 包含两端', () => {
  assert.equal(sumRange(1, 3), 6)
  assert.equal(sumRange(5, 5), 5)
})
```

验证 fixture 本身：`cd bench/fixtures/buggy-calc && npm test`，Expected: **FAIL**（这是故意的 bug：循环少了 `<=`）。

- [ ] **Step 2: `bench/scenarios.json` 追加场景**

scenarios 数组末尾加：

```json
    {
      "id": "s6-fix-bug",
      "type": "fix",
      "fixture": "fixtures/buggy-calc",
      "prompt": "运行 npm test，找出失败原因，修复代码使所有测试通过。做最小改动，不要改测试文件。",
      "verifyCmd": "npm test"
    }
```

- [ ] **Step 3: 改 `bench/run.ts` 支持 fix 型场景**

a. Scenario 接口改为：

```typescript
interface Scenario {
  id: string
  repo?: string
  fixture?: string
  type?: 'qa' | 'fix'
  prompt: string
  expected?: string[]
  verifyCmd?: string
}
```

b. import 区追加 `import os from 'node:os'`，并在 `stripAnsi` 附近加两个 helper：

```typescript
/** fix 型场景：把 fixture 复制到临时目录（每次 run 全新副本，互不污染） */
function materializeRepo(s: Scenario): string {
  if (s.fixture) {
    const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'bench-'))
    fs.cpSync(path.join(BENCH_DIR, s.fixture), dir, { recursive: true })
    return dir
  }
  return s.repo!
}

function verifyFix(cwd: string, cmd: string): boolean {
  try {
    execSync(cmd, { cwd, stdio: 'ignore', timeout: 60_000 })
    return true
  } catch {
    return false
  }
}
```

c. `runDeepcode` 与 `runCC` 的签名都加第三个参数 `repoDir: string`，函数体内 `cwd: s.repo` 改为 `cwd: repoDir`（runDeepcode 的 spawnCapture cwd 与 runCC 的 spawnCapture cwd 各一处）。

d. 两个 run 函数的评分段改为（fix 型按 verifyCmd 判分）：

runDeepcode 末尾（usage 解析之后）：

```typescript
  let success: boolean
  let missed: string[] = []
  if (s.type === 'fix') {
    success = verifyFix(repoDir, s.verifyCmd!)
    if (!success) missed = ['verifyCmd 未通过']
  } else {
    const modelText = clean
      .split('\n')
      .filter(l => !/^(deepcode \||›|⏺ |\s*⎿ |\[入 )/.test(l))
      .join('\n')
    ;({ success, missed } = grade(modelText, s.expected!))
  }
  return { scenario: s.id, track: 'deepcode', run, success, missedKeywords: missed, wallMs, tokensIn, tokensOut, cacheHit, toolCalls }
```

runCC 的 try 块内同理：

```typescript
    const text: string = j.result ?? ''
    let success: boolean
    let missed: string[] = []
    if (s.type === 'fix') {
      success = verifyFix(repoDir, s.verifyCmd!)
      if (!success) missed = ['verifyCmd 未通过']
    } else {
      ;({ success, missed } = grade(text, s.expected!))
    }
```

（return 中 `success: success && !j.is_error`、missedKeywords: missed 保持。）

e. main 的调度循环改为每次 run 先 materialize：

```typescript
  for (const s of todo) {
    for (let run = 1; run <= RUNS; run++) {
      if (ONLY_TRACK !== 'cc') {
        process.stderr.write(`[${s.id}] deepcode run ${run}...\n`)
        results.push(await runDeepcode(s, run, materializeRepo(s)))
      }
      if (ONLY_TRACK !== 'deepcode') {
        process.stderr.write(`[${s.id}] cc run ${run}...\n`)
        results.push(await runCC(s, run, materializeRepo(s)))
      }
    }
  }
```

f. timeout：runDeepcode 的 timeoutMs 对 fix 型放宽——`timeoutMs: s.type === 'fix' ? 360_000 : 240_000`。

- [ ] **Step 4: 单轨验证 fix 场景跑通**

Run: `DEEPSEEK_API_KEY=<真实key> npx tsx bench/run.ts --scenario s6-fix-bug --runs 1 --track deepcode`
Expected: s6-fix-bug deepcode 1/1 成功（deepcode 自主跑测试→读 calc.mjs→Edit 修复→测试通过）

- [ ] **Step 5: Commit**

```bash
git add bench/ && git commit -m "feat: bench fix-type scenario with verify-command grading"
```

---

### Task 8: M2 验收

- [ ] **Step 1: 自主修真 bug 全流程（默认权限模式，验证 acceptEdits 体验）**

在 fixture 临时副本上手动跑一次完整交互（不是 --yolo）：

```bash
TMP=$(mktemp -d) && cp -r bench/fixtures/buggy-calc/* $TMP && cd $TMP
printf '/accept\n运行 npm test，找出失败原因并修复代码使测试通过，最小改动\ny\n/exit\n' | DEEPSEEK_API_KEY=<真实key> npx tsx /Users/silas/loop/deepcode/src/index.ts
cd $TMP && npm test
```

Expected: `/accept` 后 Edit 免确认（只有跑 npm test 的 Bash 需要那个 `y`）；最终 `npm test` 通过。

- [ ] **Step 2: 跑第二轮 bench 全量**

Run: `DEEPSEEK_API_KEY=<真实key> npx tsx bench/run.ts --runs 2`（后台跑，约 40 分钟）
Expected: 生成新结果文件；s6 两轨成功率与 token 对比是 M2 的核心数据

- [ ] **Step 3: 写验收文档 `docs/specs/m2-acceptance.md`**

内容：验收清单结果（修真 bug 全流程/读改闸门体验/bench 第二轮表格摘要）、新发现的待调 prompt 项、带入 M3 的权衡清单。

- [ ] **Step 4: Commit + tag + push**

```bash
git add -A && git commit -m "docs: M2 acceptance results" && git tag v0.2.0-m2 && git push && git push --tags
```

---

## 自查记录

- spec 覆盖：§2 Edit/Write 防呆细节（read-before-edit、唯一性报错、mtime 失效）→ Task 1/2；§2 acceptEdits → Task 3；m1-acceptance 待办五项 → Task 4（高危警告）/5（maxRetries、reasoning 样式）/6（排版、引用纪律）；§7 M2 验收 → Task 7/8 ✓
- 占位符：无 ✓
- 类型一致性：`checkFileState` 在 Task 1 定义、Task 2 复用；`StreamDelta` 在 Task 5 定义并被 loop/smoke 消费；`PermissionMode`/`isDangerous` 导出与 import 对齐 ✓
